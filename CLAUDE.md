# CLAUDE.md

**Đọc trước mỗi phiên** (nạp tự động — không cần nhắc lại):

- Cửa vào dự án (dự án là gì, chạy/test, ràng buộc cứng, quy trình): @AGENTS.md
- Business rules toàn hệ thống (GEN/AUTH/INT/CAMP/PAY/AI/BC/SEC/DATA): @docs/rules.md
- Trạng thái hiện tại + bước kế tiếp (handoff, cập nhật mỗi phiên): @docs/progress.md

Chi tiết còn lại nằm trong [`docs/`](docs/) — mở đúng file khi cần:
`docs/work-division.md` (phạm vi/phân việc), `docs/tasks.md` (task + lệnh xác minh),
`docs/architecture.md` (§5 quy ước, §6 routing/mã lỗi), `docs/services/<service>.md`.

**Quy tắc làm việc:** plan trước → chờ duyệt mới sửa file/chạy lệnh thay đổi (chi tiết trong AGENTS.md).

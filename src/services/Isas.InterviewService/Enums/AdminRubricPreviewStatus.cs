namespace Isas.InterviewService.Enums;

/// <summary>Trạng thái một lượt chấm thử bộ chuẩn. Lưu string (GEN-2).</summary>
public enum AdminRubricPreviewStatus
{
    Running,
    Succeeded,
    Failed
}

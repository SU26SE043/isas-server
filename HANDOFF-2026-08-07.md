# Prompt mở phiên tiếp — dán nguyên khối này

```
Tiếp tục vòng vá từ e2e 2026-08-07 của ISAS.

TRẠNG THÁI: branch `fix/e2e-20260807-quick-batch` đã push (tip `a62691a`, 9 commit,
CHƯA mở PR, CHƯA merge). Gate cuối: build 0 error · .NET 1815 · pytest 490 ·
has-pending-model-changes No changes ×4 · 2 migration CHƯA apply DB thật.
Đã vá 9/13 task §S12: Q2 Q3 Q4 Q5 Q7 Q8 Q10 Q15 Q16.
Chi tiết đầy đủ: docs/progress.md §2026-08-07 và docs/tasks.md §S12.

ĐỌC TRƯỚC: AGENTS.md · docs/rules.md · docs/progress.md (mục 2026-08-07 ở gần cuối,
trước §Pha hiện tại) · docs/tasks.md §S12.

⚠ ĐỪNG trích số test trong progress.md làm mốc — ĐO thật trên commit đang làm.
   pytest chạy bằng `src/services/Isas.AIService/.venv/bin/pytest` (python3 hệ thống
   KHÔNG có pytest).

CHỌN MỘT TRONG BỐN HƯỚNG (hỏi tôi nếu không rõ nên làm cái nào):

A) MỞ PR + DEPLOY + L3 — khép vòng hiện tại. L3 chưa chạy cho cả 9 task. Đáng nhất:
   gọi ẩn danh 8 endpoint AI → phải 401 VÀ `ai_usage_logs` KHÔNG tăng (vế sau mới là
   bằng chứng thật). Kèm 3 việc bắt buộc khi deploy:
   · env `Internal:Token` phải có trong Campaign + Interview — thiếu thì
     AiServiceCriteriaSuggester hỏng IM LẶNG (nuốt lỗi, rơi về tiêu chí mặc định) và
     AiServiceSessionSummarizer để overall_comment null; KHÔNG lỗi nào hiện ra.
   · apply 2 migration (Campaign backfill slot_id — Postgres-only, không test nào phủ,
     verify trên Postgres throwaway trước).
   · dọn dữ liệu prod mà vá code không dọn được: phantom session_criterion_scores
     (user ĐÃ CHỐT XOÁ) · 1 dòng cờ ngoại lai của Q4.

B) 4 TASK CÒN CHẶN của §S12:
   · Q1 — user đã chốt tách PR RIÊNG (ASPNETCORE_ENVIRONMENT=Production vào
     deploy/compose.yaml, KHÔNG sed trên server; OPS6 tái phát lần 6).
   · Q9 — đổi hợp đồng API /practice/rubrics, cần lockstep FE repo. Hệ quả đang sống:
     GET trả 14 tiêu chí Σweight=2.0 và PUT chính payload đó → 400 (API tự từ chối
     chính nó); ứng viên KHÔNG có đường nào khai rubric riêng cho buổi tiếng Anh.
   · Q12 — CẦN TEAM CHỐT trước khi code: pass_score_pct đang so như ĐIỂM TUYỆT ĐỐI,
     nên HR khai maxScore=10 + ngưỡng "50%" thì KHÔNG ứng viên nào pass được.
   · Q14 — repo FE (thêm 1 trường idempotency key vào GrantCreditRequest).

C) OPS phần server (repo đã sửa, chưa áp lên `~/docker/main/docker-compose.yml`):
   OPS8 `Internal__CallbackBase` → tên container (sàng lọc CV B2B đang chết: AI chấm
   xong rồi callback timeout → nack → republish vô hạn) · OPS9 bật song ngữ cần đủ
   BA chỗ: Interview__Bilingual__Enabled + Campaign__Bilingual__Enabled +
   BILINGUAL_ALLOWED_LANGUAGES trên CẢ aiapi LẪN aiworker.

D) Việc khác trong docs/tasks.md §CHƯA XONG (~45 việc: S10 R3/R6/R8-R14, tiered
   subscription T1-T13, hardening, tính năng chưa xây).

CÁCH LÀM (quy ước repo, giữ nguyên):
- Plan trước → chờ tôi duyệt mới sửa file/chạy lệnh thay đổi.
- 1 đợt = 1 branch → 1 PR. Commit nguyên tử.
- Task tiền/bảo mật PHẢI mutation-check: vô hiệu guard → test tương ứng phải ĐỎ.
  Harness phải TỰ CHỨNG MINH nó chạy đúng thư mục (đối chứng dương: đếm test, so với
  số của worktree này) — đã sai 3 kiểu, xem memory mutation-check.
  Mutation ra XANH thì ĐIỀU TRA, đừng nhận.
- Chia agent thì chia theo SỞ HỮU FILE, không theo task; mỗi worker 1 worktree riêng
  pin base SHA; brief ghi rõ file worker KHÔNG được sửa (kèm tên worker đang giữ).
```

---

## Ngữ cảnh nhanh (không cần đọc nếu đã mở phiên bằng prompt trên)

**Nguồn cơn:** vòng e2e API toàn hệ 2026-08-07, 6 agent song song, ~450 lượt kiểm qua gateway thật, PayOS tiền thật 2.000₫ ra và về, lần đầu bật cờ song ngữ. 22 báo cáo FAIL → 15 lỗi riêng biệt → `docs/tasks.md` §S12 `Q1`–`Q16`.

**Đã vá 9, qua 2 đợt trên cùng branch:**

| Đợt | Task |
|---|---|
| 1 | `Q5` analytics 500 (4 service) · `Q8` breakdown trộn ngôn ngữ · `Q15` trần chuỗi B2B · `OPS8` repo |
| 2 | `Q3` đổi mật khẩu thu hồi phiên · `Q4` giả mạo cờ + `Q7` slot chết · `Q2` gate 13/13 endpoint AI · `Q10` bài giảng EN + `Q16` câu hỏi cụt |

**Ba thứ đáng nhớ hơn bản thân bản vá:**

1. **Bug trong harness mutation** — `def run(cmd, cwd=ROOT)` gán mặc định lúc định nghĩa hàm, nên worker chạy từ worktree riêng mutate đúng file nhưng test ở worktree chính. Một worker nhận 5/5 XANH và suýt ship fix bảo mật chưa hề được test. Biến thể **thứ ba** của *"source đúng không chứng minh binary/thư mục đang test là source đó"*.
2. **Bốn phát hiện của worker vượt brief** — nhánh `if not expected` (server chưa cấu hình token) có 0 coverage toàn repo · khe nối renderer↔retry-feedback chưa test · `face-check` nguy hiểm hơn `flags` vì `sessionId` nuôi cả khoá S3 · suite slot tự set `SlotId` nên mù việc guard không bao giờ có dữ liệu.
3. **Hai con số baseline trong `progress.md` lệch thực tế** (Interview 552 vs đo 592; pytest 402 vs đo 403) — đã đính chính, nhưng đó là dấu hiệu doc trôi ở đúng chỗ hay được trích làm mốc.

**`Q16` nguyên nhân thật CHƯA chứng minh** — mới là giả thuyết có tiền lệ. Log `finish_reason` + `candidates_token_count` vừa thêm là thứ phân xử ở L3: `MAX_TOKENS` ⇒ cắt truyền; `STOP` + `candidates_token_count` nhỏ ⇒ model tự dừng, bản vá prompt là đòn bẩy đúng.

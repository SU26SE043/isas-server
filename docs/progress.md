# ISAS — Progress / Handoff

> Trạng thái hiện tại + bước kế tiếp, để phiên/người mới nối tiếp nhanh. Kế hoạch đầy đủ & phân việc: [work-division.md](work-division.md). Lý do quyết định: [decisions.md](decisions.md).
> **Cập nhật mỗi khi đổi trạng thái** (tan ca). Cập nhật lần cuối: **2026-07-12** (vòng giám sát: **D1·BC6·E2·P1** (vòng 1) + **P7·E3·E4·BC13** (vòng 2) + **P4·C11·E8** (vòng 3) + **P5·C12·BC11** (vòng 4) + **P6·E5·BC9** (vòng 5) + **P8a·E6·BC7** (vòng 6) + **E7·C13·BC2** (vòng 7) + **C14·BC8·BK7** (vòng 8) + **BK11·BK1·BK12** (vòng 9 — bug-fix) + **P2·C15·BC12** (vòng 10) + **BC14·BK4·BC10-AI** (vòng 11) + **D2** (vòng 12 — solo cross-service, membership redesign) + **BC10·P3·D3** (vòng 13 — 2 worker sạch + D3 kèm) + **BC15·D4·P8b** (vòng 14 — 3 worktree thật, 0 race) + **I2·A4** (vòng 15 — 2 worker, frontier mỏng) + **BK17·BK18·BK16** (vòng 16 — BK-cleanup, 3 worktree thật) + **E9** (vòng 17 — SOLO cross-service Interview+AIService, chấm neo mức) + **E10** (vòng 18 — SOLO cross-service, self-consistency median+needs_review) + **E11·A6** (vòng 19 — E11 đóng chuỗi chất lượng chấm + A6 org nhiều thành viên) + **BC7b** (vòng 20 — wire billing CV analysis, chốt BK5) + **BK15·BK6·A6b** (vòng 21 — backlog cleanup, 3 worktree disjoint) + **A5** (vòng 22 — SOLO cross-cutting, auth gate `[Authorize(Roles)]` mọi service) + **BC16** (vòng 23 — rubric cá nhân B2C candidate-owned) passing, integrated vào `docs/sync-design-d18-d21`). Cập nhật lần cuối vòng 23: **2026-07-13**.

## Pha hiện tại
**Feature engine cho cả 2 dòng đã đủ trên nhánh tích hợp — pha còn lại = hardening + e2e thật.** Thiết kế chốt **D1–D23**. **23 vòng giám sát** (multi-agent supervised) đã tích hợp vào nhánh **`docs/sync-design-d18-d21`** (**451 .NET test + 53 pytest**, `dotnet build` 0 error) — xem §Vòng tables bên dưới cho từng vòng.

> **⚠ Main-vs-branch:** `main` mới có nền pre-vòng-1 (**A1–A3** PR #23 · **C1–C10** PR #22 · **I1** PR #24 · **E1** `796d8bb`). **Toàn bộ 19 vòng (E2→E11 · P1–P8 · D1–D4 · C11–C15 · BC2–BC15 · A4/A6 · I2 · BK cleanup) nằm ở nhánh `docs/sync-design-d18-d21`, CHƯA merge `main`, CHƯA push.** Nhánh này là nguồn trạng thái hiện hành.

**Đã có (nhánh tích hợp):** B2C luyện+lịch sử+ví credit+tổng kết điểm/nhận xét+phân tích CV+roadmap ôn tập (BC*); B2B org nhiều thành viên (A1–A3/A6)+campaign/tiêu chí (C*)+lọc CV (C13–15)+membership join→start (D1–D4)+ranking/result/export (E4–6)+chất lượng chấm neo-mức/self-consistency/nhận-xét (E9–11); thanh toán credit/PayOS (P1–P8, prepaid+postpaid); **auth gate `[Authorize(Roles)]` mọi service (A5 ✅ v22)**. **Còn lại:** Phase 0 (compose máy sạch/make), verify tay (PayOS sandbox · e2e B2B+B2C compose/broker/Gemini · A5 live 401/403), backlog follow-up (§Backlog tasks.md).

## Trạng thái branch / task (so với `main`)
| Mục | Nội dung | Trạng thái |
|---|---|---|
| `main` | engine B2C + Auth(+Org) + Campaign + Gateway; Interview thêm I1 | ✅ chạy |
| Auth org (A1–A3) | `organizations`/`org_members` (migration `AddOrganizations`) + JWT mang `org_id`/`org_role` + `POST /auth/register-org` | ✅ **merged main (PR #23)** · test ✅ · ⚠ e2e HTTP chờ stack chạy |
| Campaign (C1–C10) | 6 bug + soft-delete (C9) + lifecycle (C7) + snake_case + publish/`campaign_criteria` Σ=1 (C8) + `audit_logs` (C10) + AI `/suggest-criteria` (Gemini + fallback) + Dockerfile/CI/gateway route | ✅ **merged main (PR #22)** · 34 unit test ✅ · thiếu M3/M4/M5 (distribution/ranking/export), `org_id` (code còn `employer_id`) |
| I1 | session B2B nhận `campaign_id` + materialize tiêu chí → `rubric_criteria(campaign_id)` (idempotent theo campaign) | ✅ **merged main (PR #24)** · test ✅ (2/2) · ⚠ HTTP entry B2B chờ **D2** |
| E1 | chấm B2B theo tiêu chí campaign (chọn tiêu chí branch theo `campaign_id` ở `AnswerService` + `StuckAnswerRepublisher`) | ✅ **merged main (commit 796d8bb)** · `dotnet test` 31/31 ✅ · ⚠ e2e đầy đủ chờ D2 |
| `features/payment-b2c` | PaymentService (Order/Package/PayOS theo `user_id`) — **chưa có folder trong tree `main`** | 🟡 cần refactor: `credit_accounts(owner_type)`, reserve/consume/release, postpaid + hóa đơn, active-polling |

### Vòng giám sát 2026-07-11 — 4 task passing (branch tích hợp `docs/sync-design-d18-d21`)
| Task | Nội dung | Trạng thái |
|---|---|---|
| **E2** | phát `SessionScored{SessionId,CampaignId?,CandidateId,TotalScore,ScoredAt}` từ `SessionScoringNotifier` (2 chỗ đóng `Scored`), best-effort | ✅ **passing (e2eaad4)** · Interview test 33/33 · ⚠ 2 điểm thiết kế chờ team chốt (tên exchange `interview.events`/`session.scored`; công thức `TotalScore` áp chung B2B/B2C) |
| **D1** | `campaign_invitations` + magic-link + `POST /campaign/{id}/invitations` (dedup/cap/`failed[]`) + `InvitationEmailPublisher` (RabbitMQ) | ✅ **passing (82c9faa)** · Campaign test 14/14 · `campaign_candidate_id` nullable không FK (chờ C13) · chưa wire `org_id` · ⚠ e2e email chờ stack |
| **BC6** | AIService `POST /analyze-cv` sync (1 Gemini call, AI không ghi DB) + anti prompt-injection | ✅ **passing (9cfa622)** · pytest 11/11 · queue B2B (C14) chưa build |
| **P1** | PaymentService vào tree + `credit_accounts(owner_type)`/`credit_reservations`/`credit_transactions` + migration `InitPaymentDb` (không apply Neon) | ✅ **passing (dfd57ce)** · Payment test 4/4 (P0.4 xong) · bỏ `CreditService`/`PayOSService` cũ (D15 retire `UserCredit`) → reimplement P2/P5 · chưa vào CI/compose |

> Base test sau tích hợp: **Auth 4 · Campaign 14 · Interview 33 · Payment 4 = 55 pass**, `dotnet build` 0 error.

### Vòng giám sát 2026-07-11 (vòng 2) — 4 task passing
| Task | Nội dung | Trạng thái |
|---|---|---|
| **P7** | `OrderCodeGenerator` time+random (`yyMMddHHmmss`+4 random), guard ≤2^53−1, retry ≤10 khi đụng UNIQUE (index sẵn từ P1) | ✅ **passing (7ffe1c9)** · Payment 7/7 · không migration mới · chưa wire `OrderService` (P2) |
| **E3** | `SessionAbandoned` + `SessionAbandonSweeper` (2 phút) phát key `session.abandoned` trên `interview.events` | ✅ **passing (50cab51)** · Interview 38/38 · ⚠ deadline placeholder `CreatedAt+30min` (`campaigns.expires_at` không đọc xuyên service) → I2 materialize deadline lên session; sweeper là plumbing forward |
| **E4** | `SessionScoredConsumer` (queue `campaign.ranking`) → upsert `campaign_rankings` idempotent theo `session_id`; B2C no-op | ✅ **passing (19fb040)** · Campaign 17/17 · migration `AddCampaignRankings` (không apply Neon) · re-anchor v2 sau khi base có D1 |
| **BC13** | AIService 3 endpoint roadmap sync (`/generate-roadmap`·`/generate-lesson-theory`·`/summarize-roadmap`), không ghi DB | ✅ **passing (b8d018f)** · pytest 29/29 (11 BC6+18) · layer trên BC6 · BC12/14/15 (DB) chưa build |

> Base test sau vòng 2: **Auth 4 · Payment 7 · Campaign 17 · Interview 38 = 66 .NET pass** (+ AIService pytest 29), `dotnet build` 0 error.
> **⚠ Lưu ý điều phối (worktree):** worker isolation tạo worktree off `a4c80d5` (merge-base), KHÔNG off tip base → 2 worker (E4/BC13) phải re-anchor onto tip (`git switch -c <br> <base-sha>`; worktree chia object store nên với tới được); P7/E3 tự nhận ra & tự sửa. **Vòng sau: brief phải ghi rõ base SHA để worker tạo nhánh off đúng tip.**

### Vòng giám sát 2026-07-11 (vòng 3) — 3 task passing (3 worker song song, mỗi service 1 worker)
| Task | Nội dung | Trạng thái |
|---|---|---|
| **P4** | `POST /internal/credits/reserve` (`X-Internal-Token`) → `CreditAccountService.ReserveAsync`: reservation chèn trước (UNIQUE(session_id)=khoá idempotency) + bút toán atomic `ExecuteUpdate WHERE remaining≥1` → hết → **402** no-orphan | ✅ **passing (0409ba4)** · Payment 13/13 (+6) · reuse schema P1 (không migration) · ⚠ no-wallet→402(block); postpaid→P8a · chưa wire P2 |
| **C11** | JD/Criteria nhập **text trực tiếp** (`jdText`/`criteriaText`) — text ưu tiên file; publish vẫn sinh `campaign_criteria` từ text | ✅ **passing (4425461)** · Campaign 22/22 (+5) · không migration (cột có từ `InitialCreate`) · ⚠ `CreateCampaign` set `CreatedAt/UpdatedAt` trong code (SQLite-testable) |
| **E8** | Guard điểm C# ở callback chấm: criterion ngoài rubric session → bỏ; điểm → kẹp `[0,maxScore]` (INT-9); áp B2B & B2C | ✅ **passing (2dfb3c3)** · Interview 41/41 (+3) · không migration · defense-in-depth cho callback (dùng branch rubric E1) |

> Base test sau vòng 3: **Auth 4 · Payment 13 · Campaign 22 · Interview 41 = 80 .NET pass** (+ AIService pytest 29), `dotnet build` 0 error. Cả 3 worker được pin base SHA `6e1d93a` trong brief → re-anchor sạch (không lệch merge-base như vòng 2). Merge `--no-ff` vào `docs/sync-design-d18-d21`: `e45f6b6`(P4)·`dc9a25e`(C11)·`8997818`(E8).

### Vòng giám sát 2026-07-11 (vòng 4) — 3 task passing (3 worker song song ở phiên riêng, pin base `9ad7427`)
| Task | Nội dung | Trạng thái |
|---|---|---|
| **P5** | `POST /internal/credits/consume` khi Scored → reservation Reserved→Consumed (`ExecuteUpdate WHERE status=Reserved`) + `reserved−1` + `credit_transactions(Consume,−1)`; owner từ reservation | ✅ **passing (374db18)** · Payment 17/17 (+4) · reuse schema P1 (không migration) · absorbing PAY-11 (Consumed/Released/miss-reserve → no-op) · ⚠ chưa wire consumer RabbitMQ (E7/BC3); postpaid → P8a |
| **C12** | `criteria[]` structured HR khai thẳng → replace-all atomic `campaign_criteria(source=HrEdited,order_no)`; Σweight chuẩn hoá→1/ngoài→400; sửa Active→409; publish bỏ AI khi có criteria[] | ✅ **passing (656402c)** · Campaign 35/35 (+13) · migration `AddCriteriaOrderAndUpdatedAt` (không apply Neon; has-pending=No changes) · ⚠ **apply-caveat:** backfill `order_no` rows cũ trước UNIQUE(campaign_id,order_no) nếu >1 tiêu chí/campaign |
| **BC11** | Seed rubric B2C mặc định (BA/BE/FE, `campaign_id=null`, Σweight=1, is_active) qua `HasData` bọc `if(IsNpgsql())` → B2C upload có publish job chấm | ✅ **passing (e2b6016)** · Interview 47/47 (+6) · migration `SeedB2CRubric` (không apply Neon; has-pending=No changes) · ⚠ seed chỉ Npgsql (giữ SQLite controlled không phá E1); CRUD rubric để sau |

> Base test sau vòng 4: **Auth 4 · Payment 17 · Campaign 35 · Interview 47 = 103 .NET pass** (+ AIService pytest 29), `dotnet build` 0 error. 3 worker chạy phiên riêng (worktree `../isas-P5|C12|BC11`), pin base `9ad7427` → anchor sạch. Merge `--no-ff`: P5·C12·BC11.

### Vòng giám sát 2026-07-11 (vòng 5) — 3 task passing (3 worker phiên riêng, pin base `d5a2aa5`, prompt kèm Context7 EF Core)
| Task | Nội dung | Trạng thái |
|---|---|---|
| **P6** | `POST /internal/credits/release` → reservation Reserved→Released (`ExecuteUpdate WHERE status=Reserved`) + `reserved−1`/`remaining+1` (hoàn chỗ giữ, không ghi ledger); owner từ reservation | ✅ **passing (8c8c885)** · Payment 21/21 (+4) · reuse schema P1 (không migration) · absorbing PAY-11 (Consumed→no-op không hoàn oan) · ⚠ postpaid→P8a; chưa wire consumer (E7/BC4) |
| **E5** | `GET /campaign/{id}/results` → sắp `total_score` DESC + rank read-time (competition) + pass/fail theo `pass_score_pct`; chỉ Scored; ngoài org→404 | ✅ **passing (1774d50)** · Campaign 40/40 (+5) · migration `AddCampaignPassScorePct` (1 cột nullable; không apply Neon; has-pending=No changes) · ⚠ rank competition (đổi được sang ROW_NUMBER); cột rank/result thừa E4 drop sau |
| **BC9** | Session B2C Scored → `overall_score`(equal-weight INT-10)+`answered_count`+`session_criterion_scores`; GET trả `overallScore`+`criteriaScores[]`+`needsImprovement[]`; B2B no-op | ✅ **passing (1cc2fb0)** · Interview 60/60 (+13) · migration `AddSessionResultBC9` (cột + bảng UNIQUE(session_id,criterion_id); không apply Neon; has-pending=No changes) · ⚠ compute trong `SessionScoringNotifier` best-effort (lỗi không chặn Scored); `overallComment` chờ BC10 |

> Base test sau vòng 5: **Auth 4 · Payment 21 · Campaign 40 · Interview 60 = 125 .NET pass** (+ AIService pytest 29), `dotnet build` 0 error. 3 worker phiên riêng (worktree `../isas-P6|E5|BC9`), pin base `d5a2aa5`, prompt nhúng Context7 EF Core (ExecuteUpdate/tx · ordering-rank · GroupBy-Average+SQLite ef_avg). Merge `--no-ff`: P6·E5·BC9.

### Vòng giám sát 2026-07-11 (vòng 6) — 3 task passing (3 worker phiên riêng, pin base `46578c5`, Context7 CsvHelper/QuestPDF/EF)
| Task | Nội dung | Trạng thái |
|---|---|---|
| **P8a** | Postpaid: `ReserveAsync` nhánh postpaid — guard `(period_usage ?? 0)+reserved+1 ≤ credit_limit`, vượt → 402 no-orphan; prepaid P4 giữ nguyên | ✅ **passing (f2a4a5a)** · Payment 27/27 (+6) · không migration (credit_limit/period_usage sẵn) · ⚠ reserve KHÔNG cộng period_usage (payment.md:377) → **BK7** consume postpaid; `credit_limit=NULL`→402 |
| **E6** | Export CSV `GET /campaign/{id}/results/export?format=csv` (CsvHelper, khớp E5); pdf/xlsx→400; ngoài org→404 | ✅ **passing (01bbf28)** · Campaign 46/46 (+6) · +CsvHelper 33.1.0 · không migration · ⚠ **PDF hoãn** (SkiaSharp risk) → **BK8**; cột CSV chưa chốt doc |
| **BC7** | `POST /api/practice/cv-analysis` → parse + AIService `/analyze-cv` → 201 + `cv_analyses` (jsonb); GET chỉ chủ; AI lỗi→502; AI không ghi DB | ✅ **passing (e612908)** · Interview 70/70 (+10) · migration `AddCvAnalyses` (không apply Neon; has-pending=No changes) · ⚠ **cần chốt doc:** `jobCategory` thêm vào request → **BK6**; billing BC-4 mâu thuẫn → **BK5** |

> Base test sau vòng 6: **Auth 4 · Payment 27 · Campaign 46 · Interview 70 = 147 .NET pass** (+ AIService pytest 29), `dotnet build` 0 error. Merge `--no-ff`: P8a·E6·BC7. **Backlog từ ghi chú:** BK1–BK4 (dọn dẹp) + **BK5** (DECISION billing CV — cần team chốt) · **BK6** (ratify jobCategory) · **BK7** (postpaid consume period_usage) · **BK8** (E6 PDF) — xem §Backlog trong tasks.md.

### Vòng giám sát 2026-07-11 (vòng 7) — 3 task passing (3 worker phiên riêng, pin base `70896db`, Context7 RabbitMQ v7/EF/HttpClient)
| Task | Nội dung | Trạng thái |
|---|---|---|
| **E7** | Payment consumer `InterviewEventConsumer` (queue `payment.credit` ↔ `interview.events`) → `CreditEventHandler`: `session.scored`→consume (P5), `session.abandoned`→release (P6); handler tách class unit-test | ✅ **passing (be7b168)** · Payment 33/33 (+6) · không migration · +RabbitMQ.Client 7.2.1 (async API) · absorbing sẵn P5/P6 · ⚠ tên queue/exchange chưa vào doc → **BK9**; e2e broker chờ compose |
| **C13** | Bulk CV `POST /campaign/{id}/candidates` → parse (ParserService) + archive PDF S3 KEY + hard-filter (required_skill/keywords/min_years) → Filtered/Rejected; dedup email; cap→400; chưa Active→409; GET cv | ✅ **passing (dc24c1a)** · Campaign 58/58 (+12) · migration `AddCampaignCandidates` (2 bảng + 3 cột rule, jsonb portable; không apply Neon; has-pending=No changes) · 0 credit (D18/D19) · ⚠ cột AI + `candidate_criterion_scores` để **C14** điền; full_name null (PATCH C14); trả 202 |
| **BC2** | Interview `CreateSessionAsync` B2C reserve credit ví User **trước** insert (typed HttpClient→Payment P4); ví hết→402 no session (PAY-5) | ✅ **passing (916aecd)** · Interview 73/73 (+3) · không migration · reserve idempotent theo sessionId · B2B không đổi (PAY-6) · ⚠ reserve-first→AI-gen lỗi giữ credit→**BC4** release; B2C hard-dep Payment; `Payment:BaseUrl` placeholder |

> Base test sau vòng 7: **Auth 4 · Payment 33 · Campaign 58 · Interview 73 = 168 .NET pass** (+ AIService pytest 29), `dotnet build` 0 error. Merge `--no-ff`: E7·C13·BC2. Backlog +**BK9** (ratify event-bus convention architecture.md §6 — ghi chú lặp E2/E4/E7).

### Vòng giám sát 2026-07-11 (vòng 8) — 3 task passing (3 worker phiên riêng, pin base `9e142d4`, Context7 RabbitMQ v7 publish/EF)
| Task | Nội dung | Trạng thái |
|---|---|---|
| **C14** | Async AI CV scoring: `Filtered`→publish `cv_screening_queue`→`Analyzing`; callback `cv-result`→`candidate_criterion_scores`+`overall_match_score`→`Analyzed`, `cv-failed`→`AnalysisFailed`; `?sort=score` shortlist; PATCH email/fullName (Invited→409); idempotent | ✅ **passing (ba2c1b8)** · Campaign 72/72 (+14) · không migration (schema C13; has-pending=No changes) · 0 credit (D19) · ⚠ tên queue/callback route→BK9; worker Python (AIService) + broker chờ compose |
| **BC8** | Báo cáo "CV↔câu trả lời" read-time: `SessionResultResponse.cvVsAnswer` gap = tiêu chí `needs_improvement` (BC9) ∩ CV mạnh (token-overlap `cv_analyses` BC7); không AI | ✅ **passing (7d91466)** · Interview 83/83 (+10) · không migration (read-time) · doc interview.md §BC8 · ⚠ "mạnh"=token-match không semantic; **worker tự sửa tasks.md → supervisor đã bỏ, tự đánh status** (nhắc brief: chỉ code+test) |
| **BK7** | Postpaid consume cộng `period_usage` (`ConsumeAsync` nhánh postpaid `COALESCE(period_usage,0)+1`; prepaid giữ nguyên) | ✅ **passing (9b702b3)** · Payment 36/36 (+3) · không migration · ⚠ phát hiện bug tiềm ẩn `ReleaseAsync` (P6) `remaining+1` mọi mode → sai postpaid → **BK11** |

> Base test sau vòng 8: **Auth 4 · Payment 36 · Campaign 72 · Interview 83 = 195 .NET pass** (+ AIService pytest 29), `dotnet build` 0 error. Merge `--no-ff`: C14·BK7·BC8 (BC8 bỏ phần worker tự sửa tasks.md, giữ code + interview.md §BC8). Backlog +**BK10** (BC3/BC4-vs-E7 decision) +**BK11** (release postpaid remaining).

### Vòng giám sát 2026-07-11 (vòng 9 — **bug-fix**) — 3 task passing (pin base `6319382`; prompt cấm sửa docs — rút kinh nghiệm BC8)
| Task | Nội dung | Trạng thái |
|---|---|---|
| **BK11** | Fix `ReleaseAsync` postpaid: tách nhánh theo `payment_mode` → postpaid chỉ `reserved−1` (KHÔNG `remaining+1`); prepaid giữ P6 | ✅ **passing (28fff09)** · Payment 38/38 (+2) · không migration · khớp payment.md:373 |
| **BK1** | Drop cột chết `rank`/`result` trên `campaign_rankings` (E4 tạo, E5 tính read-time) | ✅ **passing (9c9d450)** · Campaign 72/72 · migration `DropCampaignRankingRankResult` (reversible; không apply Neon; has-pending=No changes) |
| **BK12** | Fix orphan credit: B2C session `Failed` sau reserve (BC2) → phát `SessionAbandoned(generation_failed)` → E7 release ví User | ✅ **passing (a08b53d)** · Interview 86/86 (+3) · không migration · chỉ B2C; E7 absorbing → an toàn |

> Base test sau vòng 9: **Auth 4 · Payment 38 · Campaign 72 · Interview 86 = 200 .NET pass** (+ AIService pytest 29), `dotnet build` 0 error. Merge `--no-ff`: BK11·BK1·BK12. Cả 3 worker respect "không sửa docs" (khác BC8 vòng 8). **Backlog bug còn:** BK2 (backfill order_no ops) · BK8 (E6 PDF) · BK4 (org_id) · BK6 (ratify jobCategory) · BK9/BK5/BK10 (cần chốt).

### Vòng giám sát 2026-07-11 (vòng 10) — 3 task passing (3 worker phiên riêng, pin base `2839c7e`, Context7 EF Core/RabbitMQ v7)
| Task | Nội dung | Trạng thái |
|---|---|---|
| **P2** | Mua pack OneTime → `POST /webhook/payos` (verify HMAC ChecksumKey TRƯỚC, `[AllowAnonymous]` không gateway) → `ApplyPaidWebhookAsync` idempotent theo `payos_order_code`: order Pending→Paid atomic → ensure ví → `remaining += interview_credits` → ledger(Purchase,+N) + payment_transactions log; Order refactor owner model + wire P7 | ✅ **passing (c3ea069)** · Payment 42/42 (+4) · migration `AddOrderOwner` (cũng alter payment_transactions `order_id` nullable+N-1, khớp doc — **BK13** ratify tên; không apply Neon) · ⚠ **layer-3 PayOS sandbox thật chưa chạy → verify tay** (tạo payment-link + webhook thật) |
| **C15** | `POST /campaign/{id}/candidates/invite {candidateIds[]}` bulk (Active→409, lọc org→404; per-item `Analyzed`+email→invitation gắn `campaign_candidate_id`+email queue+`Invited`+audit; đã Invited→skip; ≠Analyzed/email null/dup→`failed[]`; vượt cap→400) + `StuckScreeningRepublisher` (2': Filtered publish-hụt / Analyzing>15' mất-tích → re-publish) + filter `?status=&minScore=&skill=` | ✅ **passing (f5b854a)** · Campaign 93/93 (+21) · **không migration** (`last_screening_published_at` từ C13; has-pending=No changes) · ⚠ dedup email đường-1 + skill client-eval ngoài checklist (gỡ dễ); e2e broker chờ compose |
| **BC12** | Roadmap 3 bảng (`roadmaps`/`roadmap_milestones`/`roadmap_lessons`) + `POST/GET /practice/roadmaps`: gom Scored B2C→baseline+weakness → AIService `/generate-roadmap` (typed HttpClient, trước Add → lỗi 502 no-row) → lưu milestones(Pending)+lessons(Theory); **0 buổi→201 baseline null**; **không trừ credit**; owner-only GET | ✅ **passing (225e0c3)** · Interview 94/94 (+8) · migration `AddRoadmaps` (jsonb ValueConverter, FK Cascade/Restrict; không apply Neon; has-pending=No changes) · reconcile interview.md:205 (bỏ quy tắc "0 buổi→403" cũ, khớp behavior) · ⚠ baseline gom MỌI buổi Scored (chưa giới hạn N); final_report/overall_comment null chờ BC15 |

> Base test sau vòng 10: **Auth 4 · Payment 42 · Campaign 93 · Interview 94 = 233 .NET pass** (+ AIService pytest 29), `dotnet build` 0 error. Merge `--no-ff`: P2·C15·BC12 (`c3ea069`·`f5b854a`·`225e0c3`). Backlog +**BK13** (ratify migration/schema payment_transactions P2). **⚠ P2 cần verify tay ngoài worker:** PayOS sandbox thật (payment-link + webhook HMAC) — worker chỉ chứng minh tới lớp-2 (unit test credit-add + idempotency).

### Vòng giám sát 2026-07-11 (vòng 11) — 3 task passing (3 worker phiên riêng, pin base `5639a25`, Context7 EF Core; AIService theo pattern repo)
| Task | Nội dung | Trạng thái |
|---|---|---|
| **BC14** | Lesson theory lazy (`GET …/lessons/{id}`→AIService `/generate-lesson-theory`, lưu 1 lần) + `POST …/lessons/{id}/start` (reserve ví User BC2, hết→402; câu hỏi bám `focusCriteria`; `Theory→Practicing`+mile InProgress; Practicing→409) + Scored→Done (`SessionScoringNotifier`) + Abandoned→Theory (`SessionAbandonSweeper`) | ✅ **passing (44a4aed)** · Interview 103/103 (+9) · **không migration** (schema roadmap_lessons từ BC12) · ⚠ `/start` link-after→race hiếm session mồ côi; `focusCriteria` chưa được `/generate-questions` consume (AIService cần thêm field); Abandoned→Theory forward-looking (sweeper quét InProgress, /start tạo Ready → chờ **I2** expire Ready session + release credit treo) |
| **BK4** | Rename `campaigns.employer_id`→`org_id` (+`campaign_questions`) + owner từ JWT claim `org_id` (thiếu→403 AUTH-8); ownership/filter/results/invitations/candidates theo ORG; audit actor giữ = user sub | ✅ **passing (ea480e7)** · Campaign 97/97 (93 sửa + 4) · migration `RenameCampaignEmployerToOrg` (RenameColumn/RenameIndex data-preserving; không apply Neon; has-pending=No changes) · ⚠ **backfill Neon (BK4b):** rows cũ `org_id`=user sub → cần data-migration user→org (`org_members`) trước khi credit-by-org chạy thật; bản copy AGENTS.md còn EmployerId (đồng bộ ở BK4b) |
| **BC10-AI** | AIService `POST /summarize-session` (Python sync best-effort, `{jobCategory,overallScore,criteriaScores[]}`→`{overallComment}` tiếng Việt; anti prompt-injection delimiter; AI không ghi DB) | 🟡 **nửa AI passing (7a60481)** · pytest 39/39 (29+10) · **task BC10 chưa đủ** — còn nửa Interview (gọi endpoint + lưu `overall_comment` khi Scored + migration cột + GET trả) |

> Base test sau vòng 11: **Auth 4 · Payment 42 · Campaign 97 · Interview 103 = 246 .NET pass** (+ AIService pytest 39), `dotnet build` 0 error. Merge `--no-ff`: BC14·BK4·BC10-AI. Backlog +**BK4b** (backfill user→org ops + đồng bộ AGENTS.md copy). **BC10 mới xong nửa AIService** → nửa Interview (wiring `SessionScoringNotifier`) là task kế.

### Vòng giám sát 2026-07-11 (vòng 12) — 1 task passing (**D2 SOLO cross-service**, pin base `6855543`) — **REDESIGN membership model**
| Task | Nội dung | Trạng thái |
|---|---|---|
| **D2** | **Membership model** (Discord/Classroom): Invitation → **Join** → membership (`CampaignCandidate`) → **My Campaigns** → **Start** → create-or-get session. Link mời CHỈ để join, **session tạo khi bấm Start** (không phải khi mở link) | ✅ **passing (5f486b3)** · Auth 7 (+3) · Interview 107 (+4) · Campaign 112 (+15) = **268 .NET pass** · **Auth** `/internal/auth/provision-candidate` (passwordless create-or-get by email + JWT Candidate, idempotent) · **Interview** `/internal/sessions/campaign` (`GetOrCreateCampaignSessionAsync` idempotent (candidateId,campaignId) chưa-terminal) · **Campaign** 5 endpoint (`ParticipationService`): invitation metadata-only · join (membership Joined, đường-2 update CV row + `candidate_id`, đường-1 create) · my-campaigns + detail · start (create-or-get + `session_id`+InProgress; Completed→409) · **E4** += `interview_status=Completed` · migration `AddCampaignMembership` (3 cột nullable; không apply Neon; has-pending=No changes) · ⚠ **loại (task khác):** reserve credit org (Payment), D3 resume, D4 lock-sau-submit · **ratify:** retake-sau-Completed (409) + race Scored↔E4-lag→session mới; JWT không refresh; orgName null (resolve Auth); route `/my-campaigns/{id}` |

> Base test sau vòng 12: **Auth 7 · Payment 42 · Campaign 112 · Interview 107 = 268 .NET pass** (+ AIService pytest 39), `dotnet build` 0 error. Merge `--no-ff`: D2. Reconcile **campaign.md §Distribution** + **tasks.md D2** sang flow membership mới (thay flow cũ "mở token→tạo session ngay"). Backlog +**BK14** (D2 follow-ups: reserve-on-start · retake policy · JWT refresh · orgName resolve). **⚠ D2 mở khoá HTTP entry B2B** — E2E B2B thật (compose + broker: join→start→làm bài→chấm→ranking→my-campaigns Completed) = lớp-3 verify tay ngoài worker.

### Vòng giám sát 2026-07-11 (vòng 13) — 3 task passing (2 worker sạch BC10·P3 + D3 kèm; pin base `c1165fb`)
| Task | Nội dung | Trạng thái |
|---|---|---|
| **P3** | `GET /order/{id}/status` active-polling: chưa có webhook → server gọi PayOS get-payment-info đối soát ngay; Pending+Paid → reuse `WebhookService.ApplyPaidWebhookAsync` (idempotent PAY-8); terminal → không gọi PayOS (PAY-10) | ✅ **passing (8b1168e)** · Payment 49/49 (+7) · `IPayOsQueryClient` tách abstraction (bọc SDK payOS 2.1.0) → unit-test SQLite không cần PayOS thật · **không migration** · ⚠ non-owner→404 (khác 403 ở GetOrder → **BK15**); layer-3 PayOS sandbox verify tay |
| **BC10** | Nửa Interview: session B2C Scored → gọi AIService `/summarize-session` best-effort → lưu `practice_sessions.overall_comment`; GET trả; AI lỗi→null không chặn Scored (nửa AIService 7a60481 từ vòng 11) | ✅ **passing (83000d8)** · Interview 111/111 (+4) · wiring `SessionScoringNotifier` chỉ B2C sau BC9; typed HttpClient mẫu `AiServiceCvAnalyzer`; AI không ghi DB (GEN-4) · migration `AddSessionOverallComment` (1 cột text nullable; không apply Neon; has-pending=No changes) · **mở khoá BC15** |
| **D3** | Resume phía Campaign — khoá regression hợp đồng: start 2× → cùng session, không hạ cấp InProgress; my-campaigns surface SessionId/InProgress | ✅ **passing (dc5db57)** · Campaign 115/115 (+3, test-only) · **không sửa production** (D2 create-or-get đã lo phần resume) · ⚠ mỏng có chủ đích: "câu đã nộp giữ nguyên" ở Interview → **BK16** verify Interview-side |

> Base test sau vòng 13: **Auth 7 · Payment 49 · Campaign 115 · Interview 111 = 282 .NET pass** (+ AIService pytest 39), `dotnet build` 0 error. Merge `--no-ff`: `30217a7`(P3)·`74a0280`(D3)·`5cb18de`(BC10). Backlog +**BK15** (thống nhất 403/404 order) +**BK16** (verify resume Interview-side). **⚠ Sự cố điều phối:** 3 worker chạy **chung 1 working dir** (KHÔNG worktree riêng) → D3 (dc5db57) bị commit chồng dưới BC10 trên nhánh feat/BC10; supervisor gỡ rối (`git branch -f feat/D3 dc5db57` + merge theo thứ tự P3→D3→BC10, 3 commit disjoint-service nên 0 conflict). **Vòng sau: brief PHẢI ép mỗi worker `git worktree add ../isas-<ID> <base>` thật sự** (không chia working dir).

### Vòng giám sát 2026-07-11 (vòng 14) — 3 task passing (3 worktree THẬT ../isas-BC15|D4|P8b off `e508174`, prompt kèm Context7 EF Core)
| Task | Nội dung | Trạng thái |
|---|---|---|
| **BC15** | Milestone improvement + roadmap `Completed` + `final_report`/`overall_comment` + `GET /roadmaps/{id}/report` (radar + levelEvaluation ngưỡng Fresher/Junior/Middle/Senior) | ✅ **passing (b975f8c)** · Interview 120/120 (+9) · hook lesson-Done ở `SessionScoringNotifier`; AIService `/summarize-roadmap` best-effort (GEN-4); Active→interim read-time, Completed→snapshot · **không migration** (cột jsonb từ BC12) · **hoàn tất S5 roadmap BC12→BC15** · ⚠ baseline null→improvement=điểm đạt |
| **D4** | Reissue invitation — revoke token cũ (→410) + phát token mới + resend + `audit_logs(ReissueInvitation)` | ✅ **passing (aa87ee7)** · Campaign 122/122 (+7) · `POST /campaign/{id}/invitations/{invId}/reissue` (Employer, lọc org→404, ≠Active→409); giữ `campaign_candidate_id` đường-2; 1 transaction; KHÔNG đụng membership/session · **không migration** (AuditAction string) · phần "lock-sau-submit" cũ obsolete (D2 membership) |
| **P8b** | Invoice postpaid: chốt kỳ → issue → pay(PayOS) → settle webhook branch theo `Order.Kind` | ✅ **passing (56a78cc)** · Payment 58/58 (+9) · `Invoice`+bảng `invoices`(owner_type/owner_id, numeric) + `orders.invoice_id` + `package_id` nullable · `CloseBillingPeriodAsync` 1-tx (snapshot→invoice→reset) · `POST /invoices/{id}/pay`→settlement order→PayOS · webhook Paid branch (InvoiceSettlement→Paid không cộng credit, CreditPack→P2 no-regression) · migration `AddInvoices` (không apply Neon; has-pending=No changes) · ⚠ **BK17** (Overdue-block reserve bỏ · close reset race · amount int cast · thiếu due_at/paid_at) · reconcile payment.md shape |

> Base test sau vòng 14: **Auth 7 · Payment 58 · Campaign 122 · Interview 120 = 307 .NET pass** (+ AIService pytest 39), `dotnet build` 0 error. Merge `--no-ff`: `30b1873`(P8b)·`8fedc5f`(D4)·`cf6de96`(BC15). Backlog +**BK17** (P8b follow-ups). **✅ Isolation fix hiệu quả:** 3 worker chạy **worktree thật riêng** (`../isas-<ID>` off `e508174`) → 0 race commit-lạc-nhánh (khác vòng 13). Reconcile payment.md §Invoice/§DB sang shape implement + interview.md/campaign.md markers ✅.

### Vòng giám sát 2026-07-11 (vòng 15) — 2 task passing (frontier mỏng: Campaign hết task feature; 2 worktree thật off `0274465`, prompt kèm Context7)
| Task | Nội dung | Trạng thái |
|---|---|---|
| **I2** | Per-question time-limit + bỏ tổng buổi (D21): `session.deadline` + sweeper auto-submit/abandon quá hạn + finalize câu trống→`Skipped` | ✅ **passing (443e831)** · Interview 125/125 (+5) · migration `AddSessionDeadline` (cột nullable; không apply Neon) · `CreateCampaignSessionInternalRequest` nhận `ExpiresAt` optional; `SessionAbandonSweeper` (E3) dùng Deadline thật; Deadline null (B2C)→không quét · Context7 EF Core (ExecuteUpdate/migration/BackgroundService) · ⚠ **BK18** (Campaign CHƯA gửi `expires_at`→B2B tạm Deadline=null); lesson B2C release-credit-treo vẫn hở |
| **A4** | HrMember bị chặn endpoint billing → 403 | ✅ **passing (3608959)** · Payment 66/66 (+8) · `IsHrMember()` (claim `org_role`, offline GEN-3) → `Forbid()` đầu `POST /order`·`/invoices/{id}/pay`·`/admin/invoices/close`; B2C (không claim)/OrgAdmin không chặn · check thủ công (không WebApplicationFactory, khớp idiom repo) · **không migration** · Context7 ASP.NET auth (RequireAssertion/HasClaim) · ⚠ ratify: GET billing HrMember vẫn xem; cancel không chặn; PlatformAdmin gate chờ A5 |

> Base test sau vòng 15: **Auth 7 · Payment 66 · Campaign 122 · Interview 125 = 320 .NET pass** (+ AIService pytest 39), `dotnet build` 0 error. Merge `--no-ff`: `e0e88d1`(I2)·`006b66b`(A4) (2 worktree thật, 0 race/0 conflict). Backlog +**BK18** (Campaign gửi expires_at wire I2). **Frontier thu hẹp:** S2 Campaign + S5 roadmap xong → task còn lại phần lớn collide cùng Interview (E9/E10/E11 chuỗi, BC3/BC4 chờ BK10) hoặc cross-cutting (A5) hoặc chờ DECISION (BK5/BK9/BK10). Vòng sau nên cân nhắc **A5 solo** hoặc **E9→E10→E11** (chất lượng chấm, Interview+AIService).

### Vòng giám sát 2026-07-11 (vòng 16 — BK-cleanup) — 3 task passing (3 worktree thật off `128d2a6`, 1/service, prompt kèm Context7)
| Task | Nội dung | Trạng thái |
|---|---|---|
| **BK17** | P8b follow-ups: Overdue-block reserve (402) + close subtract-snapshot (chống race consume) | ✅ **passing (ee72a0d)** · Payment 71/71 (+5) · `ReserveAsync` postpaid `AnyAsync(Overdue)`→rollback→402 no-orphan; `CloseBillingPeriodAsync` `PeriodUsage−count` thay `=0` · **không migration** · race guard verify revert→FAIL→restore→PASS · ⚠ còn (3) amount int overflow + (4) due_at/paid_at để riêng |
| **BK18** | Campaign gửi `campaign.expires_at` khi tạo session B2B → wire I2 deadline | ✅ **passing (00fb678)** · Campaign 127/127 (+5) · `CreateOrGetSessionAsync` +param `expiresAt`→payload `CreateCampaignSessionInternalRequest.ExpiresAt`; `StartInterviewAsync` truyền `campaign.ExpiresAt` → B2B có Deadline thật → sweeper I2 hoạt động · B2C không đổi (Deadline null) · **không migration** · ⚠ ratify casing payload internal |
| **BK16** | Verify resume Interview-side — "câu đã nộp giữ nguyên" | ✅ **passing (eedfeef)** · Interview 132/132 (+7, **test-only**) · `ResumeContractTests`: resume=cùng session+answer giữ (cùng AnswerId), upload-lại ghi đè idempotent (INT-3), Scored+điểm sống qua resume (INT-4), owner 403/404 (INT-11) · **KHÔNG gap production** (GetOrCreateCampaignSession/GetSessionAsync chỉ đọc `.Include(Scores)`) · đóng nốt câu chuyện D3 |

> Base test sau vòng 16: **Auth 7 · Payment 71 · Campaign 127 · Interview 132 = 337 .NET pass** (+ AIService pytest 39), `dotnet build` 0 error. Merge `--no-ff`: `6431f14`(BK17)·`ceca662`(BK18)·`6dc01fc`(BK16) (3 worktree thật, 0 race/0 conflict). **Cleanup đóng:** BK16·BK17(phần 1+2)·BK18 xong → I2 B2B deadline chạy thật, resume Interview-side khoá hợp đồng, postpaid Overdue/close race fix.

### Vòng giám sát 2026-07-11 (vòng 17) — 1 task passing (**E9 SOLO cross-service** Interview+AIService, off `c568062`, prompt kèm Context7 EF Core)
| Task | Nội dung | Trạng thái |
|---|---|---|
| **E9** | Chấm **neo theo mức** (levels/anchors): ScoringJob gửi `levels[{score,descriptor}]`+anchors, AI chọn mức trả `levelMatched` (score=level), lưu `answer_scores.level_matched`, mức lạ→snap; áp B2B & B2C | ✅ **passing (4b4d625)** · **Interview** 138/138 (+6): migration `AddAnswerScoreLevelMatched` (cột nullable; không apply Neon) · `ScoringCriteriaBuilder` (nạp rubric_levels/anchors Include, **không có→dải `0..maxScore`**) · callback guard E8 lưu `level_matched`+snap mức gần nhất (không drop→tránh Failed INT-9) · **AIService** pytest 45/45 (+6): `build_scoring_prompt` in levels+anchors, `gemini.score()` snap mức score=level (tie-break khớp C#), anti-injection giữ · **mở khoá E10** · ⚠ **E9b** (dùng dải mặc định thay `/suggest-criteria` sinh levels + seed rubric_levels B2C — chấm sát hơn); reject=snap (drop/needs_review→giao E10) |

> Base test sau vòng 17: **Auth 7 · Payment 71 · Campaign 127 · Interview 138 = 343 .NET pass** (+ AIService pytest **45**), `dotnet build` 0 error. Merge `--no-ff`: `a648a40`(E9). Backlog +**E9b** (levels giàu thay dải mặc định). **Bắt đầu chuỗi chất lượng chấm E9→E10→E11** (đều Interview+AIService, tuần tự): E9 xong → **E10** (self-consistency: chấm N lần, median, `needs_review` khi spread>ngưỡng — cần `attempt_no`+`practice_answers.needs_review`).

### Vòng giám sát 2026-07-11 (vòng 18) — 1 task passing (**E10 SOLO cross-service** Interview+AIService, off `da75601`, prompt kèm Context7 EF client-eval)
| Task | Nội dung | Trạng thái |
|---|---|---|
| **E10** | Self-consistency: chấm **N lần** (attempt_no+temperature) → điểm chốt = **median/tiêu chí**; spread=max−min > `Scoring:VarianceThreshold` → `practice_answers.needs_review=true` (cờ HR, không tự chốt) | ✅ **passing (938bef0)** · **Interview** 145/145 (+7, cũ xanh): migration `AddPracticeAnswerNeedsReview` · `ScoringOptions` (N **default 1 opt-in**/threshold/temperature) · publish N job/answer (attempt1 temp=0, 2..N temp>0); callback dùng `req.AttemptNo`, Scored chỉ khi đủ N distinct attempt; `ScoreStatistics` median; downstream **latest→median** (client-eval) · **AIService** pytest 48/48 (+3): `score(temperature)`, worker thread temp + echo attemptNo · **mở khoá E11** · ⚠ **N=1=hành vi cũ nguyên**; **E10b** (selective N× + republisher attempt hụt) |

> Base test sau vòng 18: **Auth 7 · Payment 71 · Campaign 127 · Interview 145 = 350 .NET pass** (+ AIService pytest **48**), `dotnet build` 0 error. Merge `--no-ff`: `44567f9`(E10). Backlog +**E10b** (selective N× + republisher N-attempt). **Chuỗi chất lượng chấm còn E11** (reasoning trích ≥1 dẫn chứng transcript + chặn rỗng + chống prompt-injection "chấm tối đa" + HR chốt; dùng `needs_review` E10) — Interview+AIService, task cuối chuỗi.

### Vòng giám sát 2026-07-11 (vòng 19) — 2 task passing (**E11** Interview+AIService + **A6** Auth, disjoint parallel; off `8e48c3f`, prompt kèm Context7)
| Task | Nội dung | Trạng thái |
|---|---|---|
| **E11** | Chuẩn nhận xét: reasoning trích ≥1 dẫn chứng transcript + chặn rỗng + siết anti prompt-injection ("chấm tối đa" không lái điểm) + flag `needs_review` (HR chốt) | ✅ **passing (f3ef192)** · **AIService** pytest 53/53 (+5): prompt PHỚT LỜ lệnh lái điểm trong transcript + reasoning bắt buộc dẫn chứng; `score()` reject reasoning rỗng · **Interview** 149/149 (+4): `SaveResultAsync` reasoning rỗng/ngắn (< `MinReasoningLen` default 0) → flag needs_review, KHÔNG mất điểm · **không migration** · **HOÀN TẤT E9→E10→E11** · ⚠ E11b (HR override điểm cuối) |
| **A6** | OrgAdmin mời/tạo HrMember (org nhiều thành viên) | ✅ **passing (ac40761)** · Auth 19/19 (+12): `POST/GET /auth/org/members` chỉ OrgAdmin (≠→403), tạo User(Employer) passwordless + `OrgMember(HrMember, org_id caller)`, dedup email→409; A2 loop verified (HR login mang org_role=HrMember→A4 chặn billing) · **không migration** (schema org_members từ A1) · ⚠ joinedAt=User.CreatedAt proxy; auth.md reconcile "sub-account HR phase 2"; A6b (đổi-role/xoá/invite-email) |

> Base test sau vòng 19: **Auth 19 · Payment 71 · Campaign 127 · Interview 149 = 366 .NET pass** (+ AIService pytest **53**), `dotnet build` 0 error. Merge `--no-ff`: `1f5726a`(E11)·`7642f9e`(A6). Backlog +**E11b** (HR override điểm) +**A6b** (quản thành viên đầy đủ). **✅ Chuỗi chất lượng chấm E9→E10→E11 XONG** (neo mức · self-consistency median · nhận xét có dẫn chứng + anti-injection). **✅ Org nhiều thành viên** (OrgAdmin thêm HrMember).

### Vòng giám sát 2026-07-12 (vòng 20) — 1 task passing (**BC7b** Interview-only, off `92f199a`)
| Task | Nội dung | Trạng thái |
|---|---|---|
| **BC7b** | Wire billing phân tích CV B2C (chốt BK5/D22 = tính phí): reserve trước→AI→consume; AI lỗi→release | ✅ **passing (87f3367)** · Interview 153/153 (+4) · `CreditReservationClient` +`ConsumeAsync`/`ReleaseAsync` (gọi Payment /consume /release P5/P6 đã có) · `CvAnalysisService`: reserve ví User (khoá=cvAnalysis.Id) TRƯỚC AI → hết→**402 no-row** (PAY-5); AI OK→consume; AI 502→release · config `Billing:CvAnalysisCredits` (default 1, 0=kill-switch free) · **không migration** · B2B batch CV giữ free (D19) · ⚠ reservation table reuse cho op không-session (sessionId=operationId — ratify tên); consume-fail-sau-commit → credit treo chưa có reconcile op-không-session |

> Base test sau vòng 20: **Auth 19 · Payment 71 · Campaign 127 · Interview 153 = 370 .NET pass** (+ AIService pytest **53**), `dotnet build` 0 error. Merge `--no-ff`: `7f9a466`(BC7b). **BK5/D22 khép trọn** (quyết định charge + code wire xong). Frontier còn: A5 solo · Phase 0 · e2e verify tay · backlog nhẹ.

### Vòng giám sát 2026-07-13 (vòng 21 — backlog cleanup) — 3 task passing (3 worktree thật off tip `6009f5e`, 1/service, disjoint)
| Task | Nội dung | Trạng thái |
|---|---|---|
| **BK15** | Non-owner order endpoint → **404** (leak-avoidance, khớp P3/P8b) | ✅ **passing (f248a6f, merge c98e2ea)** · Payment 77/77 (+6) · `OrderController` GET/DELETE `/order/{id}` other-owner `Forbid()`→`NotFound()`; rà soát `/status`(P3)+invoices(P8b) đã 404; `GetOwner()==null` giữ 403 (khác ca) · `OrderOwnerScopeTests` +6 · **không migration** · reconcile payment.md: hàng 403 bỏ "non-owner đọc order/invoice", hàng 404 += non-owner |
| **BK6** | `cv-analysis` bắt buộc `jobCategory` (thiếu→400 **trước** reserve, không giữ credit oan) | ✅ **passing (6dfd05b, merge eee79ff)** · Interview 154/154 (+1) · DTO `JobCategory?`+`[Required]` (phân biệt "thiếu" vs default enum `BA`=0) + guard đầu `AnalyzeAsync` → 400 trước CV-read & reserve (PAY-5) · **KHÔNG migration** — brief sai tiền đề: cột `cv_analyses.job_category` **đã** NOT NULL từ BC7 (`.IsRequired()`+migration nullable:false), `has-pending`=No changes → migration yêu-cầu là no-op, bỏ · reconcile interview.md §cv-analysis (request +jobCategory, +402 BK5) · ⚠ sibling `CreatePracticeSessionRequest.JobCategory` cùng pattern enum-default-BA (out-scope → follow-up nếu muốn) |
| **A6b** | Quản thành viên org (partial): `joined_at` thật + `PATCH` đổi role + `DELETE` xoá | ✅ **passing (0a56902, merge b384595)** · Auth 40/40 (+21: 11 service + 10 controller) · cột `org_members.joined_at` (migration `AddOrgMemberJoinedAt`, backfill `defaultValueSql now()`, **không apply Neon**) set khi tạo member; GET trả joined_at thật · `PATCH /auth/org/members/{userId}{orgRole}` OrgAdmin↔HrMember (role sai→400; hạ cấp OrgAdmin cuối→409; ngoài org→404; ≠OrgAdmin→403) · `DELETE …/{userId}` **hard-remove** membership (User giữ) (tự-xoá→400; OrgAdmin cuối→409; →204) · reconcile auth.md · ⚠ **còn phase-2:** invite-email + attach-account; hard-delete không audit (ratify vào auth.md) |

> Base test sau vòng 21: **Auth 40 · Payment 77 · Campaign 127 · Interview 154 = 398 .NET pass** (+ AIService pytest **53** không đổi), `dotnet build` 0 error. Merge `--no-ff`: `c98e2ea`(BK15)·`eee79ff`(BK6)·`b384595`(A6b) (3 worktree thật, 0 race/0 conflict — disjoint Payment/Interview/Auth). Backlog done 9→**12** (+BK6/BK15) · A6b partial. Frontier còn: **A5 solo** (auth gate cross-cutting) · Phase 0 (P0.1/P0.2/P0.5) · e2e verify tay (PayOS sandbox · compose+broker+Gemini) · backlog nhẹ (BK8/BK13/BK17(3)(4) cleanup + follow-up).

### Vòng giám sát 2026-07-13 (vòng 22 — **A5 SOLO cross-cutting**, off tip `36fd3f2`) — auth gate toàn repo
| Task | Nội dung | Trạng thái |
|---|---|---|
| **A5** | Bật `[Authorize(Roles)]` mọi service — ẩn danh→401, sai role→403; không endpoint "cần auth" nào còn mở | ✅ **passing (e6fb2c1, merge 805475d)** · **chiến lược per-endpoint attribute** (KHÔNG global FallbackPolicy → không chặn nhầm /health/public/internal/webhook/magic-link) · 🔴 **lỗ thật bịt:** Payment `PackageController` CRUD gói (comment hết auth → mở toang) → `Roles="Admin"`; `admin/invoices/close` → `Roles="Admin"` (trước chỉ `[Authorize]`) · siết role: Payment invoice me/pay→`Employer`; Auth `OrgMembers`→`Employer` (giữ `IsOrgAdmin()`); Interview Practice/Roadmaps/CvAnalysis/Answers/Files→`Candidate` (2 callback internal giữ `[AllowAnonymous]`) · GET package→`[AllowAnonymous]` (payment.md:104 Public) · Auth 9 auth-entry→`[AllowAnonymous]` tường minh · `RoleClaimType=ClaimTypes.Role` explicit ở Payment/Campaign/Interview (khớp Auth) · Campaign đã sạch sẵn không đổi · **KHÔNG migration** · test ✅ **Auth 51·Payment 84·Campaign 133·Interview 162 = 430** (+32: 4 `AuthorizationCoverageTests` reflection guard "0 endpoint trần") · ⚠ **layer-3 verify tay** (live 401/403/internal-token/health — cần stack); GET package public = mở rộng access theo doc; api/files=Candidate-only |

> Base test sau vòng 22: **Auth 51 · Payment 84 · Campaign 133 · Interview 162 = 430 .NET pass** (+ AIService pytest **53** không đổi), `dotnet build` 0 error. Merge `--no-ff`: `805475d`(A5). **Auth gate cross-cutting XONG** — mọi service enforce role; guard test khoá "0 endpoint trần". Frontier còn: **Phase 0** (P0.1/P0.2/P0.5) · **e2e verify tay** (PayOS sandbox · compose+broker+Gemini + A5 live 401/403) · backlog nhẹ (BK8 PDF · BK13 · BK17(3)(4) · follow-up A6b-email/E9b/E10b/E11b/BK14).

### Vòng giám sát 2026-07-13 (vòng 23 — feature theo yêu cầu) — 1 task passing (**BC16** Interview-only, off tip `1b25da2`)
| Task | Nội dung | Trạng thái |
|---|---|---|
| **BC16** | Rubric CÁ NHÂN B2C — candidate tự CRUD rubric theo JobCategory (KHÔNG admin; đảo hướng BK3) | ✅ **passing (c59b02d, merge 2a9521c)** · `RubricCriterion += candidate_id` (null=seed mặc định · set=rubric riêng); migration `AddRubricCandidateId` (**không apply Neon**; has-pending=No changes) · **`B2CRubricScope.ResolveOwnerAsync` dùng chung 4 site scoring** (publish · callback guard · republisher · breakdown BC9) — ưu tiên rubric riêng active else seed, publish↔callback không lệch · `RubricController` `api/practice/rubrics` (`Roles="Candidate"`, owner-scope) GET/PUT/DELETE + `RubricLibraryService` **replace-all soft-versioned** (deactivate cũ + add mới, KHÔNG hard-delete — `answer_scores` FK Restrict) + validate Σweight normalize/bounds/name-unique · **điểm tổng giữ TB cộng INT-10** (weight display-only) · reconcile rules.md BC-7 + interview.md + tasks.md (BK3 superseded) · test ✅ **Interview 183/183 (+21)**: CRUD · resolver · isolation · FK-safe · publish regression (seed-vs-custom) · breakdown BC9 · ⚠ edit-giữa-Scoring edge hiếm; weighted-total = follow-up |

> Base test sau vòng 23: **Auth 51 · Payment 84 · Campaign 133 · Interview 183 = 451 .NET pass** (+ AIService pytest **53** không đổi), `dotnet build` 0 error. Merge `--no-ff`: `2a9521c`(BC16). Frontier còn: **Phase 0** (P0.1/P0.2/P0.5) · **e2e verify tay** (PayOS sandbox · compose+broker+Gemini + A5 live 401/403) · backlog nhẹ (BK8 PDF · BK13 · BK17(3)(4) · follow-up).

### ✅ Verify tay LAYER-3 (2026-07-13) — API sweep server thật qua tunnel (gateway + 5 service + Mac AI)
> Server Ubuntu deploy compose (postgres local, squash `InitialCreate` applied, seed rubric sống) + Mac AIService (image rebuild 07-13). **47 check** — log `~/Downloads/isas-api-test-*.log` + `isas-api-retest-*.log`.

| Nhóm | Kết quả LIVE |
|---|---|
| Auth + Org | ✅ register/login/me · register-org · org members CRUD **A6/A6b** (joinedAt thật · PATCH role · DELETE · guard) · refresh |
| **A5 gates** | ✅ **8/8**: ẩn danh→401 · sai role→403 (Candidate↔Employer · org-members · invoices Employer-only · package Admin-only) |
| **BC16 rubric** | ✅ trọn vòng đời: GET seed (`isCustom=false`, GUID seed đúng) → PUT custom → GET custom → Σweight sai→400 → DELETE → về seed |
| Billing B2C | ✅ tạo session ví 0 credit → **402** (BC2/PAY-5) |
| Files S3 | ✅ upload PDF → SeaweedFS trả fileId |
| **AI e2e** | ✅ `/ai/health` + **tạo roadmap thật** server→Mac→Gemini→DB (BC12/BC13, milestones tiếng Việt) |
| Campaign B2B | ✅ full chain: create→publish (C12 bỏ AI)→results→**CAMP-2 409**→isolation 403→soft-delete |
| Payment | ✅ catalog public · order/my-orders · me/invoices Employer-only |

**🐛 3 bug THẬT tìm ra:** (1) **cv-analysis 500 mọi request** — `[property:Required]` positional record → ASP.NET throw (hotfix `83beabc`; **cần image interview mới** → retest sau deploy); (2) **campaign invitations 500** — compose server **thiếu env RabbitMQ** cho campaignservice → fix compose + DEPLOYMENT.md (`0997030`, **chỉ cần `docker compose up -d` lại**, không rebuild); (3) **BF3 — `POST /order` 500** (luồng tiền, sweep 2026-07-13 qua tunnel) — compose server **thiếu env `PayOS__ReturnUrl/CancelUrl`** → PayOS reject "return_url null" stack thô → fix code (guard config sớm + catch ApiException→502 `PaymentGatewayException`, không order mồ côi) + `appsettings.json`/`.env.example`/`DEPLOYMENT.md` +2 key (Payment 84→89, **set env server + up -d**, không rebuild). **Backlog mới:** ~~BK19~~ ✅ **done** (ratify giữ code 404; payment.md liệt kê cả 404 unknown + 400 inactive) · ~~BK20~~ ✅ **done** (Campaign bật `JsonStringEnumConverter` + test +4, backward-compat numeric; Campaign 133→137 · ⏳ retest live sau redeploy). **⚠ Deploy-gap luồng tiền:** DB server **không seed** `product_packages` (catalog rỗng) + **không seed Admin** → phải seed tay/tạo admin trước khi mua credit. **Còn verify:** cv-analysis + invitations + **BF3 order→checkoutUrl** retest sau deploy · **BK20 questions[].source="CustomHr" live** · PayOS sandbox webhook Paid+HMAC→credit thật · B2B join→start→chấm→ranking trọn luồng · B2C mua-credit→luyện→chấm trọn luồng.

> Test project trong tree hiện có (đủ 4): `Isas.AuthService.Tests` · `Isas.PaymentService.Tests` (✅ P0.4) · `Isas.CampaignService.Tests` · `Isas.InterviewService.Tests`.

## Vấn đề đã biết / cần xác minh
- **E1 → e2e thật bị chặn bởi D2:** chưa có HTTP entry tạo session B2B (magic-link) → luồng B2B end-to-end (campaign → ứng viên làm bài → chấm → ranking) chưa chạy trọn được. Chứng minh E1 ở mức engine bằng unit/integration test.
- **Campaign chưa wire `org_id`:** code dùng `employer_id` (claim). A1 đã có `Organization`/`org_members` trên main → cần task nối campaign sang `org_id` (credit theo org cần cái này).
- **PaymentService** ✅ đã refactor sang **org-credit + reserve/consume + postpaid** (P1–P8). **✅ Đã wire deploy (2026-07-13):** `Dockerfile` (trước thiếu) + CI build+push image `paymentservice` (ci.yml, nay build **5** image) + gateway route `/api/v1/payment/{**}` (payment-cluster) + service trong compose server (DB `isas_payment`). AIService vẫn deploy tay trên Mac.
- **✅ Deploy-prep migration (2026-07-13):** đã **squash 33 migration → 1 `InitialCreate`/service** (Auth/Payment/Campaign/Interview) — apply **tay** lên postgres **local** (drop & tạo lại 4 DB rỗng: `isas`/`isas_interview`/`isas_campaign`/`isas_payment` → `dotnet ef database update` hoặc SQL script; seed rubric B2C bake sẵn). Giữ rule no-auto-migrate. Test giữ 451 (EnsureCreated không đụng migration).
- **AIService** `/suggest-criteria` đã **live HTTP** nhưng deploy qua `docker cp`+restart → **ephemeral**; muốn permanent phải **rebuild image** từ Dockerfile. Các vấn đề nền còn nguyên: 🔴 `/ai/**` public không auth · 🔴 Whisper `large-v3` CPU chậm · 🔴 prompt injection bộ chấm · 🔴 `nack` không DLQ ([services/ai.md](services/ai.md) §Vấn đề).
- ✅ **Trần `orderCode` PayOS đã verify** (D12, 2026-06-28): ≤ **9.007.199.254.740.991** (2^53−1) → snowflake 64-bit vượt trần (loại). Thêm ràng buộc PayOS: `description` ≤25 ký tự, webhook **HMAC-SHA256** — bảng đầy đủ [services/payment.md](services/payment.md) §PayOS.
- **CI/CD chung Neon (DB server):** không tự apply migration lên DB chung — schema apply qua pipeline/tay trước deploy.

## Bước tiếp theo (thứ tự đề xuất)
> ✅ V1: **E2,D1,P1,BC6**. ✅ V2: **P7,E3,E4,BC13**. ✅ V3: **P4,C11,E8**. ✅ V4: **P5,C12,BC11**. ✅ V5: **P6,E5,BC9**. ✅ V6: **P8a,E6,BC7**. ✅ V7: **E7,C13,BC2**. ✅ V8: **C14,BC8,BK7**. ✅ V9: **BK11,BK1,BK12**. ✅ V10: **P2,C15,BC12**. ✅ V11: **BC14,BK4,BC10-AI**. ✅ V12: **D2**. ✅ V13: **BC10(full),P3,D3**. ✅ V14: **BC15,D4,P8b**. ✅ V15: **I2,A4**. ✅ V16: **BK17,BK18,BK16** (BK-cleanup). ✅ V17: **E9**. ✅ V18: **E10**. ✅ V19: **E11,A6** (E9→E10→E11 xong + org members). ✅ V20: **BC7b** (wire billing CV, chốt BK5). ✅ V21: **BK15,BK6,A6b** (backlog cleanup — non-owner→404 · cv-analysis jobCategory→400 · org member joined_at/đổi-role/xoá). ✅ V22: **A5** (SOLO cross-cutting — auth gate `[Authorize(Roles)]` mọi service, 0 endpoint trần). ✅ V23: **BC16** (rubric cá nhân B2C — candidate tự CRUD theo JobCategory, không admin; đảo hướng BK3). Frontier mới bên dưới.
> **⚠ P2 cần verify tay:** engine credit reserve/consume/release/purchase đủ (P4/P5/P6/P8a/BK7/BK11/E7 + **P2 webhook**), nhưng **luồng tiền thật cần PayOS sandbox** (payment-link + webhook HMAC) — verify ngoài worker trước khi coi B2C/B2B mua-credit là E2E-Xong.
> **✅ Đã chốt (2026-07-12):** **BK5**→CV analysis **tính phí** (D22; còn wire code BC7b) · **BK9**→event-bus convention vào architecture.md §6.1 (doc-only, xong) · **BK10**→**bỏ** (BC3/BC4 = covered-by-E7). *(Không còn DECISION treo trong backlog.)*
1. **D2 ✅ · D3 ✅ · D4 ✅ · BK16 ✅ (eedfeef)** — membership model + resume regression + reissue invitation + resume Interview-side khoá hợp đồng. **Gỡ chặn HTTP entry B2B.** → còn **BK14** (reserve-on-start + retake ratify). **E2E B2B thật** (compose + broker: join→start→làm bài→chấm→ranking→my-campaigns Completed) = verify tay ngoài worker.
2. **P2 ✅ · P3 ✅ · P8b ✅ · A4 ✅ · BK17 ✅ (ee72a0d)** — engine tiền đủ: mua pack (P2) · active-polling (P3) · invoice postpaid (P8b) · HrMember→403 billing (A4) · postpaid Overdue-block + close race fix (BK17). **Còn verify tay:** PayOS sandbox thật (payment-link + webhook HMAC) ngoài worker → xong mới coi luồng tiền E2E-Xong; mở khoá **BC1** (mua pack ví User). BK17 còn (3) amount int overflow + (4) due_at/paid_at — để riêng khi cần.
3. **S5 B2C roadmap HOÀN TẤT** (BC11 ✅ · BC9 ✅ · BC7 ✅ · BC2 ✅ · BC8 ✅ · BC12 ✅ · BC14 ✅ · BC10 ✅ · **BC15 ✅ b975f8c**): pipeline luyện cá nhân hoá (roadmap → milestone → lesson → improvement → report) đủ engine. → **BC3/BC4** = **covered-by-E7** (chốt BK10 — E7 consume/release generic mọi session; còn test e2e broker) · **BC7b ✅ (87f3367)** wire cv-analysis billing (reserve→consume ví User; hết→402) · BC1 (chờ P2 e2e) · **e2e B2C thật** (compose + Gemini) verify tay.
4. **Lọc CV B2B** (C13 ✅ · C14 ✅ · **C15 ✅**): pipeline sàng CV → shortlist → bulk invite đã đủ; còn e2e broker thật (compose) + chốt D2 để ứng viên vào bài.
5. **I2 ✅ (443e831) · BK18 ✅ (00fb678)** — per-question time-limit + `session.deadline` + sweeper auto-submit/abandon; Campaign đã gửi `expires_at` → B2B Deadline chạy thật. → còn lesson B2C release-credit-treo (Deadline=null, thuộc BC14/E7).
6. **Roadmap ôn tập B2C (D20) HOÀN TẤT:** `BC12` ✅ · `BC13` ✅ · `BC14` ✅ · `BC10` ✅ · **`BC15` ✅ (b975f8c)** — engine đủ; còn e2e thật (compose + Gemini) verify tay.
7. **Auth: A4 ✅ · A5 ✅ (v22·e6fb2c1) · A6 ✅ (ac40761) · A6b partial ✅ (v21·0a56902)** — **toàn bộ S1 Auth xong**: HrMember→403 billing (A4); **A5 auth gate `[Authorize(Roles)]` mọi service** (per-endpoint, bịt lỗ Package/admin-close, siết Employer/Candidate/Admin, guard reflection "0 endpoint trần"); OrgAdmin thêm HrMember (A6); A6b joined_at + đổi role + xoá. Còn A6b: invite-email · attach-account (phase-2); **A5 layer-3 live 401/403 = verify tay** khi có stack.
8. **Phase 0 còn lại**: `P0.1` (compose máy sạch), `P0.2` (`make setup/test/check`), `P0.5` (readiness + checkpoint). *(P0.3/P0.4 ✅.)*
9. **Backlog** (đã gom nhóm — xem §Backlog [tasks.md](tasks.md)): 📋 **ops** (trước apply Neon) BK2/BK4b · 🛠️ **cleanup** BK6/BK8/BK13/BK15/BK17(3)(4) · 🔵 **feature follow-up** BC7b/BK3/BK14/E9b/E10b/E11b/A6b. *(✅ done 9 + 🔒 chốt BK5/BK10; 🔴 DECISION group đã trống.)*

> Quy trình **vào ca / tan ca**: xem [../AGENTS.md](../AGENTS.md).

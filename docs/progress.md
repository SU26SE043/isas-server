# ISAS — Progress / Handoff

> Trạng thái hiện tại + bước kế tiếp, để phiên/người mới nối tiếp nhanh. Kế hoạch đầy đủ & phân việc: [work-division.md](work-division.md). Lý do quyết định: [decisions.md](decisions.md).
> **Cập nhật mỗi khi đổi trạng thái** (tan ca). Cập nhật lần cuối: **2026-07-12** (vòng giám sát: **D1·BC6·E2·P1** (vòng 1) + **P7·E3·E4·BC13** (vòng 2) + **P4·C11·E8** (vòng 3) + **P5·C12·BC11** (vòng 4) + **P6·E5·BC9** (vòng 5) + **P8a·E6·BC7** (vòng 6) + **E7·C13·BC2** (vòng 7) + **C14·BC8·BK7** (vòng 8) + **BK11·BK1·BK12** (vòng 9 — bug-fix) + **P2·C15·BC12** (vòng 10) + **BC14·BK4·BC10-AI** (vòng 11) + **D2** (vòng 12 — solo cross-service, membership redesign) + **BC10·P3·D3** (vòng 13 — 2 worker sạch + D3 kèm) + **BC15·D4·P8b** (vòng 14 — 3 worktree thật, 0 race) + **I2·A4** (vòng 15 — 2 worker, frontier mỏng) + **BK17·BK18·BK16** (vòng 16 — BK-cleanup, 3 worktree thật) + **E9** (vòng 17 — SOLO cross-service Interview+AIService, chấm neo mức) + **E10** (vòng 18 — SOLO cross-service, self-consistency median+needs_review) + **E11·A6** (vòng 19 — E11 đóng chuỗi chất lượng chấm + A6 org nhiều thành viên) + **BC7b** (vòng 20 — wire billing CV analysis, chốt BK5) + **BK15·BK6·A6b** (vòng 21 — backlog cleanup, 3 worktree disjoint) + **A5** (vòng 22 — SOLO cross-cutting, auth gate `[Authorize(Roles)]` mọi service) + **BC16** (vòng 23 — rubric cá nhân B2C candidate-owned) passing, integrated vào `docs/sync-design-d18-d21`). Cập nhật lần cuối vòng 23: **2026-07-13**.
> **Cập nhật 2026-07-16 (Đợt 1–3b hardening + BK14 org-credit-reserve + live e2e B2C/B2B verified trên server deploy):** xem §Đợt 1–3b + BK14 bên dưới. **BK14 done** (commit 3399765 — B2B reserve org credit tại Start). **Luồng tiền E2E cả 2 dòng chạy thật** (B2C mua-credit→luyện→chấm→consume · B2B org-credit→campaign→invite→join→start→chấm→ranking Pass).
> **Cập nhật 2026-07-17 (E2E toàn hệ thống trên deploy Vercel FE + tunnel BE + 2 fix qua PR→CI/CD):** e2e cả **B2C+B2B, UI thật (Chrome) + API** trên site thật → PASS; **luồng tiền PayOS→webhook→credit verify LIVE hôm nay** (order Paid + ledger `+5 Purchase`). Fix: campaign list "0 tiêu chí" (`.Include(Criteria)`, **PR #42**) + FE loading splash (PayOS-return cold-boot, deploy Vercel). CI deploy pull-tolerant (**PR #41**). Xem **§E2E 2026-07-17** bên dưới.
> **Cập nhật 2026-07-17 (Vòng S6 — DB Hardening đợt 1, 3 worker disjoint):** **DB1·DB3 (Payment) · DB11·DB12 (Auth) · DB13 (Campaign)** passing — merged main (PR #47/#48/#49). Supervisor verify độc lập xanh (Payment 118 · Auth 64 · Campaign 176). Xem **§Vòng S6 đợt 1** bên dưới. Còn P0: **DB4** (reconcile, đã mở khoá do DB1) · **DB2** (Transactional Outbox, cross-service solo).
> **Cập nhật 2026-07-17 (Vòng S6 — DB Hardening đợt 2, 2 worker disjoint):** **DB4 (Payment reconciler) · DB19 (Interview CHECK rubric single-owner)** passing (PR `s6/db4-payment`/`s6/db19-interview`). Supervisor verify độc lập xanh (Payment 118→122 · Interview 229→233). Xem **§Vòng S6 đợt 2** bên dưới. Còn P0: **DB2** (Transactional Outbox, cross-service solo — vòng riêng kế).
> **Cập nhật 2026-07-17 (Vòng S6 — DB Hardening đợt 3, SOLO):** **DB2 Transactional Outbox (Interview)** passing (PR `s6/db2-interview-outbox`) — outbox_messages + dispatcher at-least-once thay publish best-effort; gỡ SettlementReconciler (cover cả B2B+generation_failed). Verify độc lập xanh (Interview 233 · Payment 122 · Campaign 176). Xem **§Vòng S6 đợt 3**. **P0 money-integrity XONG** (DB1/DB2/DB3/DB4). Còn: DB2b (Campaign email outbox) · DB18 (Saga, nay mở khoá) · DB9 · P1/P2.
> **Cập nhật 2026-07-17 (Vòng S6 — DB Hardening đợt 4, SOLO):** **DB2b Campaign invitation-email outbox + consumer dedup** done (PR `s6/db2b-campaign-outbox`) — mirror pattern DB2 sang Campaign (outbox_messages + dispatcher thay dual-write best-effort) + bịt bug email trùng (`email_sent_at` dedup). Verify độc lập xanh (Campaign 176→189 · Interview 233 · Payment 122). **Câu chuyện outbox khép** (Interview events + Campaign email). Xem **§Vòng S6 đợt 4**.
> **Cập nhật 2026-07-17 (Vòng S6 — DB Hardening đợt 5, SOLO):** **DB18 Start orphan-reservation compensation** passing (PR `s6/db18-orphan-reservation`) — `OrphanReservationReconciler` (Payment) quét reservation `Reserved` mồ côi → hỏi Interview `/sessions/exists` → release (phương án NHẸ, không full-saga). **Khép luôn DB4b.** Verify độc lập xanh (Payment 122→129 · Interview 233→239). Xem **§Vòng S6 đợt 5**. ⚠ deploy: Payment cần env `Interview__BaseUrl`.
> **Cập nhật 2026-07-17 (Vòng S6 — DB Hardening đợt 6, 2 worker disjoint):** **DB9 FK nội-service** passing — Payment (composite FK→credit_accounts + alternate key + fix ReserveAsync) ‖ Campaign (FK rankings/flags→campaigns kèm query filter DB13 + invitation→candidate SetNull). Verify độc lập xanh (Payment 129→134 · Campaign 189→190). Xem **§Vòng S6 đợt 6**. **SQLite CÓ enforce FK** (EF10, đính chính). ⚠ apply: dọn orphan/backfill trước add FK.
> **Cập nhật 2026-07-17 (Vòng S6 — DB Hardening đợt 7, 3 worker disjoint):** **DB5 index background sweeper** passing — 3 nhánh `s6/db5-interview ‖ s6/db5-payment ‖ s6/db5-campaign` (mỗi service 1 migration `AddDb5SweeperIndexes`, reversible, không apply Neon). Partial index hình-vị-ngữ khớp sweeper thật (Interview `SessionAbandonSweeper`/`StuckAnswerRepublisher` · Payment `CreditReservationReconciler`/`OrphanReservationReconciler` · Campaign `StuckScreeningRepublisher`); **bỏ `credit_transactions(owner_id)`** (không sweeper nào quét → dead index). Supervisor verify độc lập xanh (octopus-merge 3 nhánh: build 0 error · **634 test** · `has-pending`=No changes cả 3 · Interview 239→242 · Payment 134→137 · Campaign 190→191). Xem **§Vòng S6 đợt 7**. ⚠ **L3 EXPLAIN = verify tay** tại apply-window (index add online-safe). **P1 còn:** DB6/7/8 · DB10 (xmin) · DB14 · DB15 · P2 (DB16/17).
> **Cập nhật 2026-07-17 (Vòng S6 — DB Hardening đợt 8, 2 worker disjoint):** **DB14 (audit cols, Payment+Interview) ‖ DB16 (split God-table `campaign_candidates`, Campaign)** passing — giao **1 branch** `s6/db14-db16` (DB5 đã merge main #65-68 giữa chừng → base sạch, hết cần stack). DB14: `IHasUpdatedAt`+override SaveChanges + `.SetProperty(updated_at)` cho ExecuteUpdate path; `orders.status→varchar(20)`; `product_packages.type` int→string (GEN-2). DB16: RENAME campaign_candidates→cv_submission + bảng mới campaign_membership (enum bỏ `Joined` = membership row tồn tại). Supervisor verify độc lập xanh (octopus-merge: build 0 error · **640 test** · `has-pending`=No changes cả 3 · Payment 137→141 · Interview 242→244 · Campaign 191). Xem **§Vòng S6 đợt 8**. ⚠ **DB16 backfill/rename KHÔNG test được (SQLite EnsureCreated bỏ qua migration) → L3 Postgres verify BẮT BUỘC** trước merge. **P1 còn:** DB6/7/8 · DB10 (xmin) · DB15 · P2 (DB17).
> **Cập nhật 2026-07-17 (Apply S6 đợt 7+8 lên server thật + fix migration bug):** đã apply lên **`postgres-main`** (server): **DB5** (Payment/Interview/Campaign) · **DB14** (Payment/Interview) · **DB16** (Campaign split). Verify live: Payment `product_packages.type`→varchar (`OneTime`) + `updated_at` orders/reservations · Interview `practice_sessions.updated_at` · **Campaign cv_submission=13 / campaign_membership=13 / status normalize Invited / 3 FK inbound re-point / 0 orphan / DB5 index carried**. Auth (`isas`) đã current. 🐛 **Fix migration bug (branch `fix/db14-migration-sql-semicolon`):** `AddAuditColumnsAndTypes` (DB14 Payment) raw `Sql()` `ALTER…USING(CASE…END)` **thiếu `;` cuối** → vỡ **idempotent script** (`END) END IF` syntax error) dù `dotnet ef database update` chạy được (nên L3 + test không bắt) → thêm `;`. **Bài học:** mọi `migrationBuilder.Sql()` PHẢI kết thúc `;` để idempotent-script deploy an toàn.
> **Cập nhật 2026-07-17 (Vòng S6 — DB Hardening đợt 9, 4 worker disjoint service-group):** **DB10 (xmin concurrency, 4 service) · DB15 (drop `subscriptions` · merge `rubric_anchors`→jsonb · index cleanup · CHECK weight (0,1]/pass_score_pct [0,100]) · AI2 (RabbitMQ DLX/DLQ)** passing — 1 branch `s6/db10-db15-ai2` → **PR #71 merged `main`** (`0067660`). **PM verify độc lập PASSED:** build 0 err · **664 .NET test** (Auth 68·Payment 143·Campaign 202·Interview 251) + pytest 66 · has-pending=No changes ×4 · **L3 Postgres tay** (4 idempotent script apply clean + rubric_anchors backfill Up/Down 0-loss). ⚠ Npgsql 10 bỏ `UseXminAsConcurrencyToken()` → shadow-property map tay (Context7). 2 worker chạm session-limit → resume xong. Xem **§Vòng S6 đợt 9** bên dưới.
> **Cập nhật 2026-07-17 (Apply S6 đợt 9 lên server thật — DB + RabbitMQ DLX/DLQ + Mac aiworker):** PR #71 merged → CI deploy **5 image `:main` mới** (22:07). **DB migration TĂNG DẦN** (idempotent EF script + `docker exec -i postgres-main psql`, no-drop vì DB đã có data) lên **4 DB**: Interview `SchemaCleanupDb15` (rubric_anchors→`rubric_levels.example_answers` jsonb backfill + drop bảng + weight CHECK + swap index `(session_id,question_id)`→non-unique `session_id`) · Payment `AddXminConcurrencyToken`+`DropSubscriptionsTable` · Campaign `AddCampaignCheckConstraintsDb15` · Auth `AddXminConcurrencyToken`. **Preflight read-only SẠCH trước apply** (subscriptions=0 · rubric/campaign weight ∈(0,1] · pass_score_pct ∈[0,100] · rubric_anchors=0 → 0 CHECK violation). Verify sau apply: schema đổi đúng + `__EFMigrationsHistory` ghi đủ (⚠ history: Auth cột `"MigrationId"`, 3 service khác cột `migration_id`, bảng đều `"__EFMigrationsHistory"`). **RabbitMQ AI2 (broker server):** `rabbitmqadmin` tạo DLX `scoring_pipeline_dlx` (direct) + DLQ `scoring_pipeline_dead_queue` + binding key `scoring_dead` + **recreate** `scoring_pipeline_queue` (queue cũ `arguments=[]` không sửa arg tại chỗ được → drain 0-msg → delete → declare lại với `x-dead-letter-exchange`/`-routing-key` khớp code → hết PRECONDITION_FAILED 406). **Mac aiworker:** rebuild image `isas.aiservice:local` (new `worker.py`+`config.py` DLX) + recreate container (env giữ nguyên qua `--env-file`, network bridge, `python -m app.worker`) → **connected consumer=1** trên `scoring_pipeline_queue` (declare_topology khớp topology đã tạo, no 406) + Whisper `tiny` tải xong. **Scoring pipeline e2e healthy.** ⚠ Bài học apply-window: (1) migration đụng bảng đang được code cũ đọc (rubric_anchors) PHẢI apply **sau khi deploy image mới** (đợt này CI deploy image trước → apply là bước hoàn tất, không phải breaking); (2) RabbitMQ đổi arg queue = phải recreate hoặc policy, KHÔNG sửa in-place; (3) aiworker `print()` buffered docker logs → thêm `PYTHONUNBUFFERED=1` để log live (cosmetic).
> **Cập nhật 2026-07-18 (Vòng S6 — DB Hardening đợt 10, scope-narrowed):** frontier S6 còn 4 task (DB6/7/8/17) → user chốt **chỉ làm phần "clearly-valuable-now"**, tuần tự 1 PR/task. **DB6 + DB7 = DEFERRED** (DB6 native partitioning: đổi PK composite + online-rewrite bảng tiền LIVE, `answer_scores` không append-only; DB7 leader-election: deploy single-instance nên chưa có double-processing, mọi sweeper đã idempotent) — xem lý do đầy đủ ở [tasks.md](tasks.md) DB6/DB7. **DB8 partial passing (PR A — `s6/db8-admin-keyset-pagination`, merged #73):** giao **admin keyset-pagination** cho 4 endpoint admin (Auth orgs/users · Payment orders · Campaign campaigns) — backward-compat (body giữ mảng JSON; `?cursor=&limit=` opt-in; next-cursor header `X-Next-Cursor`; default limit 500 = hành vi cũ) + keyset `(CreatedAt DESC, Id DESC)` cursor opaque (`Isas.Shared/Pagination`) + **Auth users role-filter push-down** (fix phân trang sai do lọc sau cap 500). **KHÔNG migration**; **Npgsql translation verified** (`ToQueryString`: `id < @p` uuid + `ORDER BY … DESC`); test **+11** (Auth 68→73 · Payment 143→146 · Campaign 202→205 · Interview 251 không đổi = **675**). DB8 off-row-TEXT + read-replica **deferred** (off-row gộp DB17; replica chưa có infra). **PR B — DB17 partial passing (`s6/db17-shared-file-lib`):** shared **PdfPig text extractor** vào `Isas.Shared/Files` (`IPdfTextExtractor`); Campaign `ParserService` + Interview `CVParserService` delegate (Interview giữ structured extraction); gỡ byte-identical `ParseInternal` trùng + gói `PdfPig` khỏi 2 service csproj (chỉ còn ở Isas.Shared); KHÔNG migration; test +3 (Campaign 202→203 · Interview 251→253, tính trên nhánh off main trước khi merge DB8). DB17 **DEFERRED:** SeaweedFS S3-client consolidation (subtle config diff Interview thiếu `UseHttp`/`AuthenticationRegion`, cần smoke-test upload thật) + cross-service file handoff (P2 architecture).
> **Cập nhật 2026-07-18 (Vòng S7 — AIService reliability & security, multi-agent song song 3 worker):** frontier chuyển sang **S7**; user yêu cầu **chia agent chạy cùng lúc**. 3 worker worktree-isolated disjoint (off main `dd11d2c` = đã có DB8+DB17) + supervisor gộp **1 branch `s7/ai-reliability` → 1 PR**. **AI1 ✅** (`s7/ai1-injection`): anti prompt-injection cho endpoint SINH — chỉ 2 builder thật sự hở (`build_prompt`/`build_criteria_prompt`) được bọc delimiter+directive (mẫu E11); 5 builder kia đã có sẵn; test +8. **AI3 ✅** (`s7/ai3-retry`): `worker.py` retry `score()` `ValueError` `score_max_attempts=3` lần trước khi Failed (giữ E10 + transient path); test +3. **AI4 ✅** (`s7/ai4-transcript`): transcript+reasoning+needs_review surface cho HR (drill-down) — Interview `GET /internal/sessions/{id}/answers` (reuse MapAnswer) + Campaign `GET /campaign/{id}/results/{sessionId}/transcript` (gate như OverrideResultAsync); KHÔNG migration; Interview 253→258 · Campaign 206→212. **AI5 ✅ audit clean** (supervisor, read-only): 401-commit history 0 secret leak, `.gitignore` phủ `.env*` → không cần rotate. **AI2 đã xong (đợt 9).** **Gate xanh:** `dotnet build` 0 err · **dotnet test 689** (Auth 73·Payment 146·Campaign 212·Interview 258) · **pytest 77** (66+8+3) · has-pending=No changes ×2 · KHÔNG migration. **S7 = xong** (AI1–AI5). ⚠ L3 tay tại deploy: inject payload qua endpoint sinh thật (AI1) · inject `ValueError` 1 lần → Scored (AI3) · HR mở transcript live (AI4). Follow-up nhẹ: AI3 bump-temp-khi-retry.
> **Cập nhật 2026-07-18 (INT-17 — Phỏng vấn THÍCH ỨNG, feature theo yêu cầu, branch `feat/adaptive-interview` off `origin/main`):** đổi luồng phỏng vấn "batch tĩnh" → **hybrid seed + adaptive** cho **CẢ B2C và B2B**. **AIService:** `POST /api/v1/decide-next` (schema `DecideNextRequest/Response` + `build_decide_next_prompt` anti-injection + `GeminiProvider.decide_next` temp 0.3, action `follow_up|clarify|new_question|end`) — tải audio S3 → transcribe đồng bộ → quyết định câu kế; gate **X-Internal-Token** (fail-closed, mẫu GEN-7); `worker.py` **bỏ Whisper** khi job mang `transcript` (single-source, tiết kiệm N lần self-consistency E10). **InterviewService:** migration `AddAdaptiveInterviewColumns` (`practice_questions.kind`[backfill 'Seed']/`generated_from_answer_id`[unique filtered idx = idempotency] + `practice_sessions.adaptive_enabled/max_follow_ups/max_questions`); `AnswerService.UploadAnswerAsync` → sau khi answer durable + khi **frontier** (mọi câu đã có answer) + còn ngân sách + chưa quá deadline → `IAiServiceInterviewDecider.DecideNextAsync` → append 1 câu (kind) hoặc end; transcript đồng bộ → answer + `ScoringJob.Transcript`; `StuckAnswerRepublisher` cũng mang transcript; câu kế trả trong **response upload** (`UploadAnswerResult` += `Transcript/NextAction/NextQuestion/InterviewComplete`, backward-compat); `QuestionResponse += Kind`. **B2C** = `Adaptive:Enabled` (mặc định **TẮT** = luồng cũ) giữ `SeedCount`(=1) seed; **B2B** = toggle/trần qua `CreateCampaignSessionInternalRequest` (Campaign gửi), seed = toàn bộ campaign questions, chấm cùng tiêu chí (E1 fairness). **Gate xanh:** `dotnet build` 0 err · **dotnet test 699** (Auth 73·Payment 146·Campaign 212·**Interview 268** +10) · **pytest 91** (+14: 12 decide-next + 2 worker) · **has-pending=No changes**. ⚠ **CHƯA merge** — feature-branch chờ PR review (BE `feat/adaptive-interview` @ `github.com/SU26SE043/isas-server`, FE @ `github.com/light2a/SEP490-Angular`). **✅ FE đã wire** (repo `isas-frontend` branch `feat/adaptive-interview`, đã push): B2C badge kind + gợi ý nộp khi `interviewComplete`; B2B append `nextQuestion` trước advance + resume merge full list; vitest 105 pass — chờ merge sang `master` (Vercel). **✅ Migration đã APPLY lên DB server** (2026-07-18, `postgres-main` DB `isas_interview` qua idempotent script + `docker exec psql -U admin`): 5 cột thêm, 51 practice_questions backfill `kind='Seed'`, unique filtered index tạo, `__EFMigrationsHistory` head = `AddAdaptiveInterviewColumns`; running image cũ không ảnh hưởng (additive + default). **Còn:** ①merge 2 PR + CI deploy image mới (interviewservice có decider client, aiapi/aiworker có `/decide-next` + worker bỏ Whisper) · ②đặt `Adaptive:Enabled=true` (B2C) / toggle campaign (B2B) để BẬT · ③**L3 e2e tay** (audio thật → transcribe→decide→câu kế; worker log "bỏ qua Whisper"; latency Whisper request-path — cân nhắc model nhỏ hơn cho sync path).

> **Cập nhật 2026-07-18 (E2E INT-17 TRÊN DEPLOY — adaptive LIVE cả 2 dòng, 64 PASS / 0 FAIL thật):** deploy nốt + e2e toàn hệ trên tunnel + Vercel. **Deploy:** ①rebuild image AIService trên Mac (thêm `/decide-next`) — containers `aiapi`/`aiworker` tạo bằng `docker run` THUẦN (không compose/env-file) → **capture env ra `~/isas-aiapi.env`/`~/isas-aiworker.env` trước khi `docker rm`** (nay đã có env-file vĩnh viễn, gỡ được bẫy mất `GEMINI_API_KEY`), giữ 2 volume `isas_insightface` + `isas_hf_cache`; ②**`WHISPER_MODEL=large-v3 → small`** — `/decide-next` chạy Whisper ĐỒNG BỘ trong request upload, large-v3 (40–90s) sẽ vượt timeout decider 90s + tunnel; small → **upload ~14–21s, qua tunnel OK**; ③`Adaptive__Enabled=true/SeedCount=1/MaxQuestions=6/MaxFollowUps=3` vào compose interviewservice (backup `.bak-int17-*`). **BE PR #78 + FE PR #5 đã merge sẵn** → code adaptive đã live 2 bên. **Kết quả e2e:** **B2C 45 PASS/0 FAIL** (1 seed đúng · hội thoại 4 lượt `clarify→clarify→follow_up→end` · `orderNo=max+1` · `kind` map đúng · `generated_from_answer_id` linkage · trần 6 câu/3 adaptive tôn trọng · re-upload KHÔNG nhân đôi · submit→Scored + 4 tiêu chí + nhận xét AI); **B2B 19 PASS/0 FAIL** (create→publish→invite→join→start(org credit)→…→ranking): **seed KHÔNG cuối → KHÔNG append (2.1s, không gọi AI)**, **seed CUỐI → đuôi thích ứng `FollowUp` orderNo=3 (28.5s)** = đúng thiết kế seeds-first công bằng; ranking điền qua outbox→RabbitMQ→`campaign_rankings` (rank 1, score 30). **Degrade:** `docker stop aiapi` → upload vẫn **200**, answer lưu, `nextAction/nextQuestion=null` → về luồng tĩnh, không mất bài. **Whisper skip verified:** worker log 4× "Dùng transcript có sẵn (bỏ qua Whisper)"; answer không có transcript đồng bộ (lượt hết ngân sách + re-upload reset INT-3) rơi về Whisper worker → sau chấm đủ 4/4 transcript. **UI Chrome thật:** badge `AI làm rõ`(Clarify)/`AI hỏi sâu`(FollowUp) render đúng, câu Seed KHÔNG badge, transcript + điểm + nhận xét hiển thị, 0 console error. ⚠ **Điểm = 0/100 do audio tổng hợp `say` → transcript rác** (đã thống nhất: assert CƠ CHẾ, không assert chất lượng điểm). 🐛 **Bug thật tìm được:** `POST /api/v1/campaign` trả **500** (không phải 400) khi `startsAt/expiresAt` có offset số `+00:00` thay vì `Z` — Npgsql từ chối `DateTimeKind.Local`; FE gửi `Z` nên user chưa gặp, nhưng là lỗi robustness đáng sửa. **CÒN:** ①**toggle adaptive per-campaign cho HR** (CampaignService chưa gửi `adaptiveEnabled` → B2B phải ép cờ bằng SQL để test; cần cột `campaigns` + migration + UI Employer) · ②sửa bug 500 datetime · ③PayOS thật + chất lượng điểm/hội thoại + webcam/mic thật + tải đồng thời = **KHÔNG verify được vòng này**. Log: `~/Downloads/isas-e2e-int17-*.log`.

> **Cập nhật 2026-07-18 (E2E TOÀN HỆ THỐNG — 249 PASS / 3 FAIL, 5 agent song song + UI Chrome 3 role + PayOS tiền thật):** deploy fresh (5 image `:main` build 12:46, tới `fa2ae23`) + AI Mac + FE Vercel. Chia **5 agent disjoint** (Auth 109/0 · B2B 48/0 · Admin 37/**3** · B2C 30/0 · Payment 25/0), supervisor giữ UI + PayOS. **✅ Cả 5 thứ MỚI đều verify live lần đầu:** ①**PR #81 toggle adaptive per-campaign** — chuỗi đủ: UI Employer (toggle + 2 ô trần + mô tả seeds-first) → `campaigns.adaptive_enabled` → xuyên service → `practice_sessions` tôn trọng cờ+trần → `Seed→FollowUp→Clarify→Clarify` dừng đúng `max_questions=4` → Scored (**hết cần ép cờ bằng SQL** — khép mục CÒN ① vòng trước); ②**PR #80 datetime** `Z`/`+00:00`/`+07:00` đều 200, convert UTC đúng (khép CÒN ②); ③**PAY-10 bộ ba** cancel→`Cancelled` + terminal bất biến · HrMember cancel org→403 · sweeper đóng **14 đơn Pending→Expired 1 batch** (verify PayOS trước khi đóng, có nhánh cứu Paid-webhook-rơi); ④**PAY-8+D24 tiền THẬT** order Paid 14:03:02.201 → ledger `Purchase +5` cùng transaction (.203) → ví 10→15, webhook qua gateway + HMAC chặn payload rác 400 (khép CÒN ③ phần PayOS); ⑤**INT-17 B2C** 1 seed · transcript trả trong response upload · `generated_from_answer_id` idempotent · worker 6× "bỏ qua Whisper". **Hạ tầng sạch:** RabbitMQ consumer=1 + DLQ · outbox 0 tồn đọng (Interview+Campaign) · migration head khớp `origin/main` cả 4 DB · GEN-1/GEN-7 kín. 🐛 **3 FAIL + 3 nit — đã sửa hết trong vòng này:** (1) 🔴 **5/5 container prod chạy `ASPNETCORE_ENVIRONMENT=Development`** → request ẩn danh từ Internet nhận full stack trace + path `/src/src/services/...`; **lỗi COMPOSE không phải code** (`Program.cs` gác `IsDevelopment()` đúng; sau `IsDevelopment` chỉ có OpenAPI/Scalar → đổi Production chỉ mất trang docs) → **sed 5 dòng + `up -d`, user chạy**; (2) 🟠 `GET /package/{id}` id lạ → **500** NRE trên endpoint `[AllowAnonymous]` → fix null-check + lọc `IsActive` (gộp luôn nit gói soft-delete vẫn trả 200), branch `fix/package-get-by-id-404`; (3) 🟠 **2 đơn kẹt Pending vĩnh viễn** (13/07, PayOS "mã không tồn tại" → retry vô hạn) → fix `fcd4513` **đã có sẵn** nhưng chưa vào main → branch `fix/order-expiry-infinite-retry` chờ merge; (4) 🟡 **KHÔNG endpoint nào đọc số dư ví** (`GET /me/account` còn 🔜) → cả trang Credit Candidate lẫn Employer không hiện credit → **build endpoint** (chủ ví từ JWT D15, chưa có ví → 200 với 0) + wire FE; (5) 🟡 breakdown per-answer hiện trơ "Điểm tiêu chí" (payload thiếu tên) → **fix thuần FE** (tra `result.criteriaScores[]` cùng response, khỏi đổi BE). **Test:** Payment 172→179 (+7); FE vitest 106 pass; `dotnet build` 0 error. **5 nghi-vấn đã loại sau khi kiểm** (ghi lại kẻo đuổi lại): 4 reservation org "treo" = hợp lệ (deadline campaign 22-23/07) · `criteriaScores: null` = đọc nhầm (nằm trong `result`) · login admin fail = click không submit · credit treo lesson B2C = sweeper bật, `B2CInactivityMinutes=120` · điểm 0-34 = audio `say` (E11 còn trích transcript "hoàn toàn lạc đề" — cơ chế đúng). ⚠ **Quan sát cần team chốt:** BK14 xác nhận live (hạ quyền OrgAdmin→HrMember, token cũ vẫn dùng được tới hết TTL — hệ quả GEN-3) · `logout` chỉ revoke refresh, access token cũ vẫn gọi `/auth/me` · mã lỗi email trùng lệch (register 400 vs org-members 409) · `authservice-dev` crash-loop SIGSEGV (stack dev). ⚠ **Bẫy build FE:** `npm run build` chạy `scripts/set-env.mjs` **ghi đè `src/environments/environment.prod.ts` (file được TRACK)** bằng `API_BASE` local → suýt commit URL tunnel tạm vào prod; luôn `git checkout` file này trước khi commit.
> **Cập nhật 2026-07-19 (DB architecture review → frontier S8):** review kiến trúc DB toàn hệ (**45 bảng / 4 DB**, đọc model snapshot + hành vi transaction code, **read-only, chưa sửa code**). Điểm tổng **6.5/10** — ranh giới microservice (8/10) · outbox + idempotency (8/10) · type discipline (timestamptz + string enum **100% đồng nhất 45 bảng**) đều tốt; điểm trừ nằm ở **defect cục bộ, KHÔNG cần redesign**. 🐛 **4 lỗi Critical đã verify bằng đọc code trực tiếp** (không suy diễn): ①**`WebhookService.cs:100` `credits ?? 0` → CHECK `delta<>0` nổ → `tx.Commit` không chạy → flip Pending→Paid ROLLBACK theo → khách TRẢ TIỀN, đơn kẹt `Pending` mãi**, reachable vì `OrderService` không guard package type (mua gói `Subscription` credits=null) + mọi đường cứu (polling/expiry-reconciler) re-fail deterministic; ②`CreditReservationReconciler.cs:74-96` **lost-update** (WHERE chỉ `Id`, không `AND reserved=@observed`) → job sửa drift **tự tạo drift**, phá credit đang giữ; ③`InterviewEventConsumer.cs:126` nack-requeue vô hạn + `prefetch:1` → **1 poison message chặn TOÀN BỘ queue credit**; ④**invitation token lưu PLAINTEXT** (`CampaignService.cs:1244`) mà redeem → JWT, trong khi cùng repo đã hash refresh token (DB12) = 2 chuẩn cho cùng loại credential. ⚠ **2 finding của subagent đã tự kiểm lại và ĐÍNH CHÍNH** (ghi lại kẻo đuổi nhầm): `reset-password` **KHÔNG phải bypass chiếm tài khoản** mà là **500 vô điều kiện** — `DateTime.Parse("482913")` ném `FormatException` với mọi OTP 6 chữ số (đã chạy thử thật); nhưng nó **cũng không so sánh OTP** → sửa lỗi parse mà quên thêm so sánh thì THÀNH bypass (→ DB24 bắt sửa cả hai); `AuthService.LoginAsync` mint token không check credential **nhưng caller duy nhất `AuthController.cs:78` có gọi `CheckPasswordSignInAsync`** → landmine trên interface, KHÔNG phải lỗ hổng sống. **Scale:** `answer_scores` ~**100M row @1M user** (300M nếu bật `SelfConsistencyN=3` — 1 config flip = 3× storage) với `reasoning` text nằm **inline trong heap** (<2KB, dưới ngưỡng TOAST); `refresh_tokens` **~1 tỷ row/năm @100k DAU, KHÔNG có purge ở đâu** trong Auth → làm chậm đúng đường login nóng. **→ Sinh mục [tasks.md](tasks.md) §S8** 12 task: **P0 `DB20`-`DB25`** (money-loss/security, ước tính gộp ~1 ngày, diệt toàn bộ đường mất tiền đã biết) + **P1 `DB26`-`DB31`** (index thiếu · re-shape 2 sweeper index neo nhầm cột anti-selective · retention · N+1 republisher · rubric scope · phân trang). **P2 (8 finding redesign) CHƯA vào tasks.md** theo scope chốt — liệt kê trong §S8 header, thêm khi chạm feature liên quan.
> **Cập nhật 2026-07-19 (S8 P0 XONG — `DB20`-`DB25`, 1 supervisor + 2 worker worktree song song):** nhánh `s8/p0-money-security`. **Gate xanh:** `dotnet build` 0 error · **861 test** (Auth 122→**131** · Payment 182→**189** · Campaign 234→**249** · Interview 292 không đổi) · `has-pending-model-changes`=No changes · 1 migration (`HashInvitationTokenDb23`, **chưa apply DB thật**). Phân công: supervisor giữ Payment (DB20/21/22 chạm cùng file → tuần tự) + DB25; 2 worker worktree disjoint **DB23**(Campaign) ‖ **DB24**(Auth). 🔬 **Mọi guard đều mutation-verified** (vô hiệu guard trong production → đúng test tương ứng ĐỎ) — không chỉ "test xanh". ⚠ **Bài học đắt nhất vòng này:** test DB21 **lượt đầu RỖNG** — seed `reserved == count` khiến reconciler short-circuit trước khi chạm `ExecuteUpdate`, nên gỡ guard CAS khỏi production vẫn XANH 11/11; phải viết lại bằng `DbCommandInterceptor` chen thật vào khe giữa `CountAsync` và `ExecuteUpdate`. **Nếu không chạy mutation check thì đã ship một fix tiền không hề được test mà vẫn báo "passing".** → **quy ước mới: task money/security PHẢI kèm mutation check, không nhận "test xanh" làm bằng chứng.** 🔎 **2 chỗ review 2026-07-19 tự đính chính:** (1) "cả 4 service thiếu `EnableRetryOnFailure`" **SAI** — Auth vốn đã có (subagent chỉ khảo sát Payment, tôi khái quát quá tay); (2) brief DB23 giả định "không backfill hash được" **SAI** — plaintext vẫn nằm trong DB nên migration hash tại chỗ được ⇒ magic-link đang lưu hành không chết. ⚠ **3 việc PHẢI làm khi deploy:** ①**BREAKING API** `POST /auth/reset-password` nay bắt buộc trường `otp` → **FE `isas-frontend` phải sửa** (không thể để optional vì như vậy bypass còn nguyên); ②queue `payment.credit` LIVE đang `arguments=null` → redeclare kèm `x-dead-letter-*` sẽ ném **PRECONDITION_FAILED 406** (bẫy AI2 cũ) → drain→delete→để consumer khai lại HOẶC set DLX bằng policy; ③token invitation **không còn đọc được từ DB** → quy trình e2e B2B đổi sang lấy raw token từ payload `outbox_messages`. **DB25 partial** (chỉ Campaign; Interview+Payment tách **DB25b** — bật retry mà chưa bọc `CreateExecutionStrategy` sẽ vỡ toàn bộ đường tiền lúc chạy thật, mà **SQLite không chạy execution strategy nên CI vẫn xanh giả**). **Kế:** P1 `DB26`-`DB31` (`DB28` retention value-per-effort cao nhất).
> **Cập nhật 2026-07-19 (S8 P1 XONG — `DB26`-`DB31`, 4 worker worktree song song):** nhánh `s8/p1-perf-scale` (stack trên `s8/p0-money-security`). **Gate xanh:** `dotnet build` 0 error · **921 test** (Auth 131→**142** · Payment 189→**195** · Campaign 249→**268** · Interview 292→**316**, +60) · `has-pending-model-changes`=No changes **cả 4** · **3 migration** (Interview `ReshapeSweeperAndAddLookupIndexesDb262731` · Campaign `AddDb26Indexes` · Payment `AddDb26OrderIndexes`) — **thuần index, chưa apply DB thật**. **S8 = xong** (P0+P1; P2 8 finding redesign vẫn chỉ liệt kê ở header §S8).
> **Chia theo SERVICE-GROUP chứ không theo task** (mẫu S6 đợt 9): DB26/DB28/DB31 mỗi cái cắt ngang 2-3 service → chia theo task thì 2 worker cùng sinh migration trong 1 service và chọi EF snapshot. 4 worker: Interview-index ‖ Interview-code (file-disjoint, chỉ 1 bên được đụng `Migrations/`+`Configurations/`) ‖ Campaign ‖ Auth+Payment. Base **pin `43d26bb`** chứ không `origin/main`, vì P0 chưa merge và đã mang 1 migration Campaign — off main sẽ chọi snapshot.
> 🔑 **Phát hiện quan trọng nhất vòng này (DB27):** partial index chỉ dùng được nếu planner **chứng minh được** predicate query ⇒ predicate index. Nếu EF render `status` thành **tham số** (`status = @p`) thay vì literal thì Postgres hết chứng minh được và **cả 2 index thành vô dụng TRONG IM LẶNG** — index vẫn tồn tại, `EXPLAIN` vẫn seq scan, không có gì báo lỗi. Worker verify bằng `ToQueryString` trên provider Npgsql thật (EF render **literal**) và khoá hợp đồng đó bằng test. Đây là giả định mà bản thân review 2026-07-19 đã mặc định là đương nhiên.
> 🔬 **Mutation-check giờ là quy ước** (rút từ P0): mọi hành vi mới đều bị vô hiệu để xác nhận test chuyển ĐỎ. Supervisor **tự chạy lại độc lập** cho ca rủi ro nhất (purge `refresh_tokens` = xoá dữ liệu auth): vô hiệu vị ngữ → **4 ĐỎ**, gồm đúng ca cửa sổ ân hạn AUTH-9 ⇒ claim của worker đứng vững. Campaign báo trung thực 1 mutation **VẪN XANH** kèm giải thích đúng (điều kiện `PublishedAt != null` về logic là **thừa** vì NULL so với cutoff cho UNKNOWN — không phải test rỗng); Interview-code báo 2 test regression điểm **cố ý vẫn xanh** khi gỡ fix DB30 (chúng assert đường không đổi, đúng vai trò). Phân biệt được "test không chạm code" với "code phòng thủ dư" là dấu hiệu mutation-check đang được dùng đúng chứ không phải chạy cho có.
> ⚠ **3 job purge đều BẬT mặc định.** Cân nhắc: AGENTS.md nói "dọn sau = không bao giờ dọn" (tắt mặc định ⇒ nhiều khả năng không bao giờ bật), và vị ngữ đã mutation-verified + có trần batch + có công tắc. **Khuyến nghị deploy lần đầu: đặt `RefreshTokenRetention__Enabled=false` · `Outbox__PurgeEnabled=false` (Interview+Campaign), chạy quan sát 1 chu kỳ rồi mới bật** — xoá dữ liệu là không đảo ngược được, và SQLite (test) khác Postgres (thật) về ngữ nghĩa NULL/so sánh thời gian. Đây là **quyết định cần team chốt lúc review PR**, không phải việc đã rồi.
> ⚠ **Apply-window:** cả 3 migration thuần index, **KHÔNG cần dọn dữ liệu trước**, nhưng `CREATE INDEX` mặc định **KHOÁ GHI** bảng → dùng **`CREATE INDEX CONCURRENTLY`** chạy tay ngoài EF script (EF không sinh được; `CONCURRENTLY` cũng không chạy trong transaction mà idempotent script của EF thì bọc transaction). Có `DropIndex` ở Interview+Campaign → **tạo mới TRƯỚC, drop cũ SAU**. **L3 EXPLAIN = verify tay** (`SET enable_seqscan=off; EXPLAIN` các query sweeper/lookup → Index Scan) — SQLite/EnsureCreated không chứng minh được gì về planner.
> ✅ **DB31 KHÔNG đổi hợp đồng API, FE không phải sửa** (khác DB24 của P0 vốn breaking): body vẫn mảng JSON, `?cursor=&limit=` opt-in, next-cursor ở header `X-Next-Cursor`. Đổi hành vi duy nhất: >500 campaign/buổi thì trang đầu trả 500 — đúng đánh đổi DB8 đã chốt.
> **Cập nhật 2026-07-19 (AUDIT đối chiếu Functional Requirement → sinh §S9):** rà **18 FR / 3 module + 1 NFR** đối chiếu code thật, **cả BE lẫn FE**, 3 agent song song, chuẩn nghiêm — *"có bảng/enum trong DB" KHÔNG tính là có tính năng*, phải truy được endpoint BE **và** màn hình FE. **Kết quả: 8.5/18 ≈ 47%** (sau khi ratify FR02; đọc rộng câu chữ ~55%). **Chỉ 1/18 FR đạt ĐỦ** (FR01 sinh câu hỏi từ CV+JD — cài đúng thứ tự ưu tiên JD>CV>JobCategory + có chống prompt-injection). Theo module: **B2C 50% · B2B 50% · Admin 25%**; **NFR responsive: desktop/tablet ổn, mobile CHƯA đạt**.
> **Điểm mấu chốt:** cái thiếu **KHÔNG phải bug mà là tính năng chưa từng xây** — khác hẳn S6/S8 (sửa defect trên tính năng đã có). Không vá được, phải làm mới. → sinh [tasks.md](tasks.md) **§S9** 25 task: **P0** `F1`-`F6` (lõi ĐÃ CÓ chỉ chưa lộ ra — rẻ nhất, nâng % nhanh nhất) · **P1** `F7`-`F23` (chưa từng xây) · **P2** `F24`-`F25` (NFR mobile + bug).
> 🔎 **Bốn thứ "có tên mà không có ruột"** (dễ bị chấm nhầm là ĐỦ nếu chỉ đọc tên): field tên `Radar` nhưng FE vẽ **bar** (không có thư viện chart nào trong `package.json`) · `SubscriptionService` là stub `NotImplementedException` **vẫn được DI-register** · `RubricLevel.ExampleAnswers` được đọc ở 3 chỗ nhưng **không có write path nào** nên luôn rỗng · `CampaignQuestion.TimeLimitSeconds` là **cột chết**. Thêm: `CreditTransactionReason.Refund` + `InvoiceStatus.Void` là **enum chết, 0 chỗ ghi** trên toàn `src/`.
> 🔴 **Ba khoảng trống nặng nhất:** ①**không có phiên miễn phí nào** — user đăng ký xong tạo buổi đầu tiên là **402**, cả phễu B2C bắt trả tiền trước khi được thử (`F7`); ②**hệ thống không biết mình đốt bao nhiêu token** — `gemini.py` gọi `generate_content` **13 lần, không lần nào đọc `usage_metadata`** (`F22`); ③**AI không sinh câu hỏi cho B2B** — CampaignService chưa bao giờ gọi `/generate-questions`, FE còn hardcode `source:'CustomHr'` ⇒ 100% câu hỏi B2B là HR gõ tay (`F9`/`F10`).
> ⚠ **Lỗ khai thác thật tìm được (`F4`):** camera **không thật sự bắt buộc** — OS deny camera thì `webcam-capture.ts:121` nuốt lỗi, UI báo "không ảnh hưởng bài thi", ứng viên thi tiếp **không bị giám sát mặt và không có cờ nào** ⇒ HR không phân biệt được "sạch" với "camera chưa từng bật". Vá rẻ, nên làm sớm.
> ✅ **2 quyết định đã chốt (2026-07-19):** (1) **FR02 "5 lĩnh vực" là LỖI TÀI LIỆU, đúng là 3** (BA/BE/FE) → sửa con số trong bản FR, không thêm task code; (2) **XÂY LẠI subscription** cho FR04/FR10 (không sửa spec) → `F8`. ⚠⚠ **`F8` KHÔNG được làm bằng cách gỡ guard DB20** — guard đó chặn mua gói `Type != OneTime` vì `InterviewCredits = null` → ledger `Delta=0` → CHECK nổ → `tx.Commit` không chạy → flip Pending→Paid **rollback theo** ⇒ khách trả tiền mà đơn kẹt `Pending` vĩnh viễn. Gỡ guard = tái tạo nguyên xi lỗi DB20 vừa bịt. Đường đúng: `OrderKind` RIÊNG cho subscription (mẫu `InvoiceSettlement` — nhánh đó không cộng credit, không ghi ledger) + gate "unlimited" ở `ReserveAsync`.
> ⚠ **Đính chính đánh giá ban đầu của supervisor:** đã báo "anti-cheat FE chưa có code phát hiện" — **SAI**, dựa vào ghi chú cũ trong file này thay vì đọc code hiện tại. Thực tế `proctor.service.ts` bắt `visibilitychange`/blur/paste thật, `webcam-capture.ts` chụp frame mỗi 30s, AIService chạy **InsightFace thật** (`no_face`/`multiple_faces`/`face_mismatch`). FR13 thiếu là chỗ khác (camera không ép được + `multi_voice` là cờ ma: có whitelist nhưng **0 code diarization**).
> **Cập nhật 2026-07-19 (S9 P0 XONG — `F1`-`F6` + `F2b`, 3 worker song song):** nhánh BE `s9/p0-fr-gaps` · FE `s9/p0-fe`. **Gate xanh:** `dotnet build` 0 error · **BE 961 test** (Auth 142 · Payment 195 · Campaign 278→**282** · Interview 316→**346**) · **pytest 111→120** · **FE vitest 154→193** · `has-pending-model-changes`=No changes ×4 · **1 migration Interview** (`AddSessionTimeLimitAndQuestionCapF2`) + **1 Campaign** (`AddMembershipIdentityF5`), **chưa apply DB thật**. Chia 3 worker theo service-owner; **cấp riêng 3 worktree FE** vì cả 3 đều chạm FE — file disjoint nhưng `git add/commit/switch` trên cùng working tree vẫn đua nhau (lỗi vòng 13).
> 🔎 **Ba chỗ KẾ HOẠCH CỦA SUPERVISOR SAI, worker sửa đúng — ghi lại để không lặp:** (1) `F2b` kế hoạch bảo ném `ArgumentException`, nhưng `PracticeController` chỉ bắt `InvalidOperationException`(→400) còn `ArgumentException` rơi xuống `catch(Exception)` ⇒ **sẽ ship 500 với MỌI input sai**; (2) `F2b` kế hoạch bảo chỉ cần khai `focusCriteria` trong schema là đủ — khai không thôi chỉ ngăn pydantic nuốt, **dữ liệu vẫn nằm không dùng và BC14 vẫn hỏng y nguyên**, phải luồn vào `build_prompt`; (3) `F4` brief bảo reset cờ report-once "trong `start()`" — reset ở ĐẦU `start()` làm cờ vô dụng (retry khi vẫn bị chặn sẽ bắn trùng), phải reset **khi camera chạy được**.
> 🔑 **`F3` — Chart.js bị loại BẰNG THỰC NGHIỆM** nhờ ép "chạy test trước khi xây UI": jsdom trả `null` từ `canvas.getContext('2d')` ⇒ Chart.js khởi tạo được nhưng **không bao giờ render** ⇒ mọi test radar sẽ **âm thầm chạy nhánh bar fallback** mà ta tưởng đã test xong. Chuyển ECharts `renderer:'svg'` → SVG DOM thật trong jsdom, test đọc được chart thật. ⚠ **NGOẠI LỆ `AGENTS.md` FE cần ratify** (Material+CDK, không thêm thư viện UI khác). ⚠ Sau merge **phải `npm install`** — thiếu type echarts thì `core.init()` trả `any`, TS báo "possibly null" thay vì "thiếu module" (đánh lạc hướng, supervisor đã dính).
> 🔴 **Lỗ cross-service W-B phát hiện, supervisor vá ở bước gộp (`4650c42`):** `CampaignService.ValidateAdaptiveCaps` chỉ chặn số âm ⇒ HR đặt `max_questions=100000` qua sạch; kết hợp CHECK mới `(0..20)` của F2b thì sẽ ném lúc INSERT session — **SAU khi đã reserve credit org (PAY-6)** ⇒ vừa hỏng đường doanh thu B2B vừa để lại reservation mồ côi, mà lỗi lại nổ ở service KHÁC với chỗ nhập sai. W-B không sửa được (W-C đang giữ file) nên chỉ clamp phòng thủ + báo cáo; supervisor vá upstream lúc gộp. **Bài học điều phối: chia worker theo service-owner tạo ra vùng mù ở ranh giới — worker phải BÁO CÁO thay vì im lặng clamp, và supervisor phải vá ở bước gộp.**
> ⚠ **`F2` ĐỔI UX B2C: nay phải trả lời TUẦN TỰ** (recorder câu khác bị khoá) — countdown không có nghĩa nếu vẫn trả lời tự do. Cần nêu khi review PR. 🐛 Bug con trỏ kẹt đã bịt (khoá câu hết giờ làm nó vĩnh viễn `!q.answer`, mà `currentQuestionId()` tìm đúng theo điều kiện đó ⇒ countdown chết im ở câu kế).
> ⚠ **Bẫy môi trường:** worktree FE không có `node_modules` thì `npm test` **thoát 0 mà không chạy gì** (`ng: command not found`) = **false green**. W-B phát hiện. Luôn kiểm số test chạy được, đừng tin exit code.
> **FR sau P0: 47% → ~61%** (F2 khép FR05 · F2b bổ sung · F1/F7 khép một phần FR07 · F3 khép FR08 phần biểu đồ · F4 siết FR13 · F5 khép FR14 phần CSV · F6 khép FR09 phần chất lượng). **Kế:** P1 §S9 `F7`-`F23` (chưa từng xây).
> **Cập nhật 2026-07-19 (Vòng list-paging — 1 endpoint mới + paging 7 list endpoint, 3 worker song song):** nhánh **`feat/list-paging-filters`** (off `2d06a79`), **CHƯA merge — chờ PR review**. ①**`GET /campaign/{id}/invitations` MỚI** (commit `1eeb3e5`): trước đó HR mời qua đường-1 xong đóng tab là **mất dấu hoàn toàn** — `created[]` chỉ sống trong 1 response, mà đường-1 KHÔNG sinh row `cv_submission` nên `GET /candidates` cũng không thấy, và không lấy được `invitationId` để reissue (D4). Thuần read, không migration; `status` suy read-time ưu tiên **Revoked→Joined→Expired→Sent→Queued** (Revoked TRƯỚC Joined để lời mời cũ sau reissue không "thơm lây" trạng thái của lời mời mới cùng email); `Sent`(`email_sent_at`, SMTP thật) tách khỏi `Queued`(mới vào outbox); "đã join" ghép từ `campaign_membership` chứ **KHÔNG** dùng `campaign_invitations.used_at` — cột đó **chưa từng được ghi ở đâu trong `src/`** (dead column từ D1). ②**Paging 7 list endpoint** (3 worker worktree disjoint theo service, gộp octopus 0 conflict): Interview `files`/`roadmaps`/`cv-analysis` · Campaign `candidates`/`invitations`/`my-campaigns` · Payment `my-orders`. **Gate:** build 0 error · **1040 test** (973→+67: Auth 142 · Campaign 290→**325** · Interview 346→**368** · Payment 195→**205**) · `has-pending`=No changes ×4 · **0 migration** · hợp đồng API KHÔNG đổi (body vẫn mảng, `?cursor=&limit=` opt-in, `X-Next-Cursor`, default 500 = hành vi cũ) ⇒ **FE không phải sửa**.
> 🔑 **Ba phát hiện đáng giá hơn phần paging:** ①**`GET /files` là HỞ DỮ LIỆU chứ không chỉ phình payload** — trả nguyên entity `FileRecord` kèm `parsed_text` = toàn văn MỌI CV/JD user từng upload, mỗi lần mở danh sách file. Đã `.Select` DTO **trong SQL** (map sau khi nạp entity thì cột vẫn bị đọc lên — không đạt mục đích), và test bắt **SQL thật** bằng `DbCommandInterceptor` assert không có chuỗi `parsed_text`. ②**`ORDER BY score DESC` thiếu `COALESCE` là bug production mà SQLite KHÔNG THỂ bắt** — SQLite xếp NULL cuối khi DESC, **Postgres xếp NULL đầu** ⇒ ứng viên *chưa chấm* nhảy lên đầu shortlist. Worker phát hiện vì mutation vẫn XANH → điều tra ra test hành vi trên SQLite mù ca này → sửa bằng soi SQL Npgsql sinh từ **chính hàm production**; supervisor chạy lại độc lập: gỡ `COALESCE` → ĐỎ. ③**`cv-analysis` KHÔNG được slim** dù nhìn rất đáng slim — endpoint chi tiết `GET /cv-analysis/{id}` **không có consumer nào**, trang danh sách CHÍNH LÀ trang chi tiết, 3 mảng `string[]` **non-optional** duyệt `@for` không guard ⇒ cắt là **văng runtime trắng cả mục lịch sử**. Đây là lý do bắt worker kiểm FE trước thay vì slim theo phản xạ.
> ⚠ **Hai thứ CỐ Ý không làm, đã vào backlog:** **`BK21`** trần `max_candidates` không phải trần thật (đếm **email distinct** thay vì **row** + `ReissueInvitationAsync` chèn row **không check trần** + cột `int?` nên để trống = hết trần) — paging chỉ che triệu chứng, **cần team chốt ngữ nghĩa trần trước khi code**. **`BK22`** index cho `search`/keyset (9 index) — **hiện `search` seq-scan**; `LIKE '%x%'` **không dùng được b-tree** → phải `pg_trgm` GIN; `CREATE INDEX` khoá ghi → **`CONCURRENTLY` chạy tay ngoài EF script**; index single-col cũ thành **tiền tố trái** → drop chứ đừng cộng thêm (tiền lệ DB31).
> ⚠ **Nêu khi review PR:** `?skill=` (jsonb, không push xuống SQL portable được) lọc **SAU** phân trang ⇒ trang có thể ngắn hơn `limit`, kể cả rỗng, mà **vẫn còn trang sau** — đã ghi XML doc + controller + `campaign.md` + có test khoá, nhưng đây là ngoại lệ duy nhất của hợp đồng paging. **`GET /files` đổi shape** (bỏ `parsedText`/`storagePath`/`storageBucket`) — đã verify `../isas-frontend` không component nào đọc `parsedText` từ list (chỉ gọi `GET /files/{id}/parsed-text` riêng), field khai optional → không vỡ. **`roadmaps` list bỏ hẳn `milestones`** (không trả `[]` — trả mảng rỗng là nói dối "roadmap không có chặng nào"); ⚠ FE `roadmap.models.ts` dùng CHUNG 1 interface cho list+detail với `milestones` required — **đừng "sửa" bằng cách đổi thành `milestones?`**, `strictTemplates` sẽ làm `roadmap-detail.html` fail compile; đường đúng là tách 2 interface.
> 📎 **Lịch sử phỏng vấn KHÔNG nằm trong vòng này vì đã có sẵn** — `GET /practice/sessions/history` keyset-paged từ **DB31**, và chính nó là mẫu 3 worker copy ⇒ 7 endpoint mới đồng nhất hợp đồng với nó.

> **Cập nhật 2026-07-19 (S9 P1 — `F7` suất dùng thử B2C, SOLO Payment):** nhánh **`feat/f7-free-trial-credits`** off `origin/main` (`b5dd0e7` — vòng list-paging đã merged PR #93). **Gate xanh:** `dotnet build` 0 error · **1049 test** (1040→+9: Payment 205→**214**, 3 service khác không đổi) · `has-pending-model-changes`=No changes · **1 migration Payment** `AddFreeTrialGrant` (thuần `ADD COLUMN` + mở rộng CHECK, **chưa apply DB thật**). **InterviewService 0 dòng thay đổi** — quota áp ở tầng `ReserveAsync` nên tự phủ cả buổi luyện, cv-analysis (BC7b) lẫn lesson roadmap (BC14).
> 🔄 **ĐẢO đề xuất trong chính ô `F7` của tasks.md** ("đếm riêng `free_sessions_used`, không ledger") → chọn **một xô + bút toán `FreeGrant +3`**. Lý do 1: xô riêng đẻ ra **loại tiền thứ hai KHÔNG SỔ SÁCH** mà **không bất biến nào dò được khi nó bốc hơi** — hôm nay credit trả tiền bốc hơi thì `remaining + reserved = Σ delta` gãy **thấy được**, đó chính là máy dò drift. Lý do 2 (nặng hơn): **`F8` subscription cũng gate ở `ReserveAsync`**; reservation kiểu sub không trừ xô nào nhưng sẽ mang `funded_by` mặc định `Paid` ⇒ `ReleaseAsync` nhánh prepaid `remaining+1` **ĐÚC credit trả tiền từ hư không** mỗi lần subscriber bỏ ngang, phá vĩnh viễn bất biến. Điều khoản "không sinh `credit_transactions` rác" được diễn giải lại: dòng `FreeGrant` gắn nhãn rõ **là dấu vết kiểm toán**, rác là thứ nhận được khi phát minh xô không sổ.
> 🐛 **Kế hoạch của supervisor có 2 lỗi, agent phản biện bắt được TRƯỚC khi code** (giá trị của vòng phản biện, ghi lại để giữ thói quen): ①**migration backfill `WHERE owner_type='User'` vô dụng và NGƯỢC mục tiêu** — ví chỉ tạo lazy ở `WebhookService.cs:148`, nên `credit_accounts` hiện **chỉ chứa người ĐÃ TRẢ TIỀN**; câu UPDATE đó tặng nhầm đúng nhóm khách đã trả tiền và **chạm 0 user đang kẹt 402** (nhóm kẹt chưa có row nào) ⇒ **bỏ hẳn backfill**, nhóm kẹt được phục vụ bởi đường tạo-ví-lúc-reserve; ②**chỉ cấp ở `ReserveAsync` là bỏ rơi ca "mua gói TRƯỚC khi thi buổi nào"** — nhóm đó tạo ví qua webhook nên sẽ mất trắng suất dùng thử **vĩnh viễn**; sửa bằng cách đặt phần cấp trong `CreateAccountAsync` (một điểm cấp duy nhất, phủ cả 2 đường tạo ví).
> 🔑 **Cấp phải NẰM TRONG câu INSERT**, không phải `UPDATE` tiếp sau: cả 2 đường tạo ví đều chạy đua được và bên thua nuốt `DbUpdateException` rồi đọc lại ví — nếu phần cấp ở bước sau thì **bên thua cấp lần hai**. `UNIQUE(owner_type, owner_id)` chính là thứ bảo đảm "một ví = một suất". ⚠ **CỐ Ý không thêm CHECK `granted >= remaining`**: đó đúng lớp bug **DB22** (bút toán hợp lệ làm nổ CHECK bên trong tx Consume/Release → rollback → reservation kẹt `Reserved` → consumer nack-requeue vô hạn → **nghẽn cả queue credit**).
> 🔬 **Mutation-check 5/5 ĐỎ đúng chỗ** (quy ước task tiền): bỏ gate `ownerType==User` → **2 ĐỎ** · bỏ dòng ledger `FreeGrant` → **5 ĐỎ** (chứng minh bất biến số dư thật sự được khoá, không phải test trang trí) · thêm nhánh "granted==0 thì cấp bù" → **1 ĐỎ** (đường tặng-credit-vô-hạn kinh điển) · hardcode 4 + bỏ qua kill-switch → **2 ĐỎ**.
> ⚠ **2 test cũ chuyển ĐỎ và đó là ĐÚNG** — đã sửa theo hành vi mới chứ không nới assert: `Reserve_KhongCoVi_Insufficient` dùng `OwnerType.User` (tiền đề bị F7 đổi có chủ đích) → tách **2 test**, vế **Org giữ nguyên 402 + không tự tạo ví**; `ApplyPaid_ChuaCoAccount_TaoRoiCong` 7→**10** (3 tặng + 7 mua) kèm assert sổ cái có **cả** `FreeGrant` lẫn `Purchase` và bất biến `remaining+reserved=Σdelta`.
> ✅ **FE không phải sửa** — credit tặng nằm chung `remainingCredits` nên trang Credit tự hiện đúng; `freeCreditsGranted` là field **additive**. ⚠ **Deploy:** env `FREE_TRIAL_CREDITS` (compose dùng `${FREE_TRIAL_CREDITS:-3}` vì chuỗi RỖNG không parse được thành int → thiếu env sẽ làm options binder ném lúc khởi động). ⚠ **Apply-window:** migration drop+add lại CHECK ⇒ Postgres quét kiểm tra toàn bảng dưới ACCESS EXCLUSIVE (bảng tiền hiện rất nhỏ, không đáng kể). ⚠ **Rủi ro chưa xử, cần team chốt:** 1 email = 3 lượt mà đăng ký **không xác minh email** ⇒ lạm dụng bằng email dùng-một-lần là có thật.

> **Cập nhật 2026-07-19 (S9 P1 vòng 1 — `F8` ‖ `F9` ‖ `F20`-Auth + `F23` ratify, 3 worker worktree disjoint):** nhánh gộp **`s9/p1-round1`** (base pin **`35774e7`** = origin/main + F7, **KHÔNG** off `origin/main` vì F7 mang 1 migration Payment chưa merge). **Gate xanh trên bản ĐÃ GỘP:** `dotnet build` 0 error · **1122 test** (1049→+73: Auth 142→**167** · Campaign 325→**351** · Payment 214→**236** · Interview 368 không đổi) · `has-pending-model-changes`=**No changes ×4** · **2 migration** (Payment `AddSubscriptionsF8` · Auth `AddUserBanF20`, **chưa apply DB thật**). Octopus 3 nhánh, 0 conflict (chia theo **service-owner**, không theo task).
> 🔑 **F8 — không gỡ guard DB20, mà rẽ nhánh ở TRÊN nó.** Khối `if (package.Type != PackageType.OneTime) throw` giữ **byte-identical** (supervisor verify bằng diff); gói Subscription đi đường riêng nên bất biến "Kind=CreditPack ⇒ gói sinh credit>0" nguyên vẹn, và guard cũ nay thành **lưới an toàn** cho `PackageType` thêm về sau. Bất biến `remaining+reserved=Σdelta` được giữ bằng cách **KHÔNG ĐỤNG GÌ**: chỗ giữ do thuê bao tài trợ không đổi cột nào, không ghi sổ, ở cả 3 bước — hai vế cùng đứng yên. **Hệ quả bắt buộc:** bất biến DB4/DB21 **thu hẹp** thành `reserved_credits = count(Reserved AND funded_by='Credit')`; thiếu vế đó reconciler đếm cả chỗ giữ subscriber → bơm `reserved_credits` → **rơi đúng hình dạng lỗi DB21** qua một cửa khác.
> ⚠ **Bài học đắt nhất vòng này — M3 XANH ở lượt đầu và worker KHÔNG nhận, đi điều tra tiếp.** Chỗ giữ thuê bao không nâng `reserved_credits` nên guard DB22 `reserved>=1` **vô tình khớp 0 row và chặn hộ** ⇒ mutation trông như đã được phủ. Lớp che đó chỉ đúng khi ví **không có** chỗ giữ credit nào khác. Chuỗi phá thật: trả credit cho buổi A → nâng cấp Premium giữa chừng → bỏ ngang buổi B ⇒ nhánh prepaid chạy, **vừa đúc 1 credit trả tiền chưa từng được mua, vừa phá chỗ giữ của buổi A**. Đây là lần thứ hai (sau S8 P0) một mutation xanh giả suýt cho qua một lỗi tiền — **"mutation xanh" phải bị điều tra chứ không được coi là bằng chứng đã phủ**.
> 🔑 **F20 — brief của supervisor SAI, worker sửa đúng.** Brief viết "user bị ban thì login bị chặn" (ngụ ý chặn ở đường đăng nhập), nhưng có **BỐN** đường phát phiên và chỉ đường đầu kiểm mật khẩu: đăng nhập · Google · refresh · **`provision-candidate`**. Đường cuối là magic-link B2B, cấp JWT **chỉ theo email, không hỏi mật khẩu** ⇒ chặn ở `AuthController.Login` là **để nguyên một lối vòng**. Worker gác ở tầng service phủ cả bốn. Thêm: **cột `banned_at` riêng, KHÔNG dùng lại `lockout_end`** — lockout là khoá tự động do sai mật khẩu, Identity tự xoá ⇒ gộp vào thì một lần đăng nhập thành công hoặc reset mật khẩu sẽ **vô tình gỡ lệnh cấm**.
> 🔬 **Supervisor tự chạy lại mutation cho 3 ca nguy hiểm nhất thay vì tin báo cáo:** F8-M3 (release đúc credit) → ĐỎ · F8-M5 (reconciler đếm nhầm) → ĐỎ · F20-M3 (bypass ban qua `provision-candidate`) → ĐỎ · F9 CAMP-2 (gỡ guard Draft) → **3 ĐỎ**. Đều tái hiện được. Ngoài ra supervisor **tự kiểm hợp đồng .NET↔AIService của F9**: `count` đã được khai trong `schemas.py` (F2b) nên KHÔNG rơi vào bẫy pydantic `extra=ignore` **nuốt field im lặng** — đúng lớp bug đã làm BC14 hỏng âm thầm. 🔧 Supervisor vá 1 chỗ ở F9 (`132513b`): cắt bớt câu AI vượt trần **im lặng** → thêm log.
> ✅ **F23 = ratified, KHÔNG code** ([D25](decisions.md)): magic-link hiện tại (256-bit CSPRNG + hash SHA-256 DB23 + hạn + revoke + single-use) đã đạt mục tiêu "chỉ đúng ứng viên được mời vào được bài". Mật khẩu HR đặt sẽ ngắn/dùng lại/gửi qua **chính email chứa link** ⇒ không tăng an toàn thật; lỗ "email bị chuyển tiếp" là bài toán **định danh**, đã giải bằng face-verify + anti-cheat flag. Việc còn lại là **sửa câu chữ bản FR12**, như đã làm với FR02.
> ⚠ **`F20` mới là PARTIAL** — vế **tặng credit khuyến mãi** thuộc Payment, hoãn sang vòng 2 (tránh 2 worker cùng sinh migration Payment). ⚠ **CÒN cho cả vòng:** FE chưa có gì cho F9/F20 (nút sinh câu hỏi, cột actions admin) · **F10 làm F9 mất giá trị thật**: `PUT questions` gọi `Clear()` + FE hardcode `source:'CustomHr'` ⇒ HR sửa bất kỳ câu nào là **xoá sạch provenance `AiGenerated`** · ban **không lan sang service khác** (candidate bị ban đang thi vẫn làm tới hết TTL, **credit đã reserve không được release**) — cần team chốt cascade · `SubscriptionExpiryReconciler` **bật mặc định** (tiền lệ S8 P1 job purge: nên chốt tường minh lúc review) · không có endpoint huỷ/hoàn thuê bao · **L3 chưa chạy** cho cả 3 task.
> ⚠ **Đính chính đánh số:** `progress.md` tham chiếu **D24** ("PAY-8+D24 tiền THẬT") nhưng `decisions.md` **không hề có D24** — quyết định F23 lấy số **D25** để không bịa nội dung cho một quyết định không ai viết. Cần người biết vụ đó điền lại hoặc sửa tham chiếu.

> **Cập nhật 2026-07-19 (S9 P1 vòng 2 — `F18`·`F19`·`F20`-credit ‖ `F10`·`F16` ‖ `F12`·`F15`, 3 worker worktree disjoint):** nhánh gộp **`s9/p1-round2`** (base pin `462e782` = tip vòng 1). **Gate xanh trên bản ĐÃ GỘP:** `dotnet build` 0 error · **1209 .NET test** (1122→+87: Auth 167 · Campaign 351→**372** · Interview 368→**382** · Payment 236→**288**) · **pytest 120→145** · FE vitest (Campaign 202 · Interview 201) · `has-pending-model-changes`=**No changes ×4** · **4 migration** (3 Payment + 1 Interview, **chưa apply DB thật**). **§S9 P1 nay còn `F11`·`F13`·`F14`·`F17`·`F21`·`F22`.**
> ⚠ **Vòng này bị đứt giữa chừng** (lỗi hạ tầng/API, 3 agent chết). **Việc KHÔNG mất**: cả ba đều kịp commit task đầu, phần dở của task thứ hai còn nguyên trong worktree → resume bằng `SendMessage` (giữ context) thay vì phóng lại. Bài học điều phối: **kiểm worktree + `git log` từng nhánh TRƯỚC khi kết luận mất việc**, và resume rẻ hơn làm lại rất nhiều.
> 🔑 **F10 — worker sắc hoá bất biến hơn brief.** Brief chỉ nói "giữ id câu AI"; worker chỉ ra giữ id mà vẫn cho client gửi `source` thì HR **tự dán nhãn `AiGenerated`** cho câu mình gõ ⇒ vẫn đúng cái lỗ F10 sinh ra để bịt. Nay `source` là **sự thật do SERVER sở hữu**, bỏ qua giá trị client, ở **CẢ create lẫn update** — worker phát hiện `CreateCampaignAsync` có cùng lỗ mà brief không nêu (POST sẽ thành cửa bypass tầm thường). Hệ quả đắt: F9 xoá sạch row `AiGenerated` mỗi lần sinh lại ⇒ câu HR tự dán nhãn "AI" **bị nuốt im lặng** ở lần sinh kế.
> 🔑 **F16 — kiểm lại giả định BK8 thay vì thừa kế nó.** BK8 hoãn PDF vì sợ native dep SkiaSharp; worker chạy probe **trên đúng base image deploy** (`aspnet:10.0`, linux-x64, non-root, read-only `/app`, không `apt-get`) → `NoDependencies` tự chứa `libSkiaSharp`, **Dockerfile không phải sửa**. 🐛 Test so PDF↔CSV **bắt bug thật**: điểm/ngày format theo **culture của server** — máy `vi-VN` in `91,5` trong PDF còn CSV in `91.5` ⇒ hai bản xuất của cùng campaign **mâu thuẫn nhau, đổi theo locale máy chủ**. Sửa bằng `InvariantCulture`.
> 🐛 **F15 — bug migration mà TEST KHÔNG THỂ BẮT.** EF scaffold `defaultValue: ""` cho cột jsonb; chuỗi rỗng **không phải JSON hợp lệ** nên Postgres từ chối ngay tại `ALTER TABLE`, trong khi SQLite/`EnsureCreated` **bỏ qua migration** ⇒ test xanh 100% mà migration hỏng. Sửa tay thành `"[]"`. Cùng lớp với bug `AddAuditColumnsAndTypes` thiếu `;`. **Quy ước cần nhớ: mọi migration đụng jsonb/backfill/raw SQL phải đọc bằng mắt, test .NET không phải bằng chứng.**
> 🔎 **Supervisor tìm ra LỖ TEST mà worker không thấy (F15).** Worker báo "khớp substring → 1 ĐỎ", nhưng đó chỉ đúng với biến thể **containment** (`developer.mozilla.org.evil.com`). Supervisor chạy biến thể **hậu tố** `host.endswith(h)` → **XANH 143/143** ⇒ vector `notdeveloper.mozilla.org`/`evilmozilla.org` **không test nào bắt**. Production code vốn ĐÚNG (khớp host đầy đủ) — nhưng `endswith` là refactor **rất dễ xảy ra** (đổi thế để cho phép subdomain `docs.mozilla.org`), lúc đó domain typosquat lọt allowlist mà không ai kêu. Đã thêm 2 ca khoá (`1a741be`). **Đây là lý do supervisor phải tự nghĩ mutation riêng chứ không chỉ chạy lại mutation của worker.**
> 🔬 **Ba ca "mutation xanh" được xử lý ĐÚNG cách trong vòng này** (quy ước từ vòng 1 đã ăn vào): F10-M7 `RemoveRange` thừa → M7b gỡ **cả hai** dòng → 3 ĐỎ ⇒ phòng thủ dư, không phải đường không test · F18-M4 → hoá ra guard `WHERE status=Paid` **không phải hàng rào tiền** (UNIQUE đã chặn) mà là hàng rào **AUDIT**: thiếu nó, request đến sau **ghi đè `refunded_by` của người đã hoàn thật** ⇒ cột truy trách nhiệm ghi tên nhầm người · F19-M1 → lộ ra một **test yếu hơn tên của nó** (`ChiCongDonPaid_BoQuaDonChuaTra` seed toàn `paid_at = null` nên vị ngữ `status=Paid` **thừa** với chúng).
> ⚠ **F19-M5 "Passed" là KẾT QUẢ GIẢ:** mẫu thay thế không khớp (code xuống dòng) nên mutation **không apply được** mà script vẫn in "Passed!". Worker nhận ra và sửa. **"Passed" của một mutation chưa chắc đã được áp = không phải bằng chứng** — phải xác nhận mutation thật sự vào code trước khi đọc kết quả.
> 🐛 **Brief supervisor SAI 4 chỗ, worker sửa đúng cả 4** (ghi lại để không lặp): ①**F18** — brief bảo "`free_credits_granted` ghi lại phần được tặng, dùng nó", nhưng cột đó là **tổng ĐÃ CẤP** chứ không phải phần **CÒN LẠI**; dùng thẳng sẽ khiến ví đã tiêu hết quà **vĩnh viễn không thu hồi được** credit đã trả tiền (phải trừ tổng đã tiêu đọc từ sổ cái). ②**F19** — brief không nói xử lý đơn đã hoàn, mà đó là điểm quyết định **cả kiến trúc báo cáo** (gộp/hoàn tách đôi, đếm theo **hai mốc thời gian khác nhau**; đếm hoàn theo `paid_at` sẽ khiến khoản hoàn hôm nay **đi ngược sửa doanh thu kỳ đã chốt**). ③**F20** — brief không nêu ca "ví chưa tồn tại", mà đó là **ca chính** (cấp quà cho user mới) và là nơi va chạm F7: tự INSERT ví sẽ **im lặng tước 3 credit** của đúng người vừa được tặng quà. ④**xung đột working tree FE** — brief bảo **cả hai** worker Campaign và Interview "được sửa `../isas-frontend`"; worker Interview phát hiện và tự tạo worktree riêng. **Đúng lỗi đã biết từ vòng 13 mà vẫn lặp** → vòng sau cấp worktree FE riêng ngay trong brief.
> ⚠ **CÒN sau vòng 2:** **cấp credit CHƯA idempotent** (double-click = cấp hai lần; bịt bằng khoá idempotency client cấp, mẫu UNIQUE lọc của F18) · **hoàn tiền chỉ ghi sổ sách, KHÔNG gọi API refund PayOS** (cố ý — sinh mã giả sẽ làm đối soát ngân hàng tin vào mã không tồn tại) · `InvoiceStatus.Void` **vẫn là enum chết, có chủ đích** (hoàn hoá đơn postpaid = mở lại kỳ đã chốt, cần task riêng) · **FE thiếu nhiều** (nút sinh câu hỏi F9, nút hoàn tiền, trang doanh thu, ô cấp credit, trang lịch sử biến động credit, cột actions admin) · **allowlist 26 domain của F15 chưa ai review** và **không đo được tỉ lệ link bị loại** (gom vào `F22`) · **FR03 chỉ phủ nhóm dùng rubric seed** (người có rubric riêng BC16 không nhận 2 tiêu chí mới — cần team chốt migrate) · **token/lượt tăng ~30%** do 6 tiêu chí thay vì 4 · **L3 chưa chạy** cho toàn bộ 7 task.
> ⚠ **Apply-window vòng 2:** 4 migration đều additive + reversible, **không cần dọn dữ liệu trước**; nhưng `ix_orders_paid_at`/`ix_credit_transactions_owner_created` là `CREATE INDEX` **khoá ghi** → dùng **`CONCURRENTLY` chạy tay ngoài EF script**, và **tạo mới TRƯỚC, drop `ix_credit_transactions_owner_type_owner_id` SAU**.

> **Cập nhật 2026-07-19 (S9 P1 vòng 3 — `F13`·`F14` ‖ `F17` ‖ vá FE, 3 worker):** nhánh gộp BE **`s9/p1-round3`** (base `157f71d`) · FE **`s9/r3-fe-integration`**. **Gate xanh trên bản ĐÃ GỘP:** `dotnet build` 0 error · **1263 .NET test** (1209→+54: Auth 167 · Campaign 372→**407** · Interview 382→**401** · Payment 288) · **pytest 145→155** · **vitest 196→264** · `has-pending`=No changes ×4 · 2 migration (Campaign `AddApiKeysF17` · Interview `AddAnswerSampleAnswerF13`, **chưa apply DB thật**). **§S9 P1 nay còn `F11`·`F21`·`F22`.**
> 🔴 **LỖI ĐIỀU PHỐI CỦA SUPERVISOR — suýt làm F10 regress trong im lặng.** Tôi bảo worker FE vòng 3 tạo nhánh **off `master`**, trong khi FE của vòng 2 (`s9/r2-campaign-fe` F10 · `s9/r2-interview-fe` F15) **chưa merge vào master**. Hai worker sửa `campaign-form.ts` độc lập ⇒ trùng việc + chọi khi gộp. **Nguy hiểm thật nằm ở chỗ bản vòng 3 KHÔNG bao hàm bản vòng 2**: nó mang `source` nhưng **không mang `id`**, mà thiếu id thì mỗi lần Lưu là **xoá-và-tạo-lại ⇒ mất id câu AI** — đúng thứ F10 sinh ra để chặn. Lấy nguyên bản mới (phản xạ tự nhiên vì nó "nhiều tính năng hơn") sẽ khiến F10 **regress mà không test nào kêu**. Supervisor gộp tay: giữ nút sinh + dialog + badge của vòng 3, ghép lại phần echo `id` của vòng 2 (`60f86ec`). **Bài học: cấp worktree riêng là CHƯA ĐỦ — base của mọi worker FE phải là nhánh tích hợp FE của vòng trước, không phải `master`.**
> 🔑 **Hai bộ test FE mã hoá HAI HỢP ĐỒNG MÂU THUẪN** về `source` (vòng 2: không gửi · vòng 3: phải gửi). Trọng tài là BE: F10 **cố ý bỏ qua** `source` client gửi vì nguồn gốc là sự thật do server sở hữu ⇒ payload không mang `source`; 2 test vòng 3 assert ở tầng payload được **chuyển xuống assert ở tầng form** — đúng chỗ ý định của chúng (provenance sống sót qua vòng đọc→sửa→lưu để badge không biến mất) có nghĩa. Đổi tiền đề có chủ đích, lý do ghi tại chỗ.
> 🔎 **Phát hiện có giá trị TOÀN REPO (F17):** `System.Text.Json` **escape non-ASCII**, nên `DoesNotContain("bí mật nội bộ", json)` **XANH một cách tầm thường KỂ CẢ KHI dữ liệu đã rò**. Supervisor verify độc lập bằng probe (`{"Note":"bí mật..."}` → `Contains = False`), rồi **quét toàn repo: 19 chỗ assert tiếng Việt nhưng KHÔNG chỗ nào khác dính** (chúng assert vào collection/predicate/chuỗi thường/text trích PDF, không phải JSON serialize). Bẫy có thật nhưng **đã khoanh vùng** — không phải báo động mơ hồ. **Quy ước: assert chuỗi vào JSON serialize phải dùng sentinel ASCII.**
> 🔎 **Supervisor tự nghĩ mutation riêng và tìm ra lỗ worker không thấy — lần thứ hai liên tiếp, cùng một dạng: bộ test HẸP HƠN worker tin.** F17: worker mutate **chỗ tiêu thụ** cờ PII; supervisor mutate **NGUỒN** cờ (hardcode `IncludePii = true` lúc dựng principal) → **406/406 vẫn XANH**, vì `PublicApiF17Tests` dựng claim **bằng tay** (không qua `AuthenticateAsync`) còn `ApiKeyServiceF17Tests` chỉ có **ca dương** `Assert.True(...)`. Hệ quả nếu không bịt: ai làm hỏng mapping sao cho **mọi key đều đọc được PII ứng viên** sẽ không test nào kêu. Đã thêm vế âm (`87bfeb3`). *(Vòng 2 là lỗ typosquat hậu tố ở allowlist F15.)* **Chạy lại mutation của worker là chưa đủ — supervisor phải tự nghĩ mutation ở góc khác.**
> 🔑 **F14 — worker từ chối bịa số.** Brief hỏi "chuẩn ngành lấy từ đâu"; worker trả lời thẳng **hệ thống không có dữ liệu đó** và chỉ dùng 2 nguồn có thật (`PeerAverage` = trung bình người luyện cùng vị trí, `PassThreshold` = ngưỡng đạt nội bộ), **nhãn nói đúng nguồn**, kèm **test CẤM chuỗi "chuẩn ngành"/"industry" xuất hiện trong nhãn**. ⚠ `PeerAverage` là trung bình người dùng **của sản phẩm này**, không phải mặt bằng thị trường — muốn chuẩn ngành thật thì cần **nguồn ngoài**, KHÔNG phải đổi nhãn cho số hiện tại.
> 🔑 **F13 — quyết định bằng con số, không bằng cảm tính.** Sinh câu trả lời mẫu **lúc chấm, trong cùng call Gemini** (chỉ thêm ~250 **output** token) thay vì lazy (phải nạp lại ~1.000+ **input** token) ⇒ lazy chỉ rẻ hơn khi tỉ lệ mở kết quả **< ~20%**, mà đây là thứ người dùng vừa trả 1 credit để làm. Thiếu/rỗng → `None`, **KHÔNG raise**: raise sẽ biến tính năng phụ thành đường làm answer `Failed` ⇒ mất credit (PAY-13).
> ⚠ **CÒN sau vòng 3:** **rate-limit của F17 KHÔNG mutation-check được** (middleware wiring, cần `WebApplicationFactory` mà repo không dùng — worker tự gọi đây là *lỗ hổng lớn nhất trong bằng chứng*) · **limiter in-process** ⇒ N replica thì trần thật N× (đúng hôm nay vì single-instance) · **key API sống qua nhân sự**, không cascade từ Auth — **cùng nhóm với "ban user không lan sang service khác", cần team chốt một thể** · **`MinSampleSize=5` (F14) chưa ai chốt** · **cấp credit vẫn chưa idempotent** (guard hiện chỉ ở UI: không đỡ được reload-rồi-gửi-lại, cũng không đỡ hai admin cùng lúc) · **doanh thu: FE gửi `yyyy-MM-dd` mà BE coi timestamp trần là UTC** ⇒ admin `+07:00` thấy biên lệch 7h (cần team chốt) · **BE lệch tên field**: cùng một lượng là `clawbackPossible` trong body 409 nhưng `creditsClawedBack` trong body 200 — đọc nhầm tên ra `undefined` đúng màn hình mà con số mới quan trọng · **L3 chưa chạy** cho toàn bộ vòng.

> **Cập nhật 2026-07-19 (S9 P1 vòng 4 — `F11` ‖ `F22` ‖ FE quản lý API key, 3 worker):** nhánh gộp BE **`s9/p1-round4`** (base `a0bf3ca`) · FE `s9/r4-fe`. **Gate xanh trên bản ĐÃ GỘP:** `dotnet build` 0 error · **1297 .NET test** (1263→+34: Auth 167 · Campaign 407 · Interview 401→**415** · Payment 288→**308**) · **pytest 155→200** · **vitest 264→277** · `has-pending`=No changes ×4 · 2 migration (Interview F11 · Payment F22, **chưa apply DB thật**). **§S9 P1 nay CHỈ CÒN `F21`.**
> ✅ **Phân quyền sở hữu file có tác dụng:** F11 và F22 cùng sửa `providers/gemini.py` nhưng brief chia rõ (F11 = `transcriber.py`/`prompts.py`/chữ ký `score()`; F22 = **lớp bọc `generate_content`**, bọc tại **một chokepoint duy nhất**) ⇒ **gộp 0 conflict**. Đây là cách xử đúng khi hai task buộc phải chạm cùng file, thay vì để hai worker tự va rồi supervisor gỡ (vụ `campaign-form.ts` vòng 3).
> 🔑 **F11 — worker SỬA LẠI định hướng của supervisor, và nó đúng.** Brief bảo "bật `word_timestamps`"; worker chỉ ra bật là **thừa và có hại**: `seg.start/.end` **Whisper đã tính sẵn** và đang bị vứt ở `transcriber.py`, còn `word_timestamps` chạy thêm cross-attention + DTW — mà `/decide-next` transcribe **ĐỒNG BỘ trong request upload** (deploy đã phải hạ Whisper xuống `small` chính vì đường đó). Sửa đúng là **NGỪNG VỨT**, không phải tính thêm.
> ⚠ **Ba dạng "bằng chứng giả" xuất hiện trong CÙNG một vòng** — đáng ghi thành checklist: ①**F11-M16 XANH giả** (bộ test phủ *mapper* nhưng không phủ chỗ *đấu dây* ⇒ đúng answer phải cứu bằng republisher, tức lúc **đã có sự cố**, thì mất chỉ số); ②**verifier của worker F22 SAI** — 6 mutation báo "git diff sạch" vì file mới còn **untracked** nên `git diff` im lặng ⇒ kết quả lượt đầu vô nghĩa, phải làm lại bằng so hash; ③**supervisor tự tạo ĐỎ giả** khi mutate hỏng cú pháp → 9 lỗi biên dịch — **ĐỎ do compile error KHÔNG phải bằng chứng** (worker FE vòng này cũng độc lập nêu đúng điểm đó khi loại 19/20 test đỏ-cascade). **Quy ước: mutation phải (a) thật sự vào code — verify bằng hash chứ không `git diff`, (b) biên dịch được, (c) đỏ đúng test có ý nghĩa chứ không đỏ theo cascade.**
> 🔑 **F22 — chọn đường lưu dưới GEN-4 bằng lập luận, không bằng tiện tay.** AIService cấm ghi DB ⇒ loại (a) trả usage kèm response (**đường worker qua RabbitMQ KHÔNG có "response"** để đính kèm, và số liệu rải 2 DB thì không chỗ nào trả lời "tháng này tốn bao nhiêu"), (b) `/metrics` in-memory (mất khi restart — aiworker restart là chuyện thường), (c) log gom ngoài (chưa có hạ tầng) → chọn **callback nội bộ về PaymentService**: vừa **không chạm ranh giới worker khác**, vừa đúng bản chất (chi phí AI là câu hỏi **tiền**, chỉ có nghĩa khi đọc cạnh doanh thu F19). Đơn giá **snapshot lên từng dòng** (mẫu `Invoice.UnitPrice`) → Google đổi giá không hồi tố lịch sử.
> 📊 **F22 giải quyết được một tranh cãi bằng số:** con số "**token/lượt tăng ~30%**" ghi ở F12 **không khớp ở vế input** — đo offline kích thước prompt chấm: 4 tiêu chí `5.988` ký tự → 6 tiêu chí `6.920` = **+15,6% phía input**; phần còn lại phải đến từ **output** (6 khối reasoning thay vì 4). Từ nay đo được thật thay vì đoán.
> 🔎 **FE tìm 3 lỗi hạng "UI đẹp nhưng kết luận sai".** Nặng nhất: **F14 trục thiếu mốc bị vẽ thành 0** thay vì để khuyết ⇒ đường mốc bị dìm xuống đáy, người luyện trông như **VƯỢT mốc** ở đúng những tiêu chí **chưa hề có mốc** — sai lệch luôn nghiêng về phía **khen** người dùng, tức kiểu sai không ai đi báo. Supervisor tự mutate xác nhận (`'-'` → `0`) → 2 ĐỎ. Hai lỗi còn lại: F13 chuỗi toàn khoảng trắng lọt qua truthiness (đúng cái khung rỗng ca `null` sinh ra để chặn) · F14 chú thích nguồn nằm trong `@if (showRadar())` nên dưới 3 tiêu chí thì **hiện mốc mà không nói mốc là gì**. Màn API key chống rò bằng **cấu trúc**: `ApiKeyListItem` không khai trường `key` ⇒ bind nhầm là lỗi biên dịch.
> ⚠ **Hai chỗ worker CỐ Ý không làm, và supervisor đồng ý:** ①**F19 lệch múi giờ** — không nắn biên ở FE vì việc gom bucket chạy **phía server cắt theo ngày UTC**, nắn ngoài sẽ đẻ bucket đầu/cuối bị hụt = **một cái sai tinh vi hơn cái đang có**, và nó đổi con số doanh thu ⇒ **quyết định nghiệp vụ cần team chốt** (kỳ kế toán theo giờ VN hay UTC?). Phần an toàn đã làm: hiện "Kỳ đã tính" quy chiếu UTC tường minh — trước đó BE vẫn echo `from`/`to` nhưng **FE chưa bao giờ render, chính đó khiến độ lệch vô hình**. ②**F18 lệch tên field** — worker kiểm và kết luận **FE đã đọc ĐÚNG cả hai** (`clawbackPossible` ở 409, `creditsClawedBack` ở 200; hai interface tách riêng nên đọc chéo là lỗi biên dịch) ⇒ **không sửa gì**. Brief của supervisor sai tiền đề ở đây.
> ⚠ **CÒN sau vòng 4:** **FR06 chưa khép** — `deliveryMetrics` mới nằm trong response, **chưa màn hình nào hiện** · **đơn giá AI trong `appsettings.json` là phán đoán, PHẢI đối chiếu bảng giá Google** trước khi tin số chi phí · ngưỡng F11 (`PAUSE_THRESHOLD_SEC=0.7`, dải 180–320 **âm tiết**/phút) chưa ai chốt · `ai_usage_logs` **chưa có purge** (bảng nhiều dòng nhất Payment, 1 dòng/lượt × `SelfConsistencyN` — cùng nhóm DB28) · **BC16: rubric riêng không nhận tiêu chí mới ⇒ FR03 + FR06 đều chỉ phủ nhóm dùng seed, cần team chốt MỘT THỂ** · `Down()` của F11/F12 vướng FK `answer_scores` (Restrict) nếu đã có điểm → rollback không sạch · **L3 chưa chạy** (cần env `USAGE_SINK_BASE` cho **cả** `aiservice-api` lẫn `aiservice-worker`).

> **Cập nhật 2026-07-19 (S9 P1 vòng 5 — `F21` + vá `F11`, ĐÓNG §S9 P1):** nhánh BE **`s9/p1-round5`** · FE **`s9/r5-fe-integration`**. **Gate xanh trên bản ĐÃ GỘP:** `dotnet build` 0 error · **1315 .NET test** (1297→+18: Auth 167 · Campaign 407 · Interview 415→**433** · Payment 308) · **pytest 200→215** · **vitest 286→298** · `has-pending`=No changes ×4 · 2 migration (**chưa apply DB thật**).
> **§S9 P1 XONG:** `F7`-`F22` passing (F20 đủ 2 vế, F21 thu hẹp có chủ đích) + `F23` ratified ([D25](decisions.md)). **Tổng phiên: .NET 1040→1315 · pytest 120→215 · vitest 154→298.**
> 🔴 **Lỗi F11 nặng hơn báo cáo ban đầu — và đó là bài học về việc đọc TRIỆU CHỨNG hay đọc ĐƯỜNG ĐI.** Worker FE thấy triệu chứng hiển thị (UI sắp hiện "0 lần/100 âm tiết"). Worker BE truy tiếp và thấy phần nặng hơn: **cả hai đường đẩy job đều đi qua `Read()`** (`AnswerService.cs:360` đường thích ứng = đường CHÍNH, và `StuckAnswerRepublisher.cs:153`) ⇒ `build_delivery_block` in ra **"nói trong 0s / tổng 0s audio"** và **"0 lần/100 âm tiết"** trong khối tự giới thiệu là *"số liệu thật"*, **ngay trên dòng dặn LLM coi chỉ số thời gian là bằng chứng ĐÁNG TIN NHẤT**. Vừa bịa số vừa bảo mô hình tin nó nhất, và số bịa **nghiêng về phía khen**. ⚠ **Đề xuất của supervisor ("bỏ 4 field khỏi DTO đọc — rẻ, thật thà") là SAI**: bỏ field chỉ chữa triệu chứng hiển thị, `_num()` phía Python vẫn mặc định 0 nên **prompt vẫn nhận 0**, mà lại mất luôn số đo có thật ở thời điểm đo. Worker chọn **lưu thêm cột + đổi 4 field sang nullable**. Supervisor tự verify: bịa lại `?? 0` → **ĐỎ** đúng test `VaF11_FieldKhuyet_RaNull_KhongPhaiSo0` (mutation verify bằng **so hash** trước khi đọc kết quả).
> 🔑 **F21 — hai lỗi do CHÍNH test mới bắt được, không phải nghĩ ra trước:** ①`get()` dùng `if value` mà **chuỗi toàn khoảng trắng là truthy trong Python** ⇒ âm thầm xoá một đoạn hướng dẫn, triệu chứng duy nhất là **chất lượng chấm tệ dần**; ②khe `questions.guidance` ban đầu bọc cả dòng "tạo đúng N câu" ⇒ override làm **mất số lượng câu hỏi** — đúng hạng lỗi F21 sinh ra để chặn, mà worker suýt tự tạo lại. **Guard cấu trúc:** chokepoint `_generate` của F22 **quá muộn** (chạy SAU lúc dựng prompt) ⇒ thay bằng **test AST** khoá "mọi hàm gọi `build_*` phải gọi `refresh_if_stale`".
> 🔑 **F21 giữ enum 3 ngành là AN TOÀN, không phải giới hạn kỹ thuật.** User chốt *"chỉ cần 3 ngành và custom 3 ngành đó"*. Ngành không có rubric ⇒ 0 tiêu chí active ⇒ INT-9 → `Failed` ⇒ **user trả credit nhận buổi hỏng**. Ghi lại cho lần mở sau: **4 cột đã là string trên đĩa** và wire contract cũng string ⇒ mở tập ngành **KHÔNG phải data migration**; cái chặn chỉ là kiểu C# + validation + bắt buộc khai rubric kèm.
> ⚠ **Kế hoạch apply migration của supervisor SAI, script lộ ra trước khi chạm DB:** dự định xếp `AddLessonResourcesF15`/`AddAnswerSampleAnswerF13` vào đợt-1 (apply trước deploy) — **bất khả thi**, vì `AddLanguageRubricCriteriaF12` (đợt 2, ghi `rubric_criteria`) đứng **TRƯỚC** chúng theo timestamp và EF không cho nhảy cóc. ⇒ **toàn bộ 4 migration Interview lùi sang đợt 2**. **Đợt 1 = 8 migration** (Payment 6 · Auth 1 · Campaign 1), tất cả thuần additive.
> 🔎 **Preflight DB thật (2026-07-19, read-only):** số dư âm **0** · `delta=0` **0** · email trùng **0** · refresh token trùng **0**. Nhưng **2/5 ví lệch bất biến `remaining+reserved=Σdelta`** (Org `0610da24` +9 · User `a3907a47` +2). **Không phải bug code**: sổ cái ví Org có **10 lần `Consume` xảy ra TRƯỚC lần `Purchase` đầu tiên** (18:12 vs 23:56 ngày 15/07) ⇒ số dư ban đầu được **nạp thẳng bằng SQL** trong cửa sổ e2e 15–16/07 mà chính progress.md đã ghi là phải "seed tay". Giả thuyết vô tội "postpaid reserve không ghi sổ (BK11)" đã **thử và bác bỏ** — cả hai ví đều `Prepaid`. **KHÔNG sửa số liệu tiền**; drift này **không chặn migration** (CHECK mới chỉ thêm vế `free_credits_granted >= 0`, cột default 0 nên mọi row thoả).
> ⚠ **Tự sửa lại một chỉ dẫn thừa trong kế hoạch:** plan viết "dùng `CREATE INDEX CONCURRENTLY` chạy tay". Ở quy mô thật (`credit_transactions` **34 dòng**, `credit_accounts` **5**) thì `CONCURRENTLY` là **nghi thức không mua được gì** mà lại buộc tách script chạy tay ⇒ **nhiều đường sai hơn**. Chạy script thường. Giữ lại *lý do*: khi bảng lớn mới thành vấn đề thật — và lúc đó cái cần sửa là **THỨ TỰ** (script F19 hiện **DROP index cũ TRƯỚC rồi mới CREATE index mới**), không phải bản thân `CONCURRENTLY`.

> **✅ ĐÃ APPLY migration ĐỢT 1 lên server thật (2026-07-19, `postgres-main`)** — 8 migration thuần additive qua idempotent script + `docker exec psql`, `ON_ERROR_STOP=1`. **Head sau apply:** Auth `AddUserBanF20` · Payment `AddAiUsageLogsF22` · Campaign `AddApiKeysF17` · **Interview GIỮ NGUYÊN `AddSessionTimeLimitAndQuestionCapF2`** (đúng chủ đích — xem đợt 2). **Verify sau apply:** số dòng bảng tiền **KHÔNG đổi** (5 ví · 34 bút toán · 31 đơn) · số ví lệch bất biến **vẫn đúng 2**, không tự sinh thêm · `free_credits_granted integer default 0` có mặt · bảng `subscriptions`+`ai_usage_logs` có mặt · `users.banned_at/banned_by/ban_reason` có mặt · `api_keys` có mặt · **tráo index F19 đúng**: `ix_credit_transactions_owner_created` CÓ, `ix_credit_transactions_owner_type_owner_id` đã MẤT.
> 🔴 **ĐỢT 2 CHƯA APPLY — CHỈ chạy SAU khi deploy code mới.** 4 migration Interview (`AddLanguageRubricCriteriaF12` → `AddLessonResourcesF15` → `AddAnswerSampleAnswerF13` → `AddDeliveryMetricsAndFluencyCriterionF11`). F12 và F11 **`UpdateData` vào `rubric_criteria`** — bảng code đang chạy đọc ở 4 site. Apply sớm ⇒ DB có 7 tiêu chí trong khi AIService cũ chưa biết chấm ⇒ **INT-9 "thiếu tiêu chí → `Failed`" ⇒ mọi buổi chấm B2C hỏng, user đã trả credit**. Không tách nhỏ được vì F12 đứng trước F15/F13 theo timestamp.
> ⚠ **Env PHẢI đặt trước khi deploy code:** `FREE_TRIAL_CREDITS` (compose dùng `${FREE_TRIAL_CREDITS:-3}` — chuỗi rỗng không parse được thành int, thiếu sẽ làm options binder ném lúc khởi động) · `USAGE_SINK_BASE` cho **CẢ** `aiservice-api` **LẪN** `aiservice-worker` (endpoint sync cũng đốt token).
> 📌 **Nhánh đã push, CHƯA merge (chờ PR review):** BE **`origin/s9/p1-round5`** (bao hàm trọn vòng 1–5) · FE **`origin/s9/r5-fe-integration`** (bao hàm **cả 8** nhánh FE của 4 vòng — đã verify bằng `merge-base --is-ancestor`). ⚠ **Merge THẲNG nhánh tích hợp, ĐỪNG merge lẻ `s9/r2-*`/`s9/r3-*`** — đó chính là cách dựng lại bẫy vòng 3 (hai bản `campaign-form.ts` độc lập, bản mới thiếu echo `id` nên suýt regress F10 mà không test nào kêu). `master` FE vẫn ở `6639706`, chưa có gì của cả 5 vòng.

> **Cập nhật 2026-07-19 (vòng vá sau DB review — 3 worker disjoint):** nhánh **`fix/db-review-round`** (base `6756c64`). **Gate xanh:** `dotnet build` 0 error · **1348 .NET test** (1315→+33: Auth 167→**179** · Campaign 407→**418** · Interview 433→**443** · Payment 308) · **pytest 215→224** · `has-pending`=No changes ×4 · **1 migration Campaign** (`invitation_id` + thu varchar, **chưa apply DB thật**).
> Sửa 3 phát hiện của bản review DB: ①`campaign_membership` nay có **FK `invitation_id`** → dựng lại quan hệ mà DB16 làm đứt (nguyên nhân khiến F5 phải sao chép `email` sang bảng thứ ba); ②**`answer_scores.prompt_version` nay thật sự được ghi**; ③**AuthService có transaction** cho 5 đường tạo tài khoản. **E11b hoá ra đã có sẵn** (chỉ sai trạng thái backlog) · **E9b bỏ có chủ đích** theo quyết định user.
> 🔑 **Brief của supervisor thiếu một ràng buộc ở CẢ HAI task, và nếu worker làm theo đúng lời thì fix sẽ HỎNG HƠN BUG.** ①Auth: `EnableRetryOnFailure()` đang **bật** (`Program.cs:144`) ⇒ gọi thẳng `BeginTransactionAsync` dưới execution strategy có retry sẽ ném `InvalidOperationException` ở **MỌI lần đăng ký trên Postgres**, trong khi **SQLite xanh 100%** vì nó không chạy execution strategy — tức fix "sửa lỗi đăng ký" làm **hỏng hoàn toàn việc đăng ký** mà CI không kêu. Đúng lớp DB25b, ngược chiều. Worker dùng `CreateExecutionStrategy()` và khi mutation xanh lượt đầu thì **không nhận**, dựng hẳn test cấu hình SQLite với execution strategy giả để tái hiện đúng lỗi production. ②`prompt_version`: brief để mở việc lấy dấu từ đâu; worker loại phương án rẻ (Interview tự đọc lúc lưu) vì nó **sai một cách không phát hiện được** — registry cache theo TTL, F21 **cố ý fail-open về cache CŨ**, chấm thì async qua RabbitMQ và republisher có thể chạy sau **hàng giờ** ⇒ "phiên bản lúc callback" thường khác "phiên bản đã thực sự chấm". **Con dấu sai tệ hơn NULL**: cả lý do tồn tại của cột là trả lời "hai điểm này có cùng thước đo không", con dấu nói dối thì nó trả lời **sai một cách tự tin**.
> 🔑 **`null` ≠ "khác thước đo".** Dòng khuyết con dấu (worker AIService cũ) **không** được suy ra là khác phiên bản — suy "khác" từ "không biết" là bịa, và sẽ gắn cờ `needs_review` oan cho **toàn bộ** điểm chấm trước mốc deploy.
> 🔴 **BÀI HỌC ĐO LƯỜNG QUAN TRỌNG NHẤT VÒNG NÀY — bổ sung cho quy ước mutation-check.** Worker Campaign phát hiện harness của **chính nó** sai: restore bằng `shutil.copy2` **giữ mtime cũ** ⇒ MSBuild coi file up-to-date ⇒ lần `dotnet test` kế **chạy DLL của mutation TRƯỚC**. Triệu chứng: sau battery, baseline "5 fail" đúng bằng tập ĐỎ của M5; worker suýt kết luận nhầm là FK/EF nondeterminism và đã đi **2 nhánh điều tra sai** (`HasConstraintName`, partial index — cả hai "fix được" chỉ vì chúng **ghi file** nên ép rebuild). ⇒ **`git diff`/`sha` chứng minh SOURCE đúng nhưng KHÔNG chứng minh BINARY đang test là source đó.** Quy ước bổ sung: sau mỗi restore phải **ép mtime mới** (`os.utime`) và **chạy lại baseline**. Supervisor dính đúng biến thể khác của lỗi này khi verify: chạy `dotnet test` ở **worktree sai** (nhánh gộp nằm trong `isas-FX1` còn worktree chính vẫn ở `s9/p1-round5`) nên đọc ra số của code cũ.
> 🔎 **Worker `prompt_version` tự tìm ra một khe SAU KHI đã chạy xong 8 mutation:** `AnswersController` bind `[FromBody]` thẳng ⇒ nếu Python đổi khoá JSON thì .NET bind hụt → **cột NULL vĩnh viễn = y nguyên con bug đang sửa**, mà test hai bên **vẫn xanh** vì mỗi bên chỉ assert hợp đồng của chính mình. Cùng lớp với bug pydantic `extra=ignore` nuốt `focusCriteria` (BC14). Đã bịt bằng test đọc thẳng file DTO .NET; supervisor verify độc lập: đổi khoá phía Python → **4 ĐỎ** gồm đúng test hợp đồng chéo.
> ⚠ **CÒN:** `campaign_membership.candidate_id` vẫn **nullable trong UNIQUE `(campaign_id, candidate_id)`** ⇒ Postgres coi NULL distinct nên nhiều membership NULL cùng campaign vẫn insert được — worker **cố ý không sửa và báo lại** (fix đúng là `NOT NULL`, lan sang nhiều chỗ, là defect KHÁC); dữ liệu thật **0 dòng NULL** ⇒ tiềm ẩn, chưa chảy máu · **cảnh báo tầng ranking chưa có** (CAMP-10/E4 và BC15 vẫn trộn được 2 `prompt_version`, nhưng **nay đã có dữ liệu** để làm — phần còn lại của BK23) · điểm chấm trước mốc này **vĩnh viễn NULL** · `needs_review` nay có **nguồn thứ ba** mà UI không phân biệt được lý do · `campaign_invitations.used_at` vẫn là **dead column**, nay `invitation_id` phủ đúng ngữ nghĩa đó ⇒ có thể drop ở task riêng · **dữ liệu mồ côi cũ không được dọn** (fix chỉ chặn nguồn) — supervisor đã kiểm DB thật: **0 user thiếu role**, 7 Employer không thuộc org nhưng **cả 7 là tài khoản test** (`e2e-*`, `sweep*`) và A6b **cho phép** xoá membership giữ User ⇒ hợp lệ, không phải mồ côi.
> ⚠ **Apply-window:** migration Campaign có `ALTER … varchar(255)` — **FAIL TO nếu DB thật có giá trị > 255** (fail rõ, không cắt cụt); câu kiểm read-only đã ghi sẵn trong migration. Backfill `invitation_id` là **Postgres-only**, SQLite/`EnsureCreated` bỏ qua ⇒ **không test nào phủ, đã đọc bằng mắt**; L3 Postgres chưa chạy.

> **Cập nhật 2026-07-20 (E2E TOÀN HỆ trên deploy — vòng đầu tiên chạm S9; 6 agent song song + supervisor giữ UI/PayOS; PayOS TIỀN THẬT 4.000₫):** ~**260 check**, kết quả **2 🔴 · 6 🟠 · 7 🟡**, sinh [tasks.md](tasks.md) **§S10** (`R1`-`R14`) + **`OPS1`-`OPS6`**. Từ lần e2e gần nhất (2026-07-18, build `fa2ae23`), `main` đã nhận **12 merge** (PR #84→#95: DB20–DB31, F1–F23, endpoint invitations, paging 7 endpoint) — **toàn bộ chưa từng chạm deploy**. Chia agent: A1 Auth+Admin · A2 Payment · A3 B2C · A4 B2B · A5 Public API/bảo mật · A6 hạ tầng/toàn vẹn; supervisor giữ **UI Chrome thật** + **PayOS tiền thật**, và **tự verify lại MỌI 🔴** thay vì nhận báo cáo.
> **Bước 0 (trước khi test) đã sửa 3 thứ deploy:** ①**5/5 container prod chạy `ASPNETCORE_ENVIRONMENT=Development`** — **đúng bug 18/07 tưởng đã sửa, tức quay lại LẦN 2** → đổi `Production` + `up -d`, verify **0/8** endpoint ép lỗi rò stack trace (→ `OPS6` để sửa tận gốc trong compose version-control); ②**`USAGE_SINK_BASE` VÀ `PROMPT_REGISTRY_BASE` đều KHÔNG có trên `aiapi`/`aiworker`** dù env-file (viết 19/07 kèm comment F22/F21) đã khai sẵn ⇒ **container được dựng KHÔNG dùng env-file** ⇒ **F22 lẫn F21 đều tắt câm** — bật lại thì F22 từ **0 dòng → có dữ liệu trong vài phút** (chuỗi AIService→callback Payment→dashboard thông suốt, verify qua UI admin); ③tắt 2 job purge trong cửa sổ test (→ `OPS5`).
> 🔴 **R1 — 3 buổi phỏng vấn không thu được tiền, KHÔNG cơ chế nào cứu.** `ee655e32` Abandoned (user mất oan 1 credit, không release) · `ad20dae0` Scored **B2B** (org được buổi **miễn phí**) · `62814a3a` Scored **B2C** (user được buổi **miễn phí**). Nguồn gốc là **topology broker**: binding `interview.events→payment.credit` vắng trong một cửa sổ 19/07 — bằng chứng quyết định là **cùng một event**, `campaign_rankings` cập nhật sau **46ms** còn credit **không bao giờ tới**; topic exchange **vứt im lặng** message không route được, mà outbox đã đóng dấu `published_at` nên phía Interview **tưởng đã giao**. **Nhưng lỗ cần vá nằm ở code:** `OrphanReservationReconciler` chỉ cứu ca *session không tồn tại* ⇒ ca *session tồn tại nhưng đã terminal* **rò vĩnh viễn** — đúng khoảng trống mà `DB4b` tưởng đã khép. Đường này **đã chạy đúng trở lại** sau deploy (verify live: Scored `17:40:52` → published `.684` → `Consumed .727` → ledger `.750`).
> 🔴 **R2 — ~60 request ẩN DANH khoá được ATS của MỌI org.** Rate limit `/campaign/public/*` chạy **TRƯỚC** `UseAuthentication()` ⇒ chưa có key id để partition ⇒ rơi về **một xô dùng chung**, ngược hẳn thiết kế F17. Supervisor đo độc lập: 70 request **không credential nào** → **59×401 rồi 11×429**, ngay sau đó ẩn danh 429 liên tục, đường JWT vẫn **200**. Vế "request không có credential vẫn tiêu ngân sách" là bằng chứng cấu trúc: không thể per-key được.
> ⚠️ **SUPERVISOR TỰ SAI VÀ TỰ SỬA — bài học đắt nhất vòng này.** Tôi kết luận "image build `15:33–15:36Z`, **sau** commit tip `15:28:35Z` ⇒ đã gồm PR #95" — **suy từ dấu thời gian chứ KHÔNG đo**, đúng cái lỗi tôi vừa bắt agent rà soát mắc phải ở đầu phiên. A4 bác lại bằng **SQL do chính service phát ra**: `INSERT INTO campaign_membership (...)` **thiếu cột `invitation_id`** ⇒ model EF trong image không biết property, dù code `main` (`ParticipationService.cs:303`) có gán và migration đã apply. DB **0/23** row có giá trị. Campaign build lúc `15:34:40` — **muộn hơn** Interview `15:34:19` vốn *có* PR #95 ⇒ **không phải mốc cắt theo thời gian mà là CI checkout nhầm commit**. ⇒ **`OPS1`** rebuild + đề xuất thêm label `org.opencontainers.image.revision` để lần sau đối chiếu được. Hệ quả kéo theo: lời mời phát lại báo `Joined` **dương tính giả** (rơi về nhánh so email), HR tưởng ứng viên đã vào.
> 🔎 **Ba lần "phương pháp đo sai suýt cho kết luận sai":** ①grep chuỗi trên DLL trả **0 cho `api_keys`** trong khi A5 đã verify F17 chạy sống ⇒ **âm tính giả** (chuỗi .NET lưu UTF-16) — bỏ, chuyển sang đọc SQL thật; ②agent rà soát đầu phiên khẳng định "FE master chỉ có P0, 23 commit P1 chưa merge" — **SAI**, nó đọc ref `master` local **chưa `fetch`**; verify từng SHA thì cả 9 commit P1 **đều có trong `origin/master`**; ③cùng agent đó khẳng định "hàng loạt migration chưa apply" và "queue `payment.credit` `arguments=null` sẽ ném 406" — **cả hai SAI**, là câu chữ cũ trong chính `progress.md`; đo thật thì 4 DB đã ở head và DLX đã có sẵn. **Quy ước rút ra: không được trích `progress.md` làm bằng chứng về trạng thái hệ thống.**
> 🟠 **R3 chi phí AI thấp hơn thực ~3,3 lần** — supervisor đo lại: `total 76.793` nhưng `prompt+output = 38.060` ⇒ **bỏ sót 38.733 = 50,4%** (thinking token Gemini 2.5, Google tính giá **như output**); `cost_usd` khớp **tuyệt đối** công thức thiếu ⇒ không phải sai số làm tròn. Đơn giá trong `appsettings.json` **vẫn chưa ai đối chiếu bảng giá Google**.
> 🟠 **R4 doanh thu trừ khoản hoàn HAI LẦN** — supervisor đo: thực thu **3 đơn/6.000₫**, hoàn 2.000₫, net đúng **4.000₫**, API trả **2.000₫**. Gross lọc `status='Paid'` mà hoàn lật `Paid→Refunded` ⇒ đơn rơi khỏi gross, rồi `net = gross − refunded` trừ lần nữa — phá đúng mục tiêu mà comment `RevenueService.cs:33-36` tuyên bố.
> 🟠 **R5 F21 không gọi được qua gateway** (thiếu route `interview/admin/**`; Interview chỉ có `practice/**`+`files/**` trong khi 3 service kia đều catch-all) · **R6 webhook PayOS lọt qua gateway** (400 thay vì 404, vi phạm GEN-1; HMAC vẫn chặn **8/8** payload giả, ledger 48→48) · **R7 cờ gian lận của ứng viên BỎ NGANG không bao giờ tới HR** (`session_flags` chỉ lộ qua bảng results vốn chỉ liệt kê `Scored`; `SessionFlagController` không có `HttpGet` nào) ⇒ **đúng nhóm hành vi đáng ngờ nhất lại là nhóm bị giấu**.
> ✅ **Đáng tiền nhất trong 4.000₫: hai đường tiền chưa ai chạm tới nay đã có bằng chứng thật.** ①**F7 qua đường webhook** — ví tạo bởi webhook PayOS (user chưa thi buổi nào) **vẫn nhận đủ 3 credit**: đơn `Paid 17:33:20.744646` → `FreeGrant +3` lúc `.746810` → `Purchase +5` lúc `.751778`, bất biến `8+0=Σdelta 8` ⇒ **đúng ca mà vòng phản biện lúc thiết kế F7 đã bắt được** ("chỉ cấp ở `ReserveAsync` là bỏ rơi người mua gói trước khi thi"). ②**F8 ca "đúc credit từ hư không" PASS** — `subscriptions` từ **0 dòng** (chưa từng chạy lần nào trên production, 71/71 reservation đều `funded_by='Credit'`) → mua thật → reservation mới mang `funded_by='Subscription'`, **không** nâng `reserved_credits`, release **thoát sớm trước nhánh prepaid**, ví đứng yên `3+4=7=Σdelta`. Quan trọng hơn: nhánh chọn theo **`FundedBy` ghi cứng lúc reserve**, không hỏi "hiện còn thuê bao không" ⇒ **kịch bản thuê bao hết hạn giữa buổi cũng không lách được**.
> ✅ **Khác đã verify:** F10 `source` **do SERVER sở hữu** (client khai `AiGenerated` cho câu HR → DB vẫn `CustomHr`, đúng ở **cả POST lẫn PUT**, cả hai chiều) · **F16 PDF tiếng Việt ĐỦ DẤU trên image deploy thật ⇒ BK8 chính thức bị lật**, PDF↔CSV cùng `InvariantCulture` (`91.5`, không có `91,5`) · F18 trần thu hồi **chừa đúng 3 credit quà** + idempotent (2 request đồng thời → 1 dòng Refund) · **DB24 KHÔNG có lỗ chiếm tài khoản** (OTP thật sự được so khớp; 5 lần đoán sai đốt OTP) · **F20 ban bịt được lối vòng `provision-candidate`** (403, không chỉ chặn login) · **F11 63 answer: 61 NULL, 0 dòng nào bằng 0** (đúng ý đồ "khuyết phải là null, không bịa số 0 rồi bảo LLM tin nó nhất") · F17 hai vế âm đều kín (Bearer không mở `/public/*`, API key không mở `/api-keys`) + `flags[]` **không lộ ra ATS** (D13) · F9/CAMP-2/CAMP-1/CAMP-3 · DB23 hash token verify bằng chính SHA-256 · D4 reissue → 410 · ranking `ORDER BY COALESCE(...,-1)` ⇒ ứng viên chưa chấm **xuống đáy** (bẫy NULL-đầu của Postgres đã bịt) · **trần adaptive nay chặn ở Campaign (400) trước khi reserve credit** ⇒ lỗ cross-service của F2b vòng trước đã kín.
> ⚠ **`WHISPER_MODEL=large-v3` — user cố ý giữ, nay đã có SỐ ĐO thay vì phỏng đoán** (`OPS4`): 3 lượt upload adaptive **70,6s · 64s · 96s**, lượt 96s **vượt timeout decider 90s** → degrade về luồng tĩnh ⇒ **~1/3 lượt mất hội thoại thích ứng**; 1 worker thành nút thắt (answer nằm `Scoring` ~9'). Degrade xử lý đúng (answer vẫn lưu, không mất bài). Env-file chủ đích ghi `small` ⇒ đây là **trôi cấu hình do container không dựng từ env-file**, không phải quyết định.
> ⚠ **Chưa verify được, KHÔNG nâng thành kết luận:** Google login khi bị ban (OAuth redirect, không phải endpoint JSON) · ban Admin cuối → 409 (**cố ý không thử**: DB chỉ có 1 Admin và đang dùng chung với agent khác) · ví `Suspended` nhận quà (**không endpoint nào set `Suspended`**) · CAS của `CreditReservationReconciler` (cần bơm drift bằng SQL; chỉ chứng minh được "không bơm sai", **chưa** chứng minh "sửa đúng") · định tuyến poison message ra DLQ (DLQ rỗng suốt) · key API hết hạn (không tạo được key quá khứ qua API) · gating `fullName` của F17 (**`campaign_membership.full_name` NULL toàn bảng** ⇒ null ở **cả hai** loại key = dữ liệu nguồn rỗng, không phải bằng chứng có gating; chỉ `email` mới chứng minh được) · SMTP tới hộp thư thật · **trang `/candidate/*` treo với công cụ đo** (admin/employer render mượt trong cùng phiên; API 0,5–0,9s nên không phải tải; **nghi** hộp thoại xin quyền camera của F4 chặn tab — chưa xác nhận được vì Screen Recording chưa cấp).

> **Cập nhật 2026-07-20 (e2e email lời mời B2B + anti-cheat trên deploy thật — 1 lỗi THẬT, branch `fix/invitation-magic-link`):** chạy e2e đường ứng viên end-to-end trên server + Vercel, bằng hộp thư thật. **Gate:** `dotnet build` 0 error · **Campaign 418 xanh** · **0 migration**. Commit `6507b2e` (vá) + doc.
> 🔴 **Lỗi thật, sống nhiều ngày mà không ai báo: magic-link trong email chưa bao giờ bấm được.** Consumer dựng `{Invitation:BaseUrl}/invitations/{token}`, mà `Invitation__BaseUrl` trên server = `${GATEWAY_PUBLIC_URL}`. Sai **hai lớp độc lập**: ①base sai loại — gateway chỉ phục vụ `/api/v1/...`; ②path sai — route FE là **`invite/:token`**, còn `invitations/:token` là route **API** trả JSON. Tra `git log -S`: route FE **chưa bao giờ** tên `invitations`. Verify bằng token thật: link email → **404 body rỗng**; `{FE}/invite/{token}` → trang mời đầy đủ (tên chiến dịch, 2 tiêu chí, nút "Tham gia phỏng vấn").
> 🔑 **Vì sao lỗi này sống lâu — đáng nhớ hơn bản thân lỗi:** 404 **body rỗng** ⇒ trình duyệt hiện **trang trắng, không banner lỗi nào**, thanh địa chỉ vẫn nguyên link ⇒ **nhìn y như đang tải**. User bấm link và báo "vô được mà" — báo cáo đó **đúng về hành vi quan sát** (trình duyệt có mở) nhưng ngược với sự thật (không có gì trên trang). ⇒ **khi user nói "vào được", hỏi tiếp *nhìn thấy chữ gì* trước khi tin.** Đã lưu memory [[e2e-http-status-traps-isas]].
> ⚠ **Hai lần suýt kết luận sai trong CÙNG vòng, cả hai đều do đọc HTTP status thay vì đọc nội dung:** ①tôi báo "tunnel đã chết" vì `/health` → 404 — **SAI**, gateway đơn giản không phục vụ path đó; `GET /api/v1/payment/package` trả JSON bình thường. ②tôi tính dùng `{FE}/invitations/{token}` → 200 làm bằng chứng "chỉ cần sửa base là xong" — **SAI**, Vercel **SPA-fallback trả 200 cho MỌI path**, kể cả path bịa. Cũng vì thế `/api/v1/...` gọi vào origin Vercel trả **HTML chứ không phải JSON** ⇒ Vercel KHÔNG proxy sang gateway.
> 🔬 **Test cũ đang KHOÁ ĐÚNG HÀNH VI SAI.** `ProcessMessage_FallbackGatewayUrl_KhiThieuInvitationBaseUrl` khẳng định "thiếu `Invitation:BaseUrl` thì lấy `Gateway:Url`" là **đúng** — chính fallback đó khiến cấu hình *trông như đã đặt* mà link vẫn chết. Nên vá **gỡ hẳn fallback** chứ không chỉ sửa path, và test được **đảo tiền đề** (nay khẳng định host gateway KHÔNG được xuất hiện trong bất kỳ link nào), ghi lý do tại chỗ. Mutation: trả path về `/invitations/` → **3 ĐỎ**; trả lại fallback → **1 ĐỎ**; restore → 418 xanh (mỗi lần restore đều `touch` ép mtime — bài học harness vòng `fix/db-review-round`).
> ✅ **Phần còn lại của luồng email THÔNG, có số đo:** tạo lời mời `12:17:13` → outbox publish `12:17:21` (**8s**) → **SMTP gửi thật `12:17:26`** (13s, `attempts=0`, không nack-loop) → join (magic-link→JWT, provision Candidate theo email) → start (reserve org credit) → session. Toàn hệ **50/50 outbox published**, 50/71 invitation có `email_sent_at`, gồm 2 hộp thư thật ngày 18/07 ⇒ **SMTP không phải chỗ hỏng** (khác ghi chú cũ "chưa kiểm chứng tới hộp thư thật").
> ✅ **Anti-cheat: phần đã xây chạy đúng, verify LIVE 8 cờ / 7 loại.** Cờ hành vi (`tab_switch`×2·`paste`·`focus_lost`·`camera_blocked`) ghi thật; signal rác → 400; **candidate tự gửi `face_mismatch` → 400** (chặn giả mạo cờ AI); ẩn danh → 401. **Face-verify InsightFace phân biệt đúng người thật:** cùng người → `match=true`; **người khác → `face_mismatch`**; ảnh nhóm → **`multiple_faces` (`faceCount=3`)**; phong cảnh → `no_face`; đối chứng quay lại `match=true` (không kẹt trạng thái). `reference_image_key` lưu **key S3** không phải URL (đúng GEN-5). HR đọc được cờ qua JSON `flags[]` + CSV — **với ứng viên đã `Scored`**.
> ⚠ **Ảnh nhóm 5 người nhưng chỉ bắt được 3 mặt** (2 người quay đi/che khuất) ⇒ `multiple_faces` nghĩa là "phát hiện ≥2 mặt", **KHÔNG phải đếm đủ người trong phòng**. Đừng hứa với hội đồng là hệ thống đếm được số người.
> 🔴 **`R7` tái hiện với số đo mới:** session vừa test có **8 cờ trong DB** mà `GET /results` trả `totalCandidates: 0, results: []`, CSV chỉ có header. Toàn hệ **6 session có cờ, chỉ 4 đã chấm**. Nặng hơn vẻ ngoài: ứng viên **bỏ ngang** không bao giờ `Scored` (PAY-13) — mà bỏ ngang chính là hành vi đáng ngờ nhất ⇒ **cờ của ca đáng ngờ nhất là cờ HR không bao giờ thấy**.
> 📌 **Sinh 3 task mới, CHƯA làm** (user chốt "thêm task nhưng đừng làm"): **`BK24`** bật Postpaid cho Org — hiện **không có đường hợp lệ nào** (`payment_mode` chỉ được ghi 1 chỗ hard-code `Prepaid`; `credit_limit` **không có chỗ ghi nào**; endpoint vẫn 🔜) ⇒ chỉ có `UPDATE` SQL tay, tức bỏ qua bước PlatformAdmin duyệt của PAY-3. ⚠⚠ **Lật `payment_mode` lúc này sẽ LÀM BỐC HƠI 7 CREDIT THẬT**: ví org `0610da24` đang `remaining=0, reserved=7`, các chỗ giữ đó tạo hồi Prepaid nên **đã trừ `remaining`**, mà `ReleaseAsync` đọc mode **tại thời điểm release** (không snapshot như `funded_by` của F8) ⇒ release dưới Postpaid chỉ `reserved−1`, không hoàn ⇒ gãy bất biến `remaining+reserved=Σdelta` khi campaign hết hạn 22–23/07. Kèm phát hiện: **không dòng code nào set `InvoiceStatus.Overdue`** ⇒ guard BK17 không bao giờ kích hoạt ⇒ **postpaid hiện là "trả sau" không có phanh**. · **`BK25`** ảnh khuôn mặt S3 không có retention (30s/ảnh suốt buổi thi, **không cột DB nào trỏ tới** ⇒ không có gì để join khi muốn dọn) — vi phạm DATA-3. · **`OPS7`** đổi env `Invitation__BaseUrl` sang origin FE + redeploy campaignservice; **code không tự đủ**, thiếu vế nào link cũng vẫn hỏng.
> ⚠ **Org credit = 0** sau vòng này (buổi thi tiêu nốt credit cuối) ⇒ `start` B2B tiếp theo sẽ **402** cho tới khi nạp thêm.

> **Cập nhật 2026-07-23 (Scalar không khớp thực tế → sửa gateway OpenAPI + re-test toàn hệ qua gateway, 5 agent):** user báo Scalar (`/scalar/`) không giống API thật. Đo: **119/144 op sai đường dẫn** (chỉ Auth đúng). Gốc: `ApiServices` khai bằng **mảng-theo-index** — compose override 5 URL (chèn AI index 1) trong khi appsettings có 4 entry ⇒ mỗi service ghép nhầm `Prefix` của service trước, Payment (index 4, không entry) đổ path ra root. **Sửa 2 tầng:** ①env compose server (xoá dòng AI, đánh số lại) → Scalar khớp liền, live ~30s; ②**PR #100 merged main** (`ab804cc`): `ApiServices` → **dictionary keyed-by-name** (env `ApiServices__<tên>__…`, không ghép lệch được nữa) + lọc `/internal/*` khỏi doc (GEN-1) + guard bỏ-service-không-Prefix + log ánh xạ + route `interview-admin-route` (**đóng `R5`**) + `scripts/verify-gateway-openapi.py`. **Re-test 5 agent qua gateway (~150 op, đã đúng prefix):** Auth/Interview/Campaign **0 bug logic** (chỉ doc-gap: OpenAPI auto-gen chỉ khai `200`, thiếu mã lỗi; `jobCategory` khai `integer` mà server dùng string); business rule + auth gate + shape 200 khớp `$ref` PASS toàn bộ. **Sinh 2 PR fix — ✅ CẢ HAI ĐÃ MERGE `main`** (đối chiếu `git merge-base --is-ancestor` 2026-07-23; trạng thái §S10 đã cập nhật theo):
> - **`fix/payment-revenue-and-package-validation`** (`2d1a7b1`+`c0377e9`): **đóng `R4`** (revenue trừ đôi khoản hoàn — gross đổi `Status==Paid` → `(Paid‖Refunded) && PaidAt∈[from,to)`, đơn hoàn giữ paid_at nên vẫn thuộc gross kỳ đã thu, hoàn trừ 1 lần) + package validation (name rỗng/giá âm → 400, cả Create lẫn Update). **330/330 test** (2 test F19 đảo tiền đề + doc lý do tại chỗ vì assert cũ khoá bug trừ-đôi; +4 PackageValidation). **Mutation supervisor tự chạy lại:** revert predicate → 2 ĐỎ, restore → xanh. 0 migration.
> - **`fix/gateway-double-slash-internal`** (`5a0922c`): **lỗ HIGH MỚI** — `POST /api/v1/payment//internal/credits/reserve` (2 dấu `/`) lách khối chặn GEN-1 (chỉ khớp 1 `/`) → tới controller Payment (401 thay 404); chỉ Payment dính (route strip-về-root). Vá: middleware gộp `//`→`/` **trước `UseRouting()` gọi tường minh** (để framework tự chèn UseRouting đầu pipeline thì endpoint match path chưa gộp → vẫn 401, đã đo). Verify gateway thật: `//internal`/`///internal`→404, route thường→200.
> - **Đã tracked sẵn trong §S10, re-test chỉ re-confirm live:** `R2` (rate-limit `/public/*` xô chung — CampaignService, chưa sửa) · `R6` (webhook qua gateway, cố ý chưa chặn — cùng gốc double-slash). Chi tiết bug tiền/bảo mật đã supervisor-verify bằng đọc code.
> **Cập nhật 2026-07-23 (`R7` + gate `/face-verify` — PR #105 merged `main`):** **`R7` đóng** (`967a134`) — cờ chống gian lận của ứng viên **chưa `Scored`** nay tới được HR. ⚠ **làm theo hướng KHÁC đề xuất trong ô task**: KHÔNG thêm `HttpGet` vào `SessionFlagController` (grep controller vẫn ra rỗng — **đừng đọc đó rồi kết luận chưa sửa**) mà thêm field **additive `UnscoredFlagged`** vào `CampaignResultsResponse`: session có cờ nhưng chưa có row `campaign_rankings`, kèm danh tính F5 + cờ gom, sort nhiều-cờ-trước; CSV/PDF nối các dòng đó (rank/điểm/scored_at trống, `result="Chưa chấm"`) **cùng nguồn `results`** nên hai bản xuất không lệch (F16). Additive ⇒ FE/CSV cũ không vỡ, bảng ranking giữ nguyên. 4 test; mutation bỏ gán → 4 ĐỎ; 0 migration. Kèm **GEN-7**: gate `X-Internal-Token` cho `/face-verify` (`adf8c5c`). **Trạng thái §S10 sau vòng đối chiếu này:** ✅ done `R4`·`R5`·`R7`·`R15` · ⏳ còn `R1`·`R2`·`R3`·`R6`·`R8`+ (R6 **không** được double-slash fix đóng hộ — gateway hiện vẫn không có khối chặn `webhook/**`).
> **Cập nhật 2026-07-23 (BK24 Postpaid cho Org — test branch → e2e Postgres thật → apply migration prod → verify deploy → bật InvoiceOverdue):** `fix/postpaid` (BK24: admin duyệt Postpaid + snapshot `payment_mode` + `InvoiceOverdueReconciler` + phanh BK17) **đã merged `main` PR #103** (tip `b7501a5`, branch đã xoá trên origin). **Test branch:** `dotnet build` 0 error · Payment **342 test** pass · `has-pending`=No changes. **E2E trên Postgres 18 THẬT** (DB throwaway local, apply migration đầy đủ, drive qua HTTP + verify DB từng bước): approval (`POST /admin/credits/payment-mode` ghi `payment_mode_changed_by/at/note`, CAS-guard) · accrual postpaid (reserved↑, `remaining` giữ 0) · chạm hạn mức→**402 no-orphan** · consume→`period_usage`↑ · chốt kỳ→invoice Issued (`due_at`=periodEnd+15d, count/amount đúng, reset usage 0) · **`InvoiceOverdueReconciler` đóng dấu Issued→Overdue THẬT** (job nền) · **phanh BK17** reserve khi org có hóa đơn Overdue→402 · **snapshot chống bốc hơi credit** (reserve dưới Prepaid → admin flip Postpaid → release đọc snapshot `Prepaid` → hoàn `remaining`, bất biến `remaining+reserved=Σdelta=5` GIỮ; code cũ đọc mode HIỆN TẠI của ví sẽ mất 1 credit) · guard `StrandedCredits`(opt-in)/`UnpaidDebt`/`NotOrg`/`InvalidCreditLimit`/`NotPostpaid`/`WalletMissing` **7/7 đúng**. **E2E webhook settlement** (ký HMAC bằng chính SDK `CryptoProvider.CreateSignatureFromObject` → khớp `VerifyAsync`): `InvoiceSettlement`→order Paid + invoice Paid **0 dòng credit** (tất toán nợ KHÔNG cộng credit) · replay→`AlreadyProcessed` (idempotent PAY-8) · chữ ký bị sửa→400 · CreditPack đối chứng→+5 credit + `FreeGrant +3` (F7 qua webhook). **✅ Apply migration `AddPostpaidApprovalF23` lên prod `postgres-main/isas_payment`** (idempotent script + `ON_ERROR_STOP=1`): preflight all-green (0 `credit_limit≤0`, 13 ví Prepaid, 8 đối tượng chưa tồn tại, F23 chưa trong history — ⚠ F23 timestamp `091735` < `AddRefundSettledAt` `180847` đã applied TRƯỚC, nhưng idempotent guard-by-id xử đúng, chỉ F23 chạy); verify sau apply: head có F23, đủ **8 cột/index/CHECK**, **93 reservation backfill `payment_mode='Prepaid'`**, 0 nhiễm dữ liệu (reservations 34C/52R/7Reserved giữ nguyên). **✅ Code mới ĐÃ deploy** — probe route `admin/credits/payment-mode`→**401** (route tồn tại, không 404) = image sau PR#103 đang chạy (verify bằng HÀNH VI, không chỉ dấu thời gian — bài học OPS1). **✅ Bật InvoiceOverdue** trên `paymentservice-main` (backup compose `.bak-invoiceoverdue-*` → thêm env `InvoiceOverdue__Enabled=true`/`GraceHours=24`/`ScanIntervalSeconds=600` → `docker compose config` OK → `up -d`, health 200). **Findings (không chặn, chưa xử):** 🟠 chốt kỳ khi `period_usage=0` tạo **hóa đơn 0đ** (thiếu guard `count>0`, có sẵn từ P8b — đáng thêm skip) · ⚪ **2 reservation R1 rò** (`ad20dae0` B2B · `62814a3a` B2C) `Scored`-mà-`Reserved` từ 19/07 — `OrphanReservationReconciler` bản R1 **bắt được** (gọi Interview `/internal/sessions/exists`→200) nhưng cố ý SKIP (tạo trước mốc `ConsumeFromUtc`=lúc khởi động, chống trừ tiền hồi tố) → log "cần đối soát tay"; **user chốt kệ (development)** · ⚪ `libgssapi_krb5.so.2` trong log = noise VÔ HẠI (image aspnet không kèm Kerberos → Npgsql thử GSS lúc connect rồi fallback password; DB 51 lệnh OK, HTTP 200, 0 lần trong 5' gần nhất) · ⚠ `paymentservice-main` vẫn `ASPNETCORE_ENVIRONMENT=Development` (**OPS6**, chưa sửa).

> **Cập nhật 2026-07-24 (`R2` merged + đính chính `R1` — đối chiếu §S10 với code `main`):** user merge branch `fix/postpaid` → **PR #108 merged `main`** (tip `58f4b01`). Rà lại toàn bộ §S10 bằng **đọc code `main`, KHÔNG đọc ô Status** (bài học R7: ô task lệch code) → chỉ **R1** và **R2** cần lật trạng thái; R4/R5/R7/R15 đã done sẵn; R3/R6/R8–R14 thật sự chưa làm. **`R2` ✅ done** (PR #108): rate-limit `/campaign/public/*` — gốc bệnh là scheme mặc định JwtBearer nên `UseAuthentication()` không chạy handler `ApiKey` ⇒ claim `api_key_id` chưa có lúc limiter chạy → mọi request rơi xô chung; vá bằng middleware xác thực scheme `ApiKey` tường minh cho path `/campaign/public` **trước** `UseRateLimiter()` (dùng lại cache `HandleAuthenticateOnceAsync`, không tra DB thêm) + xô `anonymous` riêng chặt hơn (default 10) + `Retry-After`/`X-RateLimit-Remaining`. **Verify độc lập (worktree throwaway):** `dotnet build` 0 error · **1402 test** (Auth 179 · Interview 446 · Campaign 435 · Payment 342) · **mutation 3/3 ĐỎ** (gỡ pre-auth→3 ĐỎ gồm `KeyABurst_DoesNotLockOutKeyB`; xô ẩn danh dùng chung trần→4 ĐỎ; gỡ `OnRejected`→1 ĐỎ; mỗi restore `touch` ép mtime + so `shasum` trước khi đọc kết quả) · **6 probe tác dụng phụ tự viết PASS** (Bearer không mở /public · burst JWT không khoá key thật · endpoint khác không bị limit · path trùng tiền tố chuỗi không bị nuốt · 2 xô tách nhau cả 2 chiều · key rác đúng tiền tố rơi 1 xô chung) · **0 migration**, không đụng Payment/Interview/Gateway. ⚠ **3 tác dụng phụ đã đo (nêu khi review):** ①pre-auth chạy TRƯỚC limiter ⇒ key **đúng tiền tố** tốn 1 SHA-256+1 query kể cả khi sẽ 429 (đảo thứ tự lá chắn DB; key sai tiền tố chặn sớm, 0 query) · ②request mang JWT hợp lệ tới `/public` rơi xô ẩn danh (vẫn 401, không phải lỗ) · ③`OnRejected` đăng ký TOÀN CỤC không theo policy. ⚠ **2 nit chưa sửa:** `PublicApiRateLimitFactory.SeededRawKey` không bao giờ được gán ⇒ test `Rejected429_...` chạy nhầm nhánh ẩn danh (vẫn PASS, sai nhánh); comment `TODO XÁC NHẬN VỚI BẠN` rác còn sót. ⚠ tên branch `fix/postpaid` là **tên cũ còn sót** (BK24 đã merge PR #103) — dễ làm người review tưởng postpaid. **`R1` ✅ done — ĐÍNH CHÍNH doc lệch code:** ô Status cũ ghi `not_started` nhưng `fd310ef` *"fix(R1): dọn chỗ giữ credit của session ĐÃ TERMINAL"* đã ở `main` từ trước — `OrphanReservationReconciler` có đủ nhánh terminal (`Scored`→consume whitelist một-phần-tử **không** `default→Consume` · `SessionAbandoned`/`Failed`→release · guard `ConsumeFromUtc`+`ConsumeTerminalScored` chống trừ hồi tố), Interview `/internal/sessions/exists` trả thêm `states`; **`OPS2` nay mở khoá** (3 reservation kẹt tạo trước mốc → SKIP, đối soát tay). **Trạng thái §S10 sau vòng này:** ✅ done `R1`·`R2`·`R4`·`R5`·`R7`·`R15` · ⏳ còn `R3`·`R6`·`R8`·`R9`(partial `c0377e9`, còn ca `OneTime credits=0`)·`R10`·`R11`·`R12`·`R13`·`R14` (R6 gateway vẫn KHÔNG có khối chặn `webhook/**`).

## Pha hiện tại
**Feature engine cho cả 2 dòng đã đủ + ĐÃ MERGE `main` + DEPLOY LIVE — pha còn lại = hardening + scale.** Thiết kế chốt **D1–D23**. **23 vòng giám sát** (multi-agent supervised) đã **merged vào `main`** (PR #22–#43, tip `e18c212`; nhánh `docs/sync-design-d18-d21` đã hoàn thành sứ mệnh, `dotnet build` 0 error, test xanh) — xem §Vòng tables bên dưới cho từng vòng. **✅ Deploy live** trên server (tunnel gateway + 5 .NET + Mac AIService); **e2e B2C+B2B (UI Angular Vercel + API) + luồng tiền PayOS→webhook→credit verify LIVE** 2026-07-15/16/17.

> **✅ Main = full system (cập nhật 2026-07-17):** toàn bộ 23 vòng + mọi task (A*/C*/E*/P*/D*/BC*/BK*) đã **merged vào `main`** (PR #22–#43, tip `e18c212`) và **deploy live** trên server + e2e B2C+B2B verified. Nhánh `docs/sync-design-d18-d21` đã hoàn thành sứ mệnh — **không còn là nguồn trạng thái**. *(Các §Vòng tables bên dưới giữ nguyên làm **log lịch sử**; những câu "CHƯA merge/CHƯA push/chờ stack" trong đó mô tả **thời điểm ghi từng vòng**, không phản ánh hiện tại.)*

**Đã có (nhánh tích hợp):** B2C luyện+lịch sử+ví credit+tổng kết điểm/nhận xét+phân tích CV+roadmap ôn tập (BC*); B2B org nhiều thành viên (A1–A3/A6)+campaign/tiêu chí (C*)+lọc CV (C13–15)+membership join→start (D1–D4)+ranking/result/export (E4–6)+chất lượng chấm neo-mức/self-consistency/nhận-xét (E9–11); thanh toán credit/PayOS (P1–P8, prepaid+postpaid); **auth gate `[Authorize(Roles)]` mọi service (A5 ✅ v22)**. **Còn lại:** Phase 0 (compose máy sạch/make), verify tay (PayOS sandbox · e2e B2B+B2C compose/broker/Gemini · A5 live 401/403), backlog follow-up (§Backlog tasks.md).

## Trạng thái branch / task (so với `main`)
| Mục | Nội dung | Trạng thái |
|---|---|---|
| `main` | engine B2C + Auth(+Org) + Campaign + Gateway; Interview thêm I1 | ✅ chạy |
| Auth org (A1–A3) | `organizations`/`org_members` (migration `AddOrganizations`) + JWT mang `org_id`/`org_role` + `POST /auth/register-org` | ✅ **merged main (PR #23)** · test ✅ · ⚠ e2e HTTP chờ stack chạy |
| Campaign (C1–C10) | 6 bug + soft-delete (C9) + lifecycle (C7) + snake_case + publish/`campaign_criteria` Σ=1 (C8) + `audit_logs` (C10) + AI `/suggest-criteria` (Gemini + fallback) + Dockerfile/CI/gateway route | ✅ **merged main (PR #22)** · 34 unit test ✅ · thiếu M3/M4/M5 (distribution/ranking/export), `org_id` (code còn `employer_id`) |
| I1 | session B2B nhận `campaign_id` + materialize tiêu chí → `rubric_criteria(campaign_id)` (idempotent theo campaign) | ✅ **merged main (PR #24)** · test ✅ (2/2) · ⚠ HTTP entry B2B chờ **D2** |
| E1 | chấm B2B theo tiêu chí campaign (chọn tiêu chí branch theo `campaign_id` ở `AnswerService` + `StuckAnswerRepublisher`) | ✅ **merged main (commit 796d8bb)** · `dotnet test` 31/31 ✅ · ⚠ e2e đầy đủ chờ D2 |
| `features/payment-b2c` | PaymentService (Order/Package/PayOS theo `user_id`) — **chưa có folder trong tree `main`** | 🟡 cần refactor: `credit_accounts(owner_type)`, reserve/consume/release, postpaid + hóa đơn, active-polling |

### Vòng giám sát 2026-07-11 — 4 task passing (branch tích hợp `docs/sync-design-d18-d21`)
| Task | Nội dung | Trạng thái |
|---|---|---|
| **E2** | phát `SessionScored{SessionId,CampaignId?,CandidateId,TotalScore,ScoredAt}` từ `SessionScoringNotifier` (2 chỗ đóng `Scored`), best-effort | ✅ **passing (e2eaad4)** · Interview test 33/33 · ⚠ 2 điểm thiết kế chờ team chốt (tên exchange `interview.events`/`session.scored`; công thức `TotalScore` áp chung B2B/B2C) |
| **D1** | `campaign_invitations` + magic-link + `POST /campaign/{id}/invitations` (dedup/cap/`failed[]`) + `InvitationEmailPublisher` (RabbitMQ) | ✅ **passing (82c9faa)** · Campaign test 14/14 · `campaign_candidate_id` nullable không FK (chờ C13) · chưa wire `org_id` · ⚠ e2e email chờ stack |
| **BC6** | AIService `POST /analyze-cv` sync (1 Gemini call, AI không ghi DB) + anti prompt-injection | ✅ **passing (9cfa622)** · pytest 11/11 · queue B2B (C14) chưa build |
| **P1** | PaymentService vào tree + `credit_accounts(owner_type)`/`credit_reservations`/`credit_transactions` + migration `InitPaymentDb` (không apply Neon) | ✅ **passing (dfd57ce)** · Payment test 4/4 (P0.4 xong) · bỏ `CreditService`/`PayOSService` cũ (D15 retire `UserCredit`) → reimplement P2/P5 · chưa vào CI/compose |

> Base test sau tích hợp: **Auth 4 · Campaign 14 · Interview 33 · Payment 4 = 55 pass**, `dotnet build` 0 error.

### Vòng giám sát 2026-07-11 (vòng 2) — 4 task passing
| Task | Nội dung | Trạng thái |
|---|---|---|
| **P7** | `OrderCodeGenerator` time+random (`yyMMddHHmmss`+4 random), guard ≤2^53−1, retry ≤10 khi đụng UNIQUE (index sẵn từ P1) | ✅ **passing (7ffe1c9)** · Payment 7/7 · không migration mới · chưa wire `OrderService` (P2) |
| **E3** | `SessionAbandoned` + `SessionAbandonSweeper` (2 phút) phát key `session.abandoned` trên `interview.events` | ✅ **passing (50cab51)** · Interview 38/38 · ⚠ deadline placeholder `CreatedAt+30min` (`campaigns.expires_at` không đọc xuyên service) → I2 materialize deadline lên session; sweeper là plumbing forward |
| **E4** | `SessionScoredConsumer` (queue `campaign.ranking`) → upsert `campaign_rankings` idempotent theo `session_id`; B2C no-op | ✅ **passing (19fb040)** · Campaign 17/17 · migration `AddCampaignRankings` (không apply Neon) · re-anchor v2 sau khi base có D1 |
| **BC13** | AIService 3 endpoint roadmap sync (`/generate-roadmap`·`/generate-lesson-theory`·`/summarize-roadmap`), không ghi DB | ✅ **passing (b8d018f)** · pytest 29/29 (11 BC6+18) · layer trên BC6 · BC12/14/15 (DB) chưa build |

> Base test sau vòng 2: **Auth 4 · Payment 7 · Campaign 17 · Interview 38 = 66 .NET pass** (+ AIService pytest 29), `dotnet build` 0 error.
> **⚠ Lưu ý điều phối (worktree):** worker isolation tạo worktree off `a4c80d5` (merge-base), KHÔNG off tip base → 2 worker (E4/BC13) phải re-anchor onto tip (`git switch -c <br> <base-sha>`; worktree chia object store nên với tới được); P7/E3 tự nhận ra & tự sửa. **Vòng sau: brief phải ghi rõ base SHA để worker tạo nhánh off đúng tip.**

### Vòng giám sát 2026-07-11 (vòng 3) — 3 task passing (3 worker song song, mỗi service 1 worker)
| Task | Nội dung | Trạng thái |
|---|---|---|
| **P4** | `POST /internal/credits/reserve` (`X-Internal-Token`) → `CreditAccountService.ReserveAsync`: reservation chèn trước (UNIQUE(session_id)=khoá idempotency) + bút toán atomic `ExecuteUpdate WHERE remaining≥1` → hết → **402** no-orphan | ✅ **passing (0409ba4)** · Payment 13/13 (+6) · reuse schema P1 (không migration) · ⚠ no-wallet→402(block); postpaid→P8a · chưa wire P2 |
| **C11** | JD/Criteria nhập **text trực tiếp** (`jdText`/`criteriaText`) — text ưu tiên file; publish vẫn sinh `campaign_criteria` từ text | ✅ **passing (4425461)** · Campaign 22/22 (+5) · không migration (cột có từ `InitialCreate`) · ⚠ `CreateCampaign` set `CreatedAt/UpdatedAt` trong code (SQLite-testable) |
| **E8** | Guard điểm C# ở callback chấm: criterion ngoài rubric session → bỏ; điểm → kẹp `[0,maxScore]` (INT-9); áp B2B & B2C | ✅ **passing (2dfb3c3)** · Interview 41/41 (+3) · không migration · defense-in-depth cho callback (dùng branch rubric E1) |

> Base test sau vòng 3: **Auth 4 · Payment 13 · Campaign 22 · Interview 41 = 80 .NET pass** (+ AIService pytest 29), `dotnet build` 0 error. Cả 3 worker được pin base SHA `6e1d93a` trong brief → re-anchor sạch (không lệch merge-base như vòng 2). Merge `--no-ff` vào `docs/sync-design-d18-d21`: `e45f6b6`(P4)·`dc9a25e`(C11)·`8997818`(E8).

### Vòng giám sát 2026-07-11 (vòng 4) — 3 task passing (3 worker song song ở phiên riêng, pin base `9ad7427`)
| Task | Nội dung | Trạng thái |
|---|---|---|
| **P5** | `POST /internal/credits/consume` khi Scored → reservation Reserved→Consumed (`ExecuteUpdate WHERE status=Reserved`) + `reserved−1` + `credit_transactions(Consume,−1)`; owner từ reservation | ✅ **passing (374db18)** · Payment 17/17 (+4) · reuse schema P1 (không migration) · absorbing PAY-11 (Consumed/Released/miss-reserve → no-op) · ⚠ chưa wire consumer RabbitMQ (E7/BC3); postpaid → P8a |
| **C12** | `criteria[]` structured HR khai thẳng → replace-all atomic `campaign_criteria(source=HrEdited,order_no)`; Σweight chuẩn hoá→1/ngoài→400; sửa Active→409; publish bỏ AI khi có criteria[] | ✅ **passing (656402c)** · Campaign 35/35 (+13) · migration `AddCriteriaOrderAndUpdatedAt` (không apply Neon; has-pending=No changes) · ⚠ **apply-caveat:** backfill `order_no` rows cũ trước UNIQUE(campaign_id,order_no) nếu >1 tiêu chí/campaign |
| **BC11** | Seed rubric B2C mặc định (BA/BE/FE, `campaign_id=null`, Σweight=1, is_active) qua `HasData` bọc `if(IsNpgsql())` → B2C upload có publish job chấm | ✅ **passing (e2b6016)** · Interview 47/47 (+6) · migration `SeedB2CRubric` (không apply Neon; has-pending=No changes) · ⚠ seed chỉ Npgsql (giữ SQLite controlled không phá E1); CRUD rubric để sau |

> Base test sau vòng 4: **Auth 4 · Payment 17 · Campaign 35 · Interview 47 = 103 .NET pass** (+ AIService pytest 29), `dotnet build` 0 error. 3 worker chạy phiên riêng (worktree `../isas-P5|C12|BC11`), pin base `9ad7427` → anchor sạch. Merge `--no-ff`: P5·C12·BC11.

### Vòng giám sát 2026-07-11 (vòng 5) — 3 task passing (3 worker phiên riêng, pin base `d5a2aa5`, prompt kèm Context7 EF Core)
| Task | Nội dung | Trạng thái |
|---|---|---|
| **P6** | `POST /internal/credits/release` → reservation Reserved→Released (`ExecuteUpdate WHERE status=Reserved`) + `reserved−1`/`remaining+1` (hoàn chỗ giữ, không ghi ledger); owner từ reservation | ✅ **passing (8c8c885)** · Payment 21/21 (+4) · reuse schema P1 (không migration) · absorbing PAY-11 (Consumed→no-op không hoàn oan) · ⚠ postpaid→P8a; chưa wire consumer (E7/BC4) |
| **E5** | `GET /campaign/{id}/results` → sắp `total_score` DESC + rank read-time (competition) + pass/fail theo `pass_score_pct`; chỉ Scored; ngoài org→404 | ✅ **passing (1774d50)** · Campaign 40/40 (+5) · migration `AddCampaignPassScorePct` (1 cột nullable; không apply Neon; has-pending=No changes) · ⚠ rank competition (đổi được sang ROW_NUMBER); cột rank/result thừa E4 drop sau |
| **BC9** | Session B2C Scored → `overall_score`(equal-weight INT-10)+`answered_count`+`session_criterion_scores`; GET trả `overallScore`+`criteriaScores[]`+`needsImprovement[]`; B2B no-op | ✅ **passing (1cc2fb0)** · Interview 60/60 (+13) · migration `AddSessionResultBC9` (cột + bảng UNIQUE(session_id,criterion_id); không apply Neon; has-pending=No changes) · ⚠ compute trong `SessionScoringNotifier` best-effort (lỗi không chặn Scored); `overallComment` chờ BC10 |

> Base test sau vòng 5: **Auth 4 · Payment 21 · Campaign 40 · Interview 60 = 125 .NET pass** (+ AIService pytest 29), `dotnet build` 0 error. 3 worker phiên riêng (worktree `../isas-P6|E5|BC9`), pin base `d5a2aa5`, prompt nhúng Context7 EF Core (ExecuteUpdate/tx · ordering-rank · GroupBy-Average+SQLite ef_avg). Merge `--no-ff`: P6·E5·BC9.

### Vòng giám sát 2026-07-11 (vòng 6) — 3 task passing (3 worker phiên riêng, pin base `46578c5`, Context7 CsvHelper/QuestPDF/EF)
| Task | Nội dung | Trạng thái |
|---|---|---|
| **P8a** | Postpaid: `ReserveAsync` nhánh postpaid — guard `(period_usage ?? 0)+reserved+1 ≤ credit_limit`, vượt → 402 no-orphan; prepaid P4 giữ nguyên | ✅ **passing (f2a4a5a)** · Payment 27/27 (+6) · không migration (credit_limit/period_usage sẵn) · ⚠ reserve KHÔNG cộng period_usage (payment.md:377) → **BK7** consume postpaid; `credit_limit=NULL`→402 |
| **E6** | Export CSV `GET /campaign/{id}/results/export?format=csv` (CsvHelper, khớp E5); pdf/xlsx→400; ngoài org→404 | ✅ **passing (01bbf28)** · Campaign 46/46 (+6) · +CsvHelper 33.1.0 · không migration · ⚠ **PDF hoãn** (SkiaSharp risk) → **BK8**; cột CSV chưa chốt doc |
| **BC7** | `POST /api/practice/cv-analysis` → parse + AIService `/analyze-cv` → 201 + `cv_analyses` (jsonb); GET chỉ chủ; AI lỗi→502; AI không ghi DB | ✅ **passing (e612908)** · Interview 70/70 (+10) · migration `AddCvAnalyses` (không apply Neon; has-pending=No changes) · ⚠ **cần chốt doc:** `jobCategory` thêm vào request → **BK6**; billing BC-4 mâu thuẫn → **BK5** |

> Base test sau vòng 6: **Auth 4 · Payment 27 · Campaign 46 · Interview 70 = 147 .NET pass** (+ AIService pytest 29), `dotnet build` 0 error. Merge `--no-ff`: P8a·E6·BC7. **Backlog từ ghi chú:** BK1–BK4 (dọn dẹp) + **BK5** (DECISION billing CV — cần team chốt) · **BK6** (ratify jobCategory) · **BK7** (postpaid consume period_usage) · **BK8** (E6 PDF) — xem §Backlog trong tasks.md.

### Vòng giám sát 2026-07-11 (vòng 7) — 3 task passing (3 worker phiên riêng, pin base `70896db`, Context7 RabbitMQ v7/EF/HttpClient)
| Task | Nội dung | Trạng thái |
|---|---|---|
| **E7** | Payment consumer `InterviewEventConsumer` (queue `payment.credit` ↔ `interview.events`) → `CreditEventHandler`: `session.scored`→consume (P5), `session.abandoned`→release (P6); handler tách class unit-test | ✅ **passing (be7b168)** · Payment 33/33 (+6) · không migration · +RabbitMQ.Client 7.2.1 (async API) · absorbing sẵn P5/P6 · ⚠ tên queue/exchange chưa vào doc → **BK9**; e2e broker chờ compose |
| **C13** | Bulk CV `POST /campaign/{id}/candidates` → parse (ParserService) + archive PDF S3 KEY + hard-filter (required_skill/keywords/min_years) → Filtered/Rejected; dedup email; cap→400; chưa Active→409; GET cv | ✅ **passing (dc24c1a)** · Campaign 58/58 (+12) · migration `AddCampaignCandidates` (2 bảng + 3 cột rule, jsonb portable; không apply Neon; has-pending=No changes) · 0 credit (D18/D19) · ⚠ cột AI + `candidate_criterion_scores` để **C14** điền; full_name null (PATCH C14); trả 202 |
| **BC2** | Interview `CreateSessionAsync` B2C reserve credit ví User **trước** insert (typed HttpClient→Payment P4); ví hết→402 no session (PAY-5) | ✅ **passing (916aecd)** · Interview 73/73 (+3) · không migration · reserve idempotent theo sessionId · B2B không đổi (PAY-6) · ⚠ reserve-first→AI-gen lỗi giữ credit→**BC4** release; B2C hard-dep Payment; `Payment:BaseUrl` placeholder |

> Base test sau vòng 7: **Auth 4 · Payment 33 · Campaign 58 · Interview 73 = 168 .NET pass** (+ AIService pytest 29), `dotnet build` 0 error. Merge `--no-ff`: E7·C13·BC2. Backlog +**BK9** (ratify event-bus convention architecture.md §6 — ghi chú lặp E2/E4/E7).

### Vòng giám sát 2026-07-11 (vòng 8) — 3 task passing (3 worker phiên riêng, pin base `9e142d4`, Context7 RabbitMQ v7 publish/EF)
| Task | Nội dung | Trạng thái |
|---|---|---|
| **C14** | Async AI CV scoring: `Filtered`→publish `cv_screening_queue`→`Analyzing`; callback `cv-result`→`candidate_criterion_scores`+`overall_match_score`→`Analyzed`, `cv-failed`→`AnalysisFailed`; `?sort=score` shortlist; PATCH email/fullName (Invited→409); idempotent | ✅ **passing (ba2c1b8)** · Campaign 72/72 (+14) · không migration (schema C13; has-pending=No changes) · 0 credit (D19) · ⚠ tên queue/callback route→BK9; worker Python (AIService) + broker chờ compose |
| **BC8** | Báo cáo "CV↔câu trả lời" read-time: `SessionResultResponse.cvVsAnswer` gap = tiêu chí `needs_improvement` (BC9) ∩ CV mạnh (token-overlap `cv_analyses` BC7); không AI | ✅ **passing (7d91466)** · Interview 83/83 (+10) · không migration (read-time) · doc interview.md §BC8 · ⚠ "mạnh"=token-match không semantic; **worker tự sửa tasks.md → supervisor đã bỏ, tự đánh status** (nhắc brief: chỉ code+test) |
| **BK7** | Postpaid consume cộng `period_usage` (`ConsumeAsync` nhánh postpaid `COALESCE(period_usage,0)+1`; prepaid giữ nguyên) | ✅ **passing (9b702b3)** · Payment 36/36 (+3) · không migration · ⚠ phát hiện bug tiềm ẩn `ReleaseAsync` (P6) `remaining+1` mọi mode → sai postpaid → **BK11** |

> Base test sau vòng 8: **Auth 4 · Payment 36 · Campaign 72 · Interview 83 = 195 .NET pass** (+ AIService pytest 29), `dotnet build` 0 error. Merge `--no-ff`: C14·BK7·BC8 (BC8 bỏ phần worker tự sửa tasks.md, giữ code + interview.md §BC8). Backlog +**BK10** (BC3/BC4-vs-E7 decision) +**BK11** (release postpaid remaining).

### Vòng giám sát 2026-07-11 (vòng 9 — **bug-fix**) — 3 task passing (pin base `6319382`; prompt cấm sửa docs — rút kinh nghiệm BC8)
| Task | Nội dung | Trạng thái |
|---|---|---|
| **BK11** | Fix `ReleaseAsync` postpaid: tách nhánh theo `payment_mode` → postpaid chỉ `reserved−1` (KHÔNG `remaining+1`); prepaid giữ P6 | ✅ **passing (28fff09)** · Payment 38/38 (+2) · không migration · khớp payment.md:373 |
| **BK1** | Drop cột chết `rank`/`result` trên `campaign_rankings` (E4 tạo, E5 tính read-time) | ✅ **passing (9c9d450)** · Campaign 72/72 · migration `DropCampaignRankingRankResult` (reversible; không apply Neon; has-pending=No changes) |
| **BK12** | Fix orphan credit: B2C session `Failed` sau reserve (BC2) → phát `SessionAbandoned(generation_failed)` → E7 release ví User | ✅ **passing (a08b53d)** · Interview 86/86 (+3) · không migration · chỉ B2C; E7 absorbing → an toàn |

> Base test sau vòng 9: **Auth 4 · Payment 38 · Campaign 72 · Interview 86 = 200 .NET pass** (+ AIService pytest 29), `dotnet build` 0 error. Merge `--no-ff`: BK11·BK1·BK12. Cả 3 worker respect "không sửa docs" (khác BC8 vòng 8). **Backlog bug còn:** BK2 (backfill order_no ops) · BK8 (E6 PDF) · BK4 (org_id) · BK6 (ratify jobCategory) · BK9/BK5/BK10 (cần chốt).

### Vòng giám sát 2026-07-11 (vòng 10) — 3 task passing (3 worker phiên riêng, pin base `2839c7e`, Context7 EF Core/RabbitMQ v7)
| Task | Nội dung | Trạng thái |
|---|---|---|
| **P2** | Mua pack OneTime → `POST /webhook/payos` (verify HMAC ChecksumKey TRƯỚC, `[AllowAnonymous]` không gateway) → `ApplyPaidWebhookAsync` idempotent theo `payos_order_code`: order Pending→Paid atomic → ensure ví → `remaining += interview_credits` → ledger(Purchase,+N) + payment_transactions log; Order refactor owner model + wire P7 | ✅ **passing (c3ea069)** · Payment 42/42 (+4) · migration `AddOrderOwner` (cũng alter payment_transactions `order_id` nullable+N-1, khớp doc — **BK13** ratify tên; không apply Neon) · ⚠ **layer-3 PayOS sandbox thật chưa chạy → verify tay** (tạo payment-link + webhook thật) |
| **C15** | `POST /campaign/{id}/candidates/invite {candidateIds[]}` bulk (Active→409, lọc org→404; per-item `Analyzed`+email→invitation gắn `campaign_candidate_id`+email queue+`Invited`+audit; đã Invited→skip; ≠Analyzed/email null/dup→`failed[]`; vượt cap→400) + `StuckScreeningRepublisher` (2': Filtered publish-hụt / Analyzing>15' mất-tích → re-publish) + filter `?status=&minScore=&skill=` | ✅ **passing (f5b854a)** · Campaign 93/93 (+21) · **không migration** (`last_screening_published_at` từ C13; has-pending=No changes) · ⚠ dedup email đường-1 + skill client-eval ngoài checklist (gỡ dễ); e2e broker chờ compose |
| **BC12** | Roadmap 3 bảng (`roadmaps`/`roadmap_milestones`/`roadmap_lessons`) + `POST/GET /practice/roadmaps`: gom Scored B2C→baseline+weakness → AIService `/generate-roadmap` (typed HttpClient, trước Add → lỗi 502 no-row) → lưu milestones(Pending)+lessons(Theory); **0 buổi→201 baseline null**; **không trừ credit**; owner-only GET | ✅ **passing (225e0c3)** · Interview 94/94 (+8) · migration `AddRoadmaps` (jsonb ValueConverter, FK Cascade/Restrict; không apply Neon; has-pending=No changes) · reconcile interview.md:205 (bỏ quy tắc "0 buổi→403" cũ, khớp behavior) · ⚠ baseline gom MỌI buổi Scored (chưa giới hạn N); final_report/overall_comment null chờ BC15 |

> Base test sau vòng 10: **Auth 4 · Payment 42 · Campaign 93 · Interview 94 = 233 .NET pass** (+ AIService pytest 29), `dotnet build` 0 error. Merge `--no-ff`: P2·C15·BC12 (`c3ea069`·`f5b854a`·`225e0c3`). Backlog +**BK13** (ratify migration/schema payment_transactions P2). **⚠ P2 cần verify tay ngoài worker:** PayOS sandbox thật (payment-link + webhook HMAC) — worker chỉ chứng minh tới lớp-2 (unit test credit-add + idempotency).

### Vòng giám sát 2026-07-11 (vòng 11) — 3 task passing (3 worker phiên riêng, pin base `5639a25`, Context7 EF Core; AIService theo pattern repo)
| Task | Nội dung | Trạng thái |
|---|---|---|
| **BC14** | Lesson theory lazy (`GET …/lessons/{id}`→AIService `/generate-lesson-theory`, lưu 1 lần) + `POST …/lessons/{id}/start` (reserve ví User BC2, hết→402; câu hỏi bám `focusCriteria`; `Theory→Practicing`+mile InProgress; Practicing→409) + Scored→Done (`SessionScoringNotifier`) + Abandoned→Theory (`SessionAbandonSweeper`) | ✅ **passing (44a4aed)** · Interview 103/103 (+9) · **không migration** (schema roadmap_lessons từ BC12) · ⚠ `/start` link-after→race hiếm session mồ côi; `focusCriteria` chưa được `/generate-questions` consume (AIService cần thêm field); Abandoned→Theory forward-looking (sweeper quét InProgress, /start tạo Ready → chờ **I2** expire Ready session + release credit treo) |
| **BK4** | Rename `campaigns.employer_id`→`org_id` (+`campaign_questions`) + owner từ JWT claim `org_id` (thiếu→403 AUTH-8); ownership/filter/results/invitations/candidates theo ORG; audit actor giữ = user sub | ✅ **passing (ea480e7)** · Campaign 97/97 (93 sửa + 4) · migration `RenameCampaignEmployerToOrg` (RenameColumn/RenameIndex data-preserving; không apply Neon; has-pending=No changes) · ⚠ **backfill Neon (BK4b):** rows cũ `org_id`=user sub → cần data-migration user→org (`org_members`) trước khi credit-by-org chạy thật; bản copy AGENTS.md còn EmployerId (đồng bộ ở BK4b) |
| **BC10-AI** | AIService `POST /summarize-session` (Python sync best-effort, `{jobCategory,overallScore,criteriaScores[]}`→`{overallComment}` tiếng Việt; anti prompt-injection delimiter; AI không ghi DB) | 🟡 **nửa AI passing (7a60481)** · pytest 39/39 (29+10) · **task BC10 chưa đủ** — còn nửa Interview (gọi endpoint + lưu `overall_comment` khi Scored + migration cột + GET trả) |

> Base test sau vòng 11: **Auth 4 · Payment 42 · Campaign 97 · Interview 103 = 246 .NET pass** (+ AIService pytest 39), `dotnet build` 0 error. Merge `--no-ff`: BC14·BK4·BC10-AI. Backlog +**BK4b** (backfill user→org ops + đồng bộ AGENTS.md copy). **BC10 mới xong nửa AIService** → nửa Interview (wiring `SessionScoringNotifier`) là task kế.

### Vòng giám sát 2026-07-11 (vòng 12) — 1 task passing (**D2 SOLO cross-service**, pin base `6855543`) — **REDESIGN membership model**
| Task | Nội dung | Trạng thái |
|---|---|---|
| **D2** | **Membership model** (Discord/Classroom): Invitation → **Join** → membership (`CampaignCandidate`) → **My Campaigns** → **Start** → create-or-get session. Link mời CHỈ để join, **session tạo khi bấm Start** (không phải khi mở link) | ✅ **passing (5f486b3)** · Auth 7 (+3) · Interview 107 (+4) · Campaign 112 (+15) = **268 .NET pass** · **Auth** `/internal/auth/provision-candidate` (passwordless create-or-get by email + JWT Candidate, idempotent) · **Interview** `/internal/sessions/campaign` (`GetOrCreateCampaignSessionAsync` idempotent (candidateId,campaignId) chưa-terminal) · **Campaign** 5 endpoint (`ParticipationService`): invitation metadata-only · join (membership Joined, đường-2 update CV row + `candidate_id`, đường-1 create) · my-campaigns + detail · start (create-or-get + `session_id`+InProgress; Completed→409) · **E4** += `interview_status=Completed` · migration `AddCampaignMembership` (3 cột nullable; không apply Neon; has-pending=No changes) · ⚠ **loại (task khác):** reserve credit org (Payment), D3 resume, D4 lock-sau-submit · **ratify:** retake-sau-Completed (409) + race Scored↔E4-lag→session mới; JWT không refresh; orgName null (resolve Auth); route `/my-campaigns/{id}` |

> Base test sau vòng 12: **Auth 7 · Payment 42 · Campaign 112 · Interview 107 = 268 .NET pass** (+ AIService pytest 39), `dotnet build` 0 error. Merge `--no-ff`: D2. Reconcile **campaign.md §Distribution** + **tasks.md D2** sang flow membership mới (thay flow cũ "mở token→tạo session ngay"). Backlog +**BK14** (D2 follow-ups: reserve-on-start · retake policy · JWT refresh · orgName resolve). **⚠ D2 mở khoá HTTP entry B2B** — E2E B2B thật (compose + broker: join→start→làm bài→chấm→ranking→my-campaigns Completed) = lớp-3 verify tay ngoài worker.

### Vòng giám sát 2026-07-11 (vòng 13) — 3 task passing (2 worker sạch BC10·P3 + D3 kèm; pin base `c1165fb`)
| Task | Nội dung | Trạng thái |
|---|---|---|
| **P3** | `GET /order/{id}/status` active-polling: chưa có webhook → server gọi PayOS get-payment-info đối soát ngay; Pending+Paid → reuse `WebhookService.ApplyPaidWebhookAsync` (idempotent PAY-8); terminal → không gọi PayOS (PAY-10) | ✅ **passing (8b1168e)** · Payment 49/49 (+7) · `IPayOsQueryClient` tách abstraction (bọc SDK payOS 2.1.0) → unit-test SQLite không cần PayOS thật · **không migration** · ⚠ non-owner→404 (khác 403 ở GetOrder → **BK15**); layer-3 PayOS sandbox verify tay |
| **BC10** | Nửa Interview: session B2C Scored → gọi AIService `/summarize-session` best-effort → lưu `practice_sessions.overall_comment`; GET trả; AI lỗi→null không chặn Scored (nửa AIService 7a60481 từ vòng 11) | ✅ **passing (83000d8)** · Interview 111/111 (+4) · wiring `SessionScoringNotifier` chỉ B2C sau BC9; typed HttpClient mẫu `AiServiceCvAnalyzer`; AI không ghi DB (GEN-4) · migration `AddSessionOverallComment` (1 cột text nullable; không apply Neon; has-pending=No changes) · **mở khoá BC15** |
| **D3** | Resume phía Campaign — khoá regression hợp đồng: start 2× → cùng session, không hạ cấp InProgress; my-campaigns surface SessionId/InProgress | ✅ **passing (dc5db57)** · Campaign 115/115 (+3, test-only) · **không sửa production** (D2 create-or-get đã lo phần resume) · ⚠ mỏng có chủ đích: "câu đã nộp giữ nguyên" ở Interview → **BK16** verify Interview-side |

> Base test sau vòng 13: **Auth 7 · Payment 49 · Campaign 115 · Interview 111 = 282 .NET pass** (+ AIService pytest 39), `dotnet build` 0 error. Merge `--no-ff`: `30217a7`(P3)·`74a0280`(D3)·`5cb18de`(BC10). Backlog +**BK15** (thống nhất 403/404 order) +**BK16** (verify resume Interview-side). **⚠ Sự cố điều phối:** 3 worker chạy **chung 1 working dir** (KHÔNG worktree riêng) → D3 (dc5db57) bị commit chồng dưới BC10 trên nhánh feat/BC10; supervisor gỡ rối (`git branch -f feat/D3 dc5db57` + merge theo thứ tự P3→D3→BC10, 3 commit disjoint-service nên 0 conflict). **Vòng sau: brief PHẢI ép mỗi worker `git worktree add ../isas-<ID> <base>` thật sự** (không chia working dir).

### Vòng giám sát 2026-07-11 (vòng 14) — 3 task passing (3 worktree THẬT ../isas-BC15|D4|P8b off `e508174`, prompt kèm Context7 EF Core)
| Task | Nội dung | Trạng thái |
|---|---|---|
| **BC15** | Milestone improvement + roadmap `Completed` + `final_report`/`overall_comment` + `GET /roadmaps/{id}/report` (radar + levelEvaluation ngưỡng Fresher/Junior/Middle/Senior) | ✅ **passing (b975f8c)** · Interview 120/120 (+9) · hook lesson-Done ở `SessionScoringNotifier`; AIService `/summarize-roadmap` best-effort (GEN-4); Active→interim read-time, Completed→snapshot · **không migration** (cột jsonb từ BC12) · **hoàn tất S5 roadmap BC12→BC15** · ⚠ baseline null→improvement=điểm đạt |
| **D4** | Reissue invitation — revoke token cũ (→410) + phát token mới + resend + `audit_logs(ReissueInvitation)` | ✅ **passing (aa87ee7)** · Campaign 122/122 (+7) · `POST /campaign/{id}/invitations/{invId}/reissue` (Employer, lọc org→404, ≠Active→409); giữ `campaign_candidate_id` đường-2; 1 transaction; KHÔNG đụng membership/session · **không migration** (AuditAction string) · phần "lock-sau-submit" cũ obsolete (D2 membership) |
| **P8b** | Invoice postpaid: chốt kỳ → issue → pay(PayOS) → settle webhook branch theo `Order.Kind` | ✅ **passing (56a78cc)** · Payment 58/58 (+9) · `Invoice`+bảng `invoices`(owner_type/owner_id, numeric) + `orders.invoice_id` + `package_id` nullable · `CloseBillingPeriodAsync` 1-tx (snapshot→invoice→reset) · `POST /invoices/{id}/pay`→settlement order→PayOS · webhook Paid branch (InvoiceSettlement→Paid không cộng credit, CreditPack→P2 no-regression) · migration `AddInvoices` (không apply Neon; has-pending=No changes) · ⚠ **BK17** (Overdue-block reserve bỏ · close reset race · amount int cast · thiếu due_at/paid_at) · reconcile payment.md shape |

> Base test sau vòng 14: **Auth 7 · Payment 58 · Campaign 122 · Interview 120 = 307 .NET pass** (+ AIService pytest 39), `dotnet build` 0 error. Merge `--no-ff`: `30b1873`(P8b)·`8fedc5f`(D4)·`cf6de96`(BC15). Backlog +**BK17** (P8b follow-ups). **✅ Isolation fix hiệu quả:** 3 worker chạy **worktree thật riêng** (`../isas-<ID>` off `e508174`) → 0 race commit-lạc-nhánh (khác vòng 13). Reconcile payment.md §Invoice/§DB sang shape implement + interview.md/campaign.md markers ✅.

### Vòng giám sát 2026-07-11 (vòng 15) — 2 task passing (frontier mỏng: Campaign hết task feature; 2 worktree thật off `0274465`, prompt kèm Context7)
| Task | Nội dung | Trạng thái |
|---|---|---|
| **I2** | Per-question time-limit + bỏ tổng buổi (D21): `session.deadline` + sweeper auto-submit/abandon quá hạn + finalize câu trống→`Skipped` | ✅ **passing (443e831)** · Interview 125/125 (+5) · migration `AddSessionDeadline` (cột nullable; không apply Neon) · `CreateCampaignSessionInternalRequest` nhận `ExpiresAt` optional; `SessionAbandonSweeper` (E3) dùng Deadline thật; Deadline null (B2C)→không quét · Context7 EF Core (ExecuteUpdate/migration/BackgroundService) · ⚠ **BK18** (Campaign CHƯA gửi `expires_at`→B2B tạm Deadline=null); lesson B2C release-credit-treo vẫn hở |
| **A4** | HrMember bị chặn endpoint billing → 403 | ✅ **passing (3608959)** · Payment 66/66 (+8) · `IsHrMember()` (claim `org_role`, offline GEN-3) → `Forbid()` đầu `POST /order`·`/invoices/{id}/pay`·`/admin/invoices/close`; B2C (không claim)/OrgAdmin không chặn · check thủ công (không WebApplicationFactory, khớp idiom repo) · **không migration** · Context7 ASP.NET auth (RequireAssertion/HasClaim) · ⚠ ratify: GET billing HrMember vẫn xem; cancel không chặn; PlatformAdmin gate chờ A5 |

> Base test sau vòng 15: **Auth 7 · Payment 66 · Campaign 122 · Interview 125 = 320 .NET pass** (+ AIService pytest 39), `dotnet build` 0 error. Merge `--no-ff`: `e0e88d1`(I2)·`006b66b`(A4) (2 worktree thật, 0 race/0 conflict). Backlog +**BK18** (Campaign gửi expires_at wire I2). **Frontier thu hẹp:** S2 Campaign + S5 roadmap xong → task còn lại phần lớn collide cùng Interview (E9/E10/E11 chuỗi, BC3/BC4 chờ BK10) hoặc cross-cutting (A5) hoặc chờ DECISION (BK5/BK9/BK10). Vòng sau nên cân nhắc **A5 solo** hoặc **E9→E10→E11** (chất lượng chấm, Interview+AIService).

### Vòng giám sát 2026-07-11 (vòng 16 — BK-cleanup) — 3 task passing (3 worktree thật off `128d2a6`, 1/service, prompt kèm Context7)
| Task | Nội dung | Trạng thái |
|---|---|---|
| **BK17** | P8b follow-ups: Overdue-block reserve (402) + close subtract-snapshot (chống race consume) | ✅ **passing (ee72a0d)** · Payment 71/71 (+5) · `ReserveAsync` postpaid `AnyAsync(Overdue)`→rollback→402 no-orphan; `CloseBillingPeriodAsync` `PeriodUsage−count` thay `=0` · **không migration** · race guard verify revert→FAIL→restore→PASS · ⚠ còn (3) amount int overflow + (4) due_at/paid_at để riêng |
| **BK18** | Campaign gửi `campaign.expires_at` khi tạo session B2B → wire I2 deadline | ✅ **passing (00fb678)** · Campaign 127/127 (+5) · `CreateOrGetSessionAsync` +param `expiresAt`→payload `CreateCampaignSessionInternalRequest.ExpiresAt`; `StartInterviewAsync` truyền `campaign.ExpiresAt` → B2B có Deadline thật → sweeper I2 hoạt động · B2C không đổi (Deadline null) · **không migration** · ⚠ ratify casing payload internal |
| **BK16** | Verify resume Interview-side — "câu đã nộp giữ nguyên" | ✅ **passing (eedfeef)** · Interview 132/132 (+7, **test-only**) · `ResumeContractTests`: resume=cùng session+answer giữ (cùng AnswerId), upload-lại ghi đè idempotent (INT-3), Scored+điểm sống qua resume (INT-4), owner 403/404 (INT-11) · **KHÔNG gap production** (GetOrCreateCampaignSession/GetSessionAsync chỉ đọc `.Include(Scores)`) · đóng nốt câu chuyện D3 |

> Base test sau vòng 16: **Auth 7 · Payment 71 · Campaign 127 · Interview 132 = 337 .NET pass** (+ AIService pytest 39), `dotnet build` 0 error. Merge `--no-ff`: `6431f14`(BK17)·`ceca662`(BK18)·`6dc01fc`(BK16) (3 worktree thật, 0 race/0 conflict). **Cleanup đóng:** BK16·BK17(phần 1+2)·BK18 xong → I2 B2B deadline chạy thật, resume Interview-side khoá hợp đồng, postpaid Overdue/close race fix.

### Vòng giám sát 2026-07-11 (vòng 17) — 1 task passing (**E9 SOLO cross-service** Interview+AIService, off `c568062`, prompt kèm Context7 EF Core)
| Task | Nội dung | Trạng thái |
|---|---|---|
| **E9** | Chấm **neo theo mức** (levels/anchors): ScoringJob gửi `levels[{score,descriptor}]`+anchors, AI chọn mức trả `levelMatched` (score=level), lưu `answer_scores.level_matched`, mức lạ→snap; áp B2B & B2C | ✅ **passing (4b4d625)** · **Interview** 138/138 (+6): migration `AddAnswerScoreLevelMatched` (cột nullable; không apply Neon) · `ScoringCriteriaBuilder` (nạp rubric_levels/anchors Include, **không có→dải `0..maxScore`**) · callback guard E8 lưu `level_matched`+snap mức gần nhất (không drop→tránh Failed INT-9) · **AIService** pytest 45/45 (+6): `build_scoring_prompt` in levels+anchors, `gemini.score()` snap mức score=level (tie-break khớp C#), anti-injection giữ · **mở khoá E10** · ⚠ **E9b** (dùng dải mặc định thay `/suggest-criteria` sinh levels + seed rubric_levels B2C — chấm sát hơn); reject=snap (drop/needs_review→giao E10) |

> Base test sau vòng 17: **Auth 7 · Payment 71 · Campaign 127 · Interview 138 = 343 .NET pass** (+ AIService pytest **45**), `dotnet build` 0 error. Merge `--no-ff`: `a648a40`(E9). Backlog +**E9b** (levels giàu thay dải mặc định). **Bắt đầu chuỗi chất lượng chấm E9→E10→E11** (đều Interview+AIService, tuần tự): E9 xong → **E10** (self-consistency: chấm N lần, median, `needs_review` khi spread>ngưỡng — cần `attempt_no`+`practice_answers.needs_review`).

### Vòng giám sát 2026-07-11 (vòng 18) — 1 task passing (**E10 SOLO cross-service** Interview+AIService, off `da75601`, prompt kèm Context7 EF client-eval)
| Task | Nội dung | Trạng thái |
|---|---|---|
| **E10** | Self-consistency: chấm **N lần** (attempt_no+temperature) → điểm chốt = **median/tiêu chí**; spread=max−min > `Scoring:VarianceThreshold` → `practice_answers.needs_review=true` (cờ HR, không tự chốt) | ✅ **passing (938bef0)** · **Interview** 145/145 (+7, cũ xanh): migration `AddPracticeAnswerNeedsReview` · `ScoringOptions` (N **default 1 opt-in**/threshold/temperature) · publish N job/answer (attempt1 temp=0, 2..N temp>0); callback dùng `req.AttemptNo`, Scored chỉ khi đủ N distinct attempt; `ScoreStatistics` median; downstream **latest→median** (client-eval) · **AIService** pytest 48/48 (+3): `score(temperature)`, worker thread temp + echo attemptNo · **mở khoá E11** · ⚠ **N=1=hành vi cũ nguyên**; **E10b** (selective N× + republisher attempt hụt) |

> Base test sau vòng 18: **Auth 7 · Payment 71 · Campaign 127 · Interview 145 = 350 .NET pass** (+ AIService pytest **48**), `dotnet build` 0 error. Merge `--no-ff`: `44567f9`(E10). Backlog +**E10b** (selective N× + republisher N-attempt). **Chuỗi chất lượng chấm còn E11** (reasoning trích ≥1 dẫn chứng transcript + chặn rỗng + chống prompt-injection "chấm tối đa" + HR chốt; dùng `needs_review` E10) — Interview+AIService, task cuối chuỗi.

### Vòng giám sát 2026-07-11 (vòng 19) — 2 task passing (**E11** Interview+AIService + **A6** Auth, disjoint parallel; off `8e48c3f`, prompt kèm Context7)
| Task | Nội dung | Trạng thái |
|---|---|---|
| **E11** | Chuẩn nhận xét: reasoning trích ≥1 dẫn chứng transcript + chặn rỗng + siết anti prompt-injection ("chấm tối đa" không lái điểm) + flag `needs_review` (HR chốt) | ✅ **passing (f3ef192)** · **AIService** pytest 53/53 (+5): prompt PHỚT LỜ lệnh lái điểm trong transcript + reasoning bắt buộc dẫn chứng; `score()` reject reasoning rỗng · **Interview** 149/149 (+4): `SaveResultAsync` reasoning rỗng/ngắn (< `MinReasoningLen` default 0) → flag needs_review, KHÔNG mất điểm · **không migration** · **HOÀN TẤT E9→E10→E11** · ⚠ E11b (HR override điểm cuối) |
| **A6** | OrgAdmin mời/tạo HrMember (org nhiều thành viên) | ✅ **passing (ac40761)** · Auth 19/19 (+12): `POST/GET /auth/org/members` chỉ OrgAdmin (≠→403), tạo User(Employer) passwordless + `OrgMember(HrMember, org_id caller)`, dedup email→409; A2 loop verified (HR login mang org_role=HrMember→A4 chặn billing) · **không migration** (schema org_members từ A1) · ⚠ joinedAt=User.CreatedAt proxy; auth.md reconcile "sub-account HR phase 2"; A6b (đổi-role/xoá/invite-email) |

> Base test sau vòng 19: **Auth 19 · Payment 71 · Campaign 127 · Interview 149 = 366 .NET pass** (+ AIService pytest **53**), `dotnet build` 0 error. Merge `--no-ff`: `1f5726a`(E11)·`7642f9e`(A6). Backlog +**E11b** (HR override điểm) +**A6b** (quản thành viên đầy đủ). **✅ Chuỗi chất lượng chấm E9→E10→E11 XONG** (neo mức · self-consistency median · nhận xét có dẫn chứng + anti-injection). **✅ Org nhiều thành viên** (OrgAdmin thêm HrMember).

### Vòng giám sát 2026-07-12 (vòng 20) — 1 task passing (**BC7b** Interview-only, off `92f199a`)
| Task | Nội dung | Trạng thái |
|---|---|---|
| **BC7b** | Wire billing phân tích CV B2C (chốt BK5/D22 = tính phí): reserve trước→AI→consume; AI lỗi→release | ✅ **passing (87f3367)** · Interview 153/153 (+4) · `CreditReservationClient` +`ConsumeAsync`/`ReleaseAsync` (gọi Payment /consume /release P5/P6 đã có) · `CvAnalysisService`: reserve ví User (khoá=cvAnalysis.Id) TRƯỚC AI → hết→**402 no-row** (PAY-5); AI OK→consume; AI 502→release · config `Billing:CvAnalysisCredits` (default 1, 0=kill-switch free) · **không migration** · B2B batch CV giữ free (D19) · ⚠ reservation table reuse cho op không-session (sessionId=operationId — ratify tên); consume-fail-sau-commit → credit treo chưa có reconcile op-không-session |

> Base test sau vòng 20: **Auth 19 · Payment 71 · Campaign 127 · Interview 153 = 370 .NET pass** (+ AIService pytest **53**), `dotnet build` 0 error. Merge `--no-ff`: `7f9a466`(BC7b). **BK5/D22 khép trọn** (quyết định charge + code wire xong). Frontier còn: A5 solo · Phase 0 · e2e verify tay · backlog nhẹ.

### Vòng giám sát 2026-07-13 (vòng 21 — backlog cleanup) — 3 task passing (3 worktree thật off tip `6009f5e`, 1/service, disjoint)
| Task | Nội dung | Trạng thái |
|---|---|---|
| **BK15** | Non-owner order endpoint → **404** (leak-avoidance, khớp P3/P8b) | ✅ **passing (f248a6f, merge c98e2ea)** · Payment 77/77 (+6) · `OrderController` GET/DELETE `/order/{id}` other-owner `Forbid()`→`NotFound()`; rà soát `/status`(P3)+invoices(P8b) đã 404; `GetOwner()==null` giữ 403 (khác ca) · `OrderOwnerScopeTests` +6 · **không migration** · reconcile payment.md: hàng 403 bỏ "non-owner đọc order/invoice", hàng 404 += non-owner |
| **BK6** | `cv-analysis` bắt buộc `jobCategory` (thiếu→400 **trước** reserve, không giữ credit oan) | ✅ **passing (6dfd05b, merge eee79ff)** · Interview 154/154 (+1) · DTO `JobCategory?`+`[Required]` (phân biệt "thiếu" vs default enum `BA`=0) + guard đầu `AnalyzeAsync` → 400 trước CV-read & reserve (PAY-5) · **KHÔNG migration** — brief sai tiền đề: cột `cv_analyses.job_category` **đã** NOT NULL từ BC7 (`.IsRequired()`+migration nullable:false), `has-pending`=No changes → migration yêu-cầu là no-op, bỏ · reconcile interview.md §cv-analysis (request +jobCategory, +402 BK5) · ⚠ sibling `CreatePracticeSessionRequest.JobCategory` cùng pattern enum-default-BA (out-scope → follow-up nếu muốn) |
| **A6b** | Quản thành viên org (partial): `joined_at` thật + `PATCH` đổi role + `DELETE` xoá | ✅ **passing (0a56902, merge b384595)** · Auth 40/40 (+21: 11 service + 10 controller) · cột `org_members.joined_at` (migration `AddOrgMemberJoinedAt`, backfill `defaultValueSql now()`, **không apply Neon**) set khi tạo member; GET trả joined_at thật · `PATCH /auth/org/members/{userId}{orgRole}` OrgAdmin↔HrMember (role sai→400; hạ cấp OrgAdmin cuối→409; ngoài org→404; ≠OrgAdmin→403) · `DELETE …/{userId}` **hard-remove** membership (User giữ) (tự-xoá→400; OrgAdmin cuối→409; →204) · reconcile auth.md · ⚠ **còn phase-2:** invite-email + attach-account; hard-delete không audit (ratify vào auth.md) |

> Base test sau vòng 21: **Auth 40 · Payment 77 · Campaign 127 · Interview 154 = 398 .NET pass** (+ AIService pytest **53** không đổi), `dotnet build` 0 error. Merge `--no-ff`: `c98e2ea`(BK15)·`eee79ff`(BK6)·`b384595`(A6b) (3 worktree thật, 0 race/0 conflict — disjoint Payment/Interview/Auth). Backlog done 9→**12** (+BK6/BK15) · A6b partial. Frontier còn: **A5 solo** (auth gate cross-cutting) · Phase 0 (P0.1/P0.2/P0.5) · e2e verify tay (PayOS sandbox · compose+broker+Gemini) · backlog nhẹ (BK8/BK13/BK17(3)(4) cleanup + follow-up).

### Vòng giám sát 2026-07-13 (vòng 22 — **A5 SOLO cross-cutting**, off tip `36fd3f2`) — auth gate toàn repo
| Task | Nội dung | Trạng thái |
|---|---|---|
| **A5** | Bật `[Authorize(Roles)]` mọi service — ẩn danh→401, sai role→403; không endpoint "cần auth" nào còn mở | ✅ **passing (e6fb2c1, merge 805475d)** · **chiến lược per-endpoint attribute** (KHÔNG global FallbackPolicy → không chặn nhầm /health/public/internal/webhook/magic-link) · 🔴 **lỗ thật bịt:** Payment `PackageController` CRUD gói (comment hết auth → mở toang) → `Roles="Admin"`; `admin/invoices/close` → `Roles="Admin"` (trước chỉ `[Authorize]`) · siết role: Payment invoice me/pay→`Employer`; Auth `OrgMembers`→`Employer` (giữ `IsOrgAdmin()`); Interview Practice/Roadmaps/CvAnalysis/Answers/Files→`Candidate` (2 callback internal giữ `[AllowAnonymous]`) · GET package→`[AllowAnonymous]` (payment.md:104 Public) · Auth 9 auth-entry→`[AllowAnonymous]` tường minh · `RoleClaimType=ClaimTypes.Role` explicit ở Payment/Campaign/Interview (khớp Auth) · Campaign đã sạch sẵn không đổi · **KHÔNG migration** · test ✅ **Auth 51·Payment 84·Campaign 133·Interview 162 = 430** (+32: 4 `AuthorizationCoverageTests` reflection guard "0 endpoint trần") · ⚠ **layer-3 verify tay** (live 401/403/internal-token/health — cần stack); GET package public = mở rộng access theo doc; api/files=Candidate-only |

> Base test sau vòng 22: **Auth 51 · Payment 84 · Campaign 133 · Interview 162 = 430 .NET pass** (+ AIService pytest **53** không đổi), `dotnet build` 0 error. Merge `--no-ff`: `805475d`(A5). **Auth gate cross-cutting XONG** — mọi service enforce role; guard test khoá "0 endpoint trần". Frontier còn: **Phase 0** (P0.1/P0.2/P0.5) · **e2e verify tay** (PayOS sandbox · compose+broker+Gemini + A5 live 401/403) · backlog nhẹ (BK8 PDF · BK13 · BK17(3)(4) · follow-up A6b-email/E9b/E10b/E11b/BK14).

### Vòng giám sát 2026-07-13 (vòng 23 — feature theo yêu cầu) — 1 task passing (**BC16** Interview-only, off tip `1b25da2`)
| Task | Nội dung | Trạng thái |
|---|---|---|
| **BC16** | Rubric CÁ NHÂN B2C — candidate tự CRUD rubric theo JobCategory (KHÔNG admin; đảo hướng BK3) | ✅ **passing (c59b02d, merge 2a9521c)** · `RubricCriterion += candidate_id` (null=seed mặc định · set=rubric riêng); migration `AddRubricCandidateId` (**không apply Neon**; has-pending=No changes) · **`B2CRubricScope.ResolveOwnerAsync` dùng chung 4 site scoring** (publish · callback guard · republisher · breakdown BC9) — ưu tiên rubric riêng active else seed, publish↔callback không lệch · `RubricController` `api/practice/rubrics` (`Roles="Candidate"`, owner-scope) GET/PUT/DELETE + `RubricLibraryService` **replace-all soft-versioned** (deactivate cũ + add mới, KHÔNG hard-delete — `answer_scores` FK Restrict) + validate Σweight normalize/bounds/name-unique · **điểm tổng giữ TB cộng INT-10** (weight display-only) · reconcile rules.md BC-7 + interview.md + tasks.md (BK3 superseded) · test ✅ **Interview 183/183 (+21)**: CRUD · resolver · isolation · FK-safe · publish regression (seed-vs-custom) · breakdown BC9 · ⚠ edit-giữa-Scoring edge hiếm; weighted-total = follow-up |

> Base test sau vòng 23: **Auth 51 · Payment 84 · Campaign 133 · Interview 183 = 451 .NET pass** (+ AIService pytest **53** không đổi), `dotnet build` 0 error. Merge `--no-ff`: `2a9521c`(BC16). Frontier còn: **Phase 0** (P0.1/P0.2/P0.5) · **e2e verify tay** (PayOS sandbox · compose+broker+Gemini + A5 live 401/403) · backlog nhẹ (BK8 PDF · BK13 · BK17(3)(4) · follow-up).

### ✅ Verify tay LAYER-3 (2026-07-13) — API sweep server thật qua tunnel (gateway + 5 service + Mac AI)
> Server Ubuntu deploy compose (postgres local, squash `InitialCreate` applied, seed rubric sống) + Mac AIService (image rebuild 07-13). **47 check** — log `~/Downloads/isas-api-test-*.log` + `isas-api-retest-*.log`.

| Nhóm | Kết quả LIVE |
|---|---|
| Auth + Org | ✅ register/login/me · register-org · org members CRUD **A6/A6b** (joinedAt thật · PATCH role · DELETE · guard) · refresh |
| **A5 gates** | ✅ **8/8**: ẩn danh→401 · sai role→403 (Candidate↔Employer · org-members · invoices Employer-only · package Admin-only) |
| **BC16 rubric** | ✅ trọn vòng đời: GET seed (`isCustom=false`, GUID seed đúng) → PUT custom → GET custom → Σweight sai→400 → DELETE → về seed |
| Billing B2C | ✅ tạo session ví 0 credit → **402** (BC2/PAY-5) |
| Files S3 | ✅ upload PDF → SeaweedFS trả fileId |
| **AI e2e** | ✅ `/ai/health` + **tạo roadmap thật** server→Mac→Gemini→DB (BC12/BC13, milestones tiếng Việt) |
| Campaign B2B | ✅ full chain: create→publish (C12 bỏ AI)→results→**CAMP-2 409**→isolation 403→soft-delete |
| Payment | ✅ catalog public · order/my-orders · me/invoices Employer-only |

**🐛 4 bug THẬT tìm ra:** (0) **BF4 — `POST /order` 500** "No route matches" (lộ sau khi BF3 thông, lúc format 201): `CreatedAtAction(nameof(GetOrderAsync))` không khớp action 'GetOrder' (ASP.NET strip 'Async' suffix) → order+PayOS link đã tạo nhưng response 500, client mất `checkoutUrl` → fix `[HttpGet(Name="GetOrderById")]`+`CreatedAtRoute` (Payment 89→91, **cần image payment mới**); (1) **cv-analysis 500 mọi request** — `[property:Required]` positional record → ASP.NET throw (hotfix `83beabc`; **cần image interview mới** → retest sau deploy); (2) **campaign invitations 500** — compose server **thiếu env RabbitMQ** cho campaignservice → fix compose + DEPLOYMENT.md (`0997030`, **chỉ cần `docker compose up -d` lại**, không rebuild); (3) **BF3 — `POST /order` 500** (luồng tiền, sweep 2026-07-13 qua tunnel) — compose server **thiếu env `PayOS__ReturnUrl/CancelUrl`** → PayOS reject "return_url null" stack thô → fix code (guard config sớm + catch ApiException→502 `PaymentGatewayException`, không order mồ côi) + `appsettings.json`/`.env.example`/`DEPLOYMENT.md` +2 key (Payment 84→89, **set env server + up -d**, không rebuild). **Backlog mới:** ~~BK19~~ ✅ **done** (ratify giữ code 404; payment.md liệt kê cả 404 unknown + 400 inactive) · ~~BK20~~ ✅ **done** (Campaign bật `JsonStringEnumConverter` + test +4, backward-compat numeric; Campaign 133→137 · ⏳ retest live sau redeploy). **⚠ Deploy-gap luồng tiền:** DB server **không seed** `product_packages` (catalog rỗng) + **không seed Admin** → phải seed tay/tạo admin trước khi mua credit. **Còn verify:** ~~PayOS sandbox webhook Paid+HMAC→credit thật~~ ✅ done (2026-07-13/15) · ~~B2B join→start→chấm→ranking trọn luồng~~ ✅ **done live 2026-07-15/16** (§Đợt 1–3b) · ~~B2C mua-credit→luyện→chấm trọn luồng~~ ✅ **done live** · cv-analysis retest (BF1 fixed) · **BK20 questions[].source="CustomHr" live** (image redeploy). **Gap B2B còn lại (first-class):** **invitation-email consumer** (D5 — publisher có, chưa gửi mail) + **SEC anti-cheat** (SEC1 — 0% implement) + Phase 0 (compose máy sạch/make).

**✅ Luồng tiền E2E chạy thật (2026-07-13 tối, sau BF3/BF4 + set env server):** B2C mua credit trọn vòng trên server deploy — `POST /order` **201** + `checkoutUrl` PayOS thật → thanh toán sandbox → đơn **Paid** (PAY-8) → **credit vào ví** (chứng minh: tạo practice session **201** thay vì 402 lúc ví rỗng) → bonus: AI server→Mac→Gemini sinh 5 câu hỏi thật. Prereq tay: seed `product_packages` + admin (SQL) + env `PayOS__ReturnUrl/CancelUrl` + `up -d`.

**✅ Sweep đợt TỐI 2026-07-13 (~50 check, log `~/Downloads/isas-api-fullsweep-20260713-221602.log`):** PASS — auth (register/login/me/refresh) · A5 gates 8/8 · **BC16 rubric trọn vòng đời live** · **BF1 FIXED LIVE** (cv-analysis 400, hết 500) · **BF3+BF4 FIXED LIVE** (order 201+checkoutUrl · status P3 Pending) · order/my-orders · BK19 404 · admin package CRUD (tạo+xoá) · BC2 402 ví rỗng · campaign create/publish/**CAMP-2 409** (criteria+questions trên Active → 409; metadata title sửa được by-design C7)/isolation 403/results/soft-delete · files upload→fileId · **org members A6/A6b 9/9** (PATCH role · guard role-sai 400 · tự-xoá 400 · OrgAdmin-cuối 409). **❌ Còn 3 việc — đều DEPLOY, không phải code mới:** (1) **BF2** invitations vẫn **500** — compose server **chưa áp** env RabbitMQ campaign (fix `0997030`; sửa compose server + `up -d`); (2) **BK20** `source="CustomHr"` vẫn **400** — **image campaign cũ** (fix `c500472` ở branch; merge→CI→pull image campaign + payment/interview để BF3-502/BF4/BF1 code vào bản chạy); (3) ~~**`/ai/*` gateway 404**~~ → **hoá ra KHÔNG hỏng** (route đúng là `/api/v1/ai/**`; `/api/v1/ai/health` live=200, sweep gọi sai path) — nhưng lộ `/api/v1/ai/*` **public+không-auth** → **✅ làm GEN-7 (commit `2a15903`)**: gỡ ai-route+cluster+OpenApi khỏi gateway (AI internal-only qua `AiService:BaseUrl`; interview→AI vẫn chạy). Verify local: ai→404, route khác→502. ⏳ deploy gateway image mới. Ghi chú: server mới seed admin@ (candidate@/employer@/hr@ chưa — A4 HrMember-403 đã verified đợt sáng); campaign test đã soft-delete; order Pending tự Expired 30'.

### ✅ Đợt 1–3b hardening + BK14 (2026-07-15 → 07-16) — sau merge nhánh tích hợp về `main`
> Batch fix "bug không đáng có" lộ ở deploy/e2e thật + BK14 wire org-credit-reserve. `dotnet test` **518 pass** sau BK14. Nhánh `feat/bk14-org-credit-reserve` (tip `3399765`, off `main` sau các merge PR #29–#31 + squash `InitialCreate`).

**Đợt 1–3b — hardening (commits e523576 → b7a3f89):**
| Fix | Nội dung | Commit |
|---|---|---|
| jobCategory bắt buộc | `CreatePracticeSessionRequest.JobCategory` `[Required] JobCategory?` (param-level, không `[property:]` — tránh 500 .NET 10) + guard đầu `CreateSessionInternalAsync` → thiếu → **400 TRƯỚC reserve/AI/session** (không trừ credit oan) | e523576 |
| AI outage → 502 | Lỗi gọi AIService lúc sinh câu hỏi → **502** (không phải 400) | b1239d4 |
| INT-3 clear stale scores | Upload lại answer → **xoá `answer_scores` cũ** trước khi publish lại (không giữ điểm rác) | f96b900 |
| PAY-13 no-charge-all-failed | Session đóng mà mọi answer `Failed`/`Skipped` (scoredCount==0) → **KHÔNG consume** → `SessionAbandoned` (release), không `SessionScored` | 6025425 |
| release-on-create-fail | Reserve xong mà tạo session lỗi → **release** credit đã giữ (không treo) | a879a70 |
| release inactive abandoned B2C | Session B2C bỏ ngang không active → release credit | 32aeab0 |
| SettlementReconciler | `settlement_published_at` marker (migration `AddSessionSettlementPublishedAt`) + reconciler **republish** event settlement B2C bị mất | d0f7c94, b7a3f89 |
| amountVnd int→long | `orders.amount_vnd` widen (migration `AmountVndToBigint`) — tràn VND | 58e6919 |
| constant-time token | So sánh `X-Internal-Token` **constant-time** (Payment+Interview) chống timing-attack | 0a55343 |

**BK14 — reserve/consume org credit khi bắt đầu phỏng vấn B2B (commit `3399765`):**
- **Trước:** `POST /campaign/{id}/start` **không** reserve/consume org credit (PAY-6 chưa wire) → credit org mua vào ví **không bao giờ bị trừ**, không có 402 khi ví org rỗng.
- **Sau:** wire reserve tại **InterviewService** (reserve-first như B2C, tránh orphan chicken-and-egg `session_id`): `CreateCampaignSessionInternalRequest` += `OrgId` (Campaign gửi `campaign.OrgId`); `PracticeService.CreateCampaignSessionAsync` reserve `owner=Org` **TRƯỚC** insert + try/catch release; ví org hết → `InsufficientCreditException` → **402 no-session** (PAY-5). Campaign map 402 → `InsufficientOrgCreditException` → `ParticipationController` **402**. **Consume/release KHÔNG đổi** (E7 lấy owner từ reservation). +5 test.
- **Còn mở (BK14 partial):** DECISION retake sau Completed · JWT-refresh khi đổi role · orgName resolve (D23 §Hệ quả).

**🐛 Deploy config bugs tìm+sửa (env compose server, không phải lỗi code):**
- **interviewservice DB thiếu cột `settlement_published_at`** → migration `AddSessionSettlementPublishedAt` apply **tay** trên server (giữ rule no-auto-migrate).
- **Mac aiworker `INTERNAL_TOKEN` lệch** với các service .NET → callback chấm bị 401 → sync token.
- **campaignservice thiếu env `Internal__Token` / `Auth__BaseUrl` / `Interview__BaseUrl`** → join/start/provision gọi nội bộ hỏng → bổ sung compose.
- **authservice thiếu env `Internal__Token`** → `/internal/auth/provision-candidate` (D2) từ chối → bổ sung.
- *(Cross-link:* [DEPLOYMENT.md](../DEPLOYMENT.md) + note no-auto-migrate — schema apply tay/pipeline TRƯỚC deploy.)*

**✅ Live e2e verified trên server deploy (2026-07-15/16):**
- **B2C trọn vòng:** mua credit (PayOS sandbox) → webhook Paid cộng credit ví User → tạo practice session (reserve −1) → ghi âm/chấm → `Scored` → **consume** 1 credit → xem lịch sử.
- **B2B trọn vòng:** org có credit → tạo campaign → invite → **join** (provision Candidate) → **Start** (**BK14 reserve org credit**) → làm bài → chấm → `SessionScored` → **consume** + upsert `campaign_rankings` → **ranking Pass/Fail** hiển thị.

> Test project trong tree hiện có (đủ 4): `Isas.AuthService.Tests` · `Isas.PaymentService.Tests` (✅ P0.4) · `Isas.CampaignService.Tests` · `Isas.InterviewService.Tests`.

### ✅ E2E toàn hệ thống trên deploy (2026-07-17) — Vercel FE + tunnel BE, UI + API
> FE `sep-490-angular.vercel.app` (Angular 21) + BE gateway qua Cloudflare tunnel + Mac AI. Test **đa-agent** (Auth/Payment/Admin/B2C/B2B, mỗi mảng 1 subagent) + **UI thật bằng Chrome** cho 3 role. Cả 2 dòng sản phẩm **xanh, UI + API**. 4 tài khoản test (candidate@/employer@/hr@/admin@, pass `Test@123456`) login live OK.

**Kết quả PASS:**
- **AI pipeline khỏe:** Gemini live · `aiworker` up · `INTERNAL_TOKEN` worker == `interviewservice` `Internal__Token` (bug "callback 401 → không bao giờ chấm" 2026-07-15 KHÔNG còn).
- **Auth/authZ 15/15** (register/login/me/refresh · org-members A6/A6b CRUD+guard 409/400 · A5 gate 401/403) · **Payment/billing 13/13** (order→`checkoutUrl` PayOS thật · owner-isolation 404 · HrMember/anon/non-admin gate) · **Admin 8/8** (package CRUD full + `admin/orders|organizations|users`, `campaign/admin`, `invoices/close` — đều gate) · **B2B engine 13/13** (create→publish (AI-free do criteria)→invite→**join**→**Start reserve org credit** (BK14: 4→3)→chấm→**ranking <60s**→consume→CSV→**HR override** giữ aiScore) · **B2C engine 11/11** (CV upload+parse→cv-analysis (JD match 90)→session (5 câu AI)→**Scored**→history→rubric CRUD (Σweight 400)→roadmap→lesson→**402 ví rỗng**).
- **UI Chrome thật:** B2C candidate (dashboard/CV-JD/analysis/practice/roadmap/rubric/credit), **Employer B2B đầy đủ** (campaigns list/detail/criteria/questions/invite/**results+ranking+badge "HR chỉnh"**/members/org-credit), **Admin đầy đủ** (users/orgs/orders platform-wide) — render dữ liệu live, network `/api/v1` 200, không 404 runtime.
- **Luồng tiền LIVE hôm nay:** mua credit qua UI → PayOS checkout thật → user thanh toán → order **Paid** (`paidAt`) → `credit_transactions` **`+5 Purchase`** gắn đúng `order_id`, idempotent → **PAY-8 verify live** (không chỉ historical).
- **Chấm tự hồi phục:** 1 answer B2C kẹt ~18' vì **Gemini 503 "high demand" + worker↔RabbitMQ flap** (Errno 111 qua Tailscale) → `nack`→republish + `StuckAnswerRepublisher` tự cứu → `Scored`. Platform-wide 0 answer kẹt (Scored 16/Skipped 11/Failed 2).

**Fix (đều qua branch → PR → main → CI/CD, KHÔNG deploy trực tiếp server):**
- **Campaign list "0 tiêu chí"** — `CampaignService.GetCampaignsAsync` thiếu `.Include(c => c.Criteria)` (detail có) → list DTO trả `criteria` rỗng → card FE hiện 0. Thêm Include. **173 Campaign test pass**. **PR #42 merged → CI/CD deploy → verify LIVE** (list trả `criteria=2/4`; card Employer hiện đúng số).
- **FE loading splash** (repo isas-frontend) — quay về từ PayOS = full cold-reload xuyên origin → app Angular boot lại → màn hình trắng/tối vài giây. Thêm splash tĩnh trong `index.html` (`#isas-splash` trong `<app-root>`, Angular tự thay khi boot). Deploy Vercel (`master`). *(Splash nền tối trên thiết bị dark-mode = **chấp nhận được**, user chốt giữ nguyên — app luôn theme sáng.)*
- **CI deploy pull-tolerant** (**PR #41**, `148c2fe`): deploy job dùng `docker compose pull --ignore-pull-failures` → gỡ root-cause "deploy never updates" (trước kẹt vì service tag local).

**Đã rà, KHÔNG phải bug:**
- **Whisper transcript rác trong test** = audio tổng hợp (`say` WAV) + Whisper **tiny**; đuôi object-key `.webm` hardcode (`AnswerService.cs:80`) **đúng** cho ghi âm thật FE (MediaRecorder webm/opus). Muốn điểm chính xác hơn → nâng model Whisper, không liên quan object-key.
- **Cloudflare tunnel 502** khi call AI sync lâu (~13s cv-analysis) → retry là được (tunnel timeout, không phải app).

**Nit nhỏ (chưa sửa, non-blocking):** invite trả HTTP **200** (không 201); `orgName` **null** trong invitation metadata (BK14 tail — Campaign chưa resolve tên org xuyên service); campaign list card criteria giờ đúng (fix trên).

**⚠ Quy trình (user chốt, cứng):** MỌI thay đổi đi qua **branch → PR vào `main` (BE) / `master` (FE)** để CI/CD test+tích hợp — **KHÔNG hotfix server trực tiếp** (`docker save|load` + compose restart). (Đã rút kinh nghiệm sau khi lỡ định hotfix + push thẳng.)

### ✅ Vòng S6 — DB Hardening đợt 1 (2026-07-17) — 5 task passing (3 worker disjoint theo service, worktree thật off `f392f53`)
> Frontier mới **S6 — DB Hardening/Integrity/Scale** (từ review kiến trúc 2026-07-17). Vòng hygiene 3-worker: front-load P0-Critical DB1 + dọn hygiene. Pattern multi-agent supervised (1 worker/service, disjoint → 0 conflict). Mỗi task = 1 branch → PR vào `main`. Supervisor verify độc lập (gộp 3 nhánh octopus-merge, full-solution build + test lại).

| Task | Service | Nội dung | Trạng thái |
|---|---|---|---|
| **DB1** 🔴 | Payment | CHECK số dư credit không âm: 2 `HasCheckConstraint` model-level (`ck_credit_accounts_non_negative` `remaining/reserved/period_usage>=0` · `ck_credit_transactions_delta_nonzero` `delta<>0`) + bật snake_case `PaymentTestDb` → verify SQLite (âm/`delta=0`→constraint fail) | ✅ **passing (s6/db1-db3-payment · `d67ab05`, PR #47)** · migration `AddCreditNonNegativeChecks` (không apply Neon) · Payment 109→118 (+9) · ⚠ apply: dọn row số dư âm / `delta=0` trước (DB4 lo drift) |
| **DB3** 🔴 | Payment | money int→bigint: `product_packages.price_vnd` int→long (+3 DTO PriceVnd); `orders.amount_vnd` đã bigint (58e6919) | ✅ **passing (s6/db1-db3-payment · `6eca49e`, PR #47)** · migration `PriceVndToBigint` (`AlterColumn<long>`, không apply Neon); PayOS SDK nhận `long` · test round-trip `> int.max` |
| **DB11** 🟠 | Auth | email UNIQUE: `normalized_email` → unique **filtered** index `WHERE … IS NOT NULL` (`RequireUniqueEmail` đã có → enforce tầng DB) | ✅ **passing (s6/db11-db12-auth · `0697fb8`, PR #48)** · migration `UniqueEmailIndex` (không apply Neon) · Auth 60→64 (+4) · ⚠ apply: dedup email trùng trước |
| **DB12** 🟠 | Auth | unique index `refresh_tokens(token)` (hash SHA-256 **đã có sẵn** `JwtService` — chỉ thêm index, chống trùng + index lookup) | ✅ **passing (s6/db11-db12-auth · `e4acffa`, PR #48)** · migration `UniqueRefreshTokenIndex` (không apply Neon) · apply an toàn (hash ngẫu nhiên) |
| **DB13** 🟠 | Campaign | soft-delete query filter: nav-based `HasQueryFilter(x.Campaign.DeletedAt==null)` 4 con (Question/Criterion/Invitation/Candidate) + chained filter thứ 5 `CandidateCriterionScore` → hết 4 warning runtime EF `PossibleIncorrectRequiredNavigation` | ✅ **passing (s6/db13-campaign · `bc55291`, PR #49)** · **CODE-ONLY, không migration** · regression-guard test `ConfigureWarnings(Throw)` · Campaign 173→176 (+3) |

> Base test sau vòng S6 đợt 1: **Auth 64 · Payment 118 · Campaign 176 · Interview 183** (Interview không đổi), `dotnet build` solution 0 error. **Còn P0 money-integrity:** **DB4** (CreditAccount reconcile — dep DB1, nay đã mở khoá) · **DB2** (Transactional Outbox — cross-service, làm SOLO 1 agent). **⚠ Apply migration lên DB thật** (no-auto-migrate): DB1 fail nếu còn số dư âm/`delta=0`; DB11 fail nếu email trùng sẵn → dọn trước; DB3/DB12 an toàn; DB13 không schema.

### ✅ Vòng S6 — DB Hardening đợt 2 (2026-07-17) — 2 task passing (2 worker disjoint Payment ‖ Interview, worktree thật off `0c7c553`)
> P0 DB4 + filler disjoint DB19 (mọi P1 còn lại cross-service/chạm Payment → không disjoint được với DB4). Supervisor review code + gộp octopus-merge + full-solution build + test lại độc lập.

| Task | Service | Nội dung | Trạng thái |
|---|---|---|---|
| **DB4** 🔴 | Payment | `CreditReservationReconciler` (BackgroundService, mirror SettlementReconciler): quét account-side → `CountAsync(Reserved cùng owner)` (no GroupBy) → lệch → `ExecuteUpdate reserved_credits=count`; guard chống âm (count≥0 + CHECK DB1); idempotent; options `Reconcile:Enabled/ScanIntervalSeconds` (on/120s) | ✅ **passing (s6/db4-payment · `442a0095`)** · **KHÔNG migration** (options config) · Payment 118→122 (+4) · **scope=core Payment-DB thuần** (không gọi Interview); phần "reservation mà session Interview terminal" = **DB4b** (backlog) |
| **DB19** 🟡 | Interview | CHECK `ck_rubric_criteria_single_owner` (`campaign_id IS NULL OR candidate_id IS NULL`) — cấm both-set, cho 3 trạng thái loại trừ (campaign-only/candidate-only/both-null seed) | ✅ **passing (s6/db19-interview · `488c4898`)** · migration `AddRubricSingleOwnerCheck` (AddCheckConstraint, không add cột; không apply Neon) · snake_case-TestDb (precedent DB1) + sửa 5 site test bg-service · Interview 229→233 (+4) · ⚠ apply: fail nếu tồn tại row both-set (kỳ vọng 0) |

> Base test sau vòng S6 đợt 2: **Auth 64 · Payment 122 · Campaign 176 · Interview 233**, `dotnet build` solution 0 error. **Còn P0:** **DB2** (Transactional Outbox — cross-service SOLO, vòng riêng; mở khoá DB18). **Kế:** DB9 (FK nội-service, dep DB4 ✅ nay mở khoá) · **DB4b** (reconcile cross-service reservation↔session-terminal).

### ✅ Vòng S6 — DB Hardening đợt 3 (2026-07-17) — 1 task passing (**DB2 SOLO Interview**, off `62e504c`) — HOÀN TẤT P0 money-integrity
> Task rủi ro cao nhất S6: đụng transaction-boundary luồng đóng session + gỡ background service. Supervisor review code thật (atomic 5 site · sweeper transaction · dispatcher retry · no double-publish · migration reversible) trước khi merge.

| Task | Service | Nội dung | Trạng thái |
|---|---|---|---|
| **DB2** 🔴 | Interview | Transactional Outbox: bảng `outbox_messages` (jsonb + partial index) + chèn row CÙNG transaction state-flip 5 site (Scored/Abandoned/**generation_failed**) + sweeper bọc `BeginTransactionAsync`; `OutboxDispatcher` at-least-once (broker down→giữ `published_at` null + `Attempts++`→gửi lại, event không mất); **gỡ `SettlementReconciler`+marker** (payload giữ nguyên weighted → cover cả B2B + generation_failed nó bỏ sót; publish path DUY NHẤT = dispatcher, no double-publish) | ✅ **passing (s6/db2-interview-outbox · `7a4edfc`)** · migration `AddOutboxMessages` (drop `settlement_published_at` + create outbox; reversible; không apply Neon) · Interview 233 (11 SettlementReconcilerTests→OutboxDispatcher/OutboxEnqueue tests) · **scope Interview**; Campaign email = **DB2b**; **Payment=0** (không phát event, consumer idempotent sẵn) |

> Base test sau vòng S6 đợt 3: **Auth 64 · Payment 122 · Campaign 176 · Interview 233** (Payment/Campaign consumer không đổi vẫn xanh), `dotnet build` solution 0 error. **✅ P0 money-integrity XONG** (DB1 CHECK · DB2 outbox · DB3 bigint · DB4 reconcile). **Kế:** **DB2b** (Campaign invitation-email outbox + consumer dedup) · **DB18** (Saga Start, nay mở khoá do DB2) · **DB9** (FK, dep DB4) · P1 hygiene (DB5/6/7/8/10/14/15). **⚠ L3 verify tay:** đóng session khi RabbitMQ tắt → outbox `published_at=null` → bật lại → dispatcher phát → Payment/Campaign nhận (event không mất; B2B abandon nay durable).

### ✅ Vòng S6 — DB Hardening đợt 4 (2026-07-17) — DB2b done (SOLO Campaign, off `b9403e7`) — khép câu chuyện outbox
> Mirror pattern DB2 (Interview) sang Campaign invitation-email, per-service. Supervisor review code thật (atomic 3 site · consumer dedup trước ack · dispatcher retry · CampaignTestDb snake_case không phá 176 test · cv_screening không đụng · migration reversible).

| Task | Service | Nội dung | Trạng thái |
|---|---|---|---|
| **DB2b** | Campaign | invitation-email Transactional Outbox + consumer dedup: bảng `outbox_messages` (config inline + jsonb gate IsNpgsql + partial index) + chèn row CÙNG SaveChanges tạo invitation 3 site (`CreateInvitations`/`InviteShortlisted`/`Reissue`) thay dual-write best-effort; `PublishRawAsync` (queue + MessageId); `OutboxDispatcher` at-least-once (broker down→giữ null+retry, mail không mất); **consumer dedup** cột `email_sent_at` (guard `InvitationId`, set TRƯỚC ack) bịt bug email trùng | ✅ **done (s6/db2b-campaign-outbox · `e4f50bd`)** · migration `AddOutboxMessages` (+`email_sent_at`; reversible; không apply Neon) · Campaign 176→189 (+13) · index (a) partial → snake_case CampaignTestDb (precedent DB19) · cv_screening (reconciler riêng) không đụng |

> Base test sau vòng S6 đợt 4: **Auth 64 · Payment 122 · Campaign 189 · Interview 233**, `dotnet build` solution 0 error. **✅ Câu chuyện outbox khép** (Interview settlement events DB2 + Campaign invitation-email DB2b). **D5 invitation-email** nay không chỉ có consumer mà đã **reliable (outbox) + idempotent (dedup)**. **Kế:** **DB18** (Saga Start, mở khoá do DB2) · **DB9** (FK, dep DB4) · P1 hygiene (DB5/6/7/8/10/14/15) · P2. **⚠ L3 verify tay:** invite khi RabbitMQ tắt → outbox `published_at=null` → bật lại → dispatcher phát → consumer gửi 1 lần (redeliver không trùng nhờ `email_sent_at`; cần SMTP creds thật).

### ✅ Vòng S6 — DB Hardening đợt 5 (2026-07-17) — DB18 passing (SOLO Payment+Interview, off `2487b8f`) — khép DB4b
> Đóng hở orphan reservation luồng Start (crash reserve↔insert). **Phương án nhẹ** (compensation-reconciler, team loại full-saga vì over-engineered). Supervisor review code thật (safety Interview-down → skip · positive-confirm · idempotent · endpoint token).

| Task | Service | Nội dung | Trạng thái |
|---|---|---|---|
| **DB18** | Payment+Interview | Start orphan-reservation compensation: `OrphanReservationReconciler` (Payment BG) quét `Reserved` quá 10' → `POST /internal/sessions/exists` (Interview batch) xác nhận DƯƠNG session không tồn tại → `ReleaseAsync` (idempotent PAY-11). Interview down → skip KHÔNG release mù. Bao B2B+B2C+lesson | ✅ **passing (s6/db18-orphan-reservation · `5463ef8`)** · `InterviewSessionClient` (lần đầu Payment→Interview) + `OrphanReconcileSettings` · **KHÔNG migration** · Payment 122→129 (+7) · Interview 233→239 (+6) · **khép DB4b** · ⚠ deploy env `Interview__BaseUrl` (trống→safe-skip) |

> Base test sau vòng S6 đợt 5: **Auth 64 · Payment 129 · Campaign 189 · Interview 239**, `dotnet build` solution 0 error. **Kế:** **DB9** (FK nội-service, dep DB4) · P1 hygiene (DB5 index sweeper · DB10 optimistic concurrency · DB14 audit cols · DB6/7/8/15) · P2 (DB16/17). **⚠ Deploy DB18:** thêm env `Interview__BaseUrl` cho Payment trong compose (không set → reconciler safe-skip mỗi vòng, log lỗi/scan). **L3 verify tay:** reserve `session_id` ngẫu nhiên (không tạo session) → chờ >10' → reconciler release (0 orphan).

### ✅ Vòng S6 — DB Hardening đợt 6 (2026-07-17) — DB9 passing (2 worker disjoint Payment ‖ Campaign, off `811092c`)
> FK nội-service (ref xuyên-service giữ Guid lỏng GEN-2). Supervisor review kỹ (Payment: fix money-logic `ReserveAsync` · Campaign: query filter kèm rankings/flags giữ 0 warning).

| Task | Service | Nội dung | Trạng thái |
|---|---|---|---|
| **DB9** 🟠 | Payment | composite FK `(owner_type,owner_id)→credit_accounts` cho reservations/transactions/invoices (Restrict) + `HasAlternateKey` (Postgres FK cần UNIQUE CONSTRAINT); **fix `ReserveAsync`** đọc ví TRƯỚC insert (FK cấm reservation mồ côi → giữ no-wallet→402 PAY-5) | ✅ **passing (s6/db9-payment · `1e19e7c`)** · migration reversible, không apply Neon · Payment 129→134 (+5) · ⚠ apply: backfill ví thiếu trước add FK |
| **DB9** 🟠 | Campaign | FK rankings/session_flags→campaigns (Restrict) **+ query filter DB13 kèm** (required nav CampaignId, không thì warning FAIL) + invitations→candidate (SetNull, optional) | ✅ **passing (s6/db9-campaign · `9aa1e50`)** · migration reversible, không apply Neon · Campaign 189→190 (+1) · ⚠ apply: DELETE ranking/flag mồ côi + SET NULL invitation mồ côi |

> Base test sau vòng S6 đợt 6: **Auth 64 · Payment 134 · Campaign 190 · Interview 239**, `dotnet build` solution 0 error. **Bài học:** **SQLite CÓ enforce FK** (EF Core 10, connection mở ngoài — đính chính giả định cũ) → test FK task phải seed parent. **Kế:** P1 hygiene (DB5 index sweeper · DB10 optimistic concurrency `UseXminAsConcurrencyToken` · DB14 audit cols · DB6/7/8/15) · P2 (DB16 tách God-table · DB17 FileService). **✅ Apply DB9 server (2026-07-17):** đã apply `AddIntraServiceForeignKeys` lên `isas_payment` + `isas_campaign` bằng **idempotent EF script** (preflight read-only: 0 orphan/dup → KHÔNG cần dọn). **4 DB server nay sync với `main`** (Auth `isas` + Interview `isas_interview` đã current sẵn; chỉ 2 FK migration này pending).

### ✅ Vòng S6 — DB Hardening đợt 7 (2026-07-17) — DB5 passing (3 worker disjoint Interview ‖ Payment ‖ Campaign, off `1dc86f8`)
> Index background sweeper (nay full-scan mỗi 2'). PM supervised: 3 worker worktree cô lập (1/service, disjoint → octopus-merge 0 conflict). Supervisor review từng diff (index shape khớp predicate · không trùng index · filter snake_case · migration reversible · test model-inspection) + octopus-merge verify độc lập.

| Task | Service | Nội dung | Trạng thái |
|---|---|---|---|
| **DB5** 🔴 | Interview | partial `ix_practice_sessions_deadline` `WHERE deadline IS NOT NULL` (B2B expired) + `ix_practice_sessions_b2c_active` `(created_at) WHERE campaign_id IS NULL AND deadline IS NULL` (B2C inactivity) + `ix_practice_answers_status_lsp` `(status,last_scoring_published_at)` (StuckAnswerRepublisher) | ✅ **passing (s6/db5-interview · `db8fcab`)** · migration reversible, không apply Neon · Interview 239→242 (+3 model-inspection) · partial neo cột **bất biến** (deadline/campaign_id) tránh index churn |
| **DB5** 🔴 | Payment | partial `ix_credit_reservations_reserved` `(owner_type,owner_id,created_at) WHERE status='Reserved'` phủ CẢ `CreditReservationReconciler` (prefix owner) + `OrphanReservationReconciler` (created_at tail); **giữ FK index `(owner_type,owner_id)`** (khai tường minh chống model-differ drop — partial không phủ FK all-status). **Bỏ `credit_transactions(owner_id)`** (không sweeper quét + FK index đã phủ owner-leftmost → tránh dead index) | ✅ **passing (s6/db5-payment · `d8f9147`)** · migration ADDITIVE (không drop FK index), reversible, không apply Neon · Payment 134→137 (+3) |
| **DB5** 🔴 | Campaign | `ix_campaign_candidates_status_lsp` `(status,last_screening_published_at)` (StuckScreeningRepublisher; `(campaign_id,status)` cũ vô dụng vì predicate không có campaign_id) | ✅ **passing (s6/db5-campaign · `68294bf`)** · migration reversible, không apply Neon · Campaign 190→191 (+1) · non-partial (status lưu string, cột dẫn selective) |

> Base test sau vòng S6 đợt 7: **Auth 64 · Payment 137 · Campaign 191 · Interview 242** (=634), `dotnet build` solution 0 error · `has-pending-model-changes`=No changes cả 3 service. **Điều chỉnh vs đề xuất tasks.md gốc:** partial neo cột **bất biến** thay `status IN(...)` (status mutate → index churn); **bỏ dead index `credit_transactions(owner_id)`** (không có scan path). **Bài học:** partial index có prefix trùng FK convention index → EF model-differ đòi DROP FK index (coi là thừa) → phải **khai FK index tường minh** để giữ (partial `WHERE status='Reserved'` không phủ referential lookup all-status của FK). **Kế:** DB10 (optimistic concurrency `UseXminAsConcurrencyToken`, 4 service) · DB14 (audit `updated_at`) · DB16 (Campaign God-table) ‖ DB14 · DB6/7/8/15 · P2 (DB16/17). ⚠ **L3 EXPLAIN = verify tay** (apply Postgres + `SET enable_seqscan=off; EXPLAIN` sweeper query → Index Scan) tại apply-window — index add **online-safe** (không backfill/dọn orphan như DB9 FK).

### ✅ Vòng S6 — DB Hardening đợt 8 (2026-07-17) — DB14 ‖ DB16 passing (2 worker disjoint Payment+Interview ‖ Campaign, off `2d98208` = main sau khi DB5 merged)
> DB14 (audit `updated_at`) + DB16 (tách God-table). **DB5 đã merge main (PR #65-68) giữa chừng** → round base = origin/main sạch (hết cần stack như kế hoạch). PM supervised: 2 worker worktree cô lập (DB14 gộp Payment+Interview 1 worker cho pattern đồng nhất · DB16 solo Campaign). Supervisor review từng diff + refine DB16 normalize + octopus-merge verify độc lập. Giao **1 branch** `s6/db14-db16` (1 PR — theo ý user).

| Task | Service | Nội dung | Trạng thái |
|---|---|---|---|
| **DB14** 🟡 | Payment | `IHasUpdatedAt`+override SaveChanges (stamp `Modified`) + `.SetProperty(updated_at)` ExecuteUpdate (WebhookService order Paid/CreditAccountService reserve Consume-Release); `updated_at` orders/credit_reservations/product_packages; `orders.status→varchar(20)`; `product_packages.type` int→string + data-migration `USING (CASE…)` Postgres | ✅ **passing (`e154dc2`)** · migration `AddAuditColumnsAndTypes` reversible, không apply Neon · Payment 137→141 · append-only credit_transactions/payment_transactions KHÔNG stamp |
| **DB14** 🟡 | Interview | `IHasUpdatedAt`+override + `.SetProperty` SessionAbandonSweeper/SessionScoringNotifier; `updated_at` practice_sessions | ✅ **passing (`1124903`)** · migration `AddSessionUpdatedAt` · Interview 242→244 · scope chỉ practice_sessions |
| **DB16** 🟠 | Campaign | RENAME campaign_candidates→cv_submission (giữ PK/data/2 FK inbound theo OID) + bảng mới campaign_membership; enum `CvSubmissionStatus` bỏ `Joined` (=membership row tồn tại); screening=rename thuần, membership readers (Participation/Ranking/FaceVerify/SessionFlag) đổi bảng; query filter re-plumb (DB13 warning-throw PASS); backfill `INSERT…SELECT` + normalize `Joined→Invited` | ✅ **passing (`3cd8741` + refine `5d6104a`)** · migration `SplitCampaignCandidates` reversible, không apply Neon · Campaign 191 (Join_Duong2 = 2-row assert) · ✅ **L3 Postgres verify PASSED** (throwaway PG18: Up backfill/rename/normalize 0 orphan/0 loss + Down reversible incl path-1 reconstruct) |

> Base test sau vòng S6 đợt 8: **Auth 64 · Payment 141 · Campaign 191 · Interview 244** (=640), `dotnet build` 0 error · `has-pending`=No changes cả 3. **Quyết định user chốt:** DB16 = **full big-bang split** (không additive) · `product_packages.type` = **convert string** (GEN-2) · **stack on DB5** (nhưng DB5 merged main #65-68 giữa chừng → base sạch, workers tự off origin/main). **Supervisor refine:** DB16 backfill normalize `Joined→'Invited'` (worker để `'Pending'`) — mọi join theo sau invitation + forward giữ cv_submission=`Invited` → đồng nhất existing↔new + giữ fact shortlist. **Bài học:** SQLite `EnsureCreated` bỏ qua migration → **backfill/rename/data-migration KHÔNG được CI test** → **L3 Postgres verify tay** — **✅ đã chạy PASSED** (throwaway PG18, seed path-1/path-2 Joined + child FK: Up 4 cv_submission/2 membership 0 orphan/0 loss · normalize Joined→Invited · Down reversible incl path-1 reconstruct). Còn apply-window data thật: **dedup `(campaign_id,candidate_id)` trùng** trước add UNIQUE. **Kế:** DB10 (xmin optimistic concurrency, 4 service) · DB6/7/8 · DB15 · DB17 (FileService). ⚠ apply DB16: dedup + kiểm FK trước; DB14 online-safe (trừ product_packages.type convert cần data-migration).

### ✅ Vòng S6 — DB Hardening đợt 9 (2026-07-17) — DB10 ‖ DB15 ‖ AI2 passing (4 worker disjoint theo service-group, worktree thật off `eadbfff`)
> 3 task giao 1 vòng, 4 worker disjoint: **W1 Auth+Payment · W2 Interview · W3 Campaign · W4 AIService+ScoringJobPublisher** (DB10+DB15 đụng cùng DbContext Payment/Interview/Campaign → chia theo service-group, không theo task, để 0 EF-snapshot conflict). Gộp octopus-merge 4 nhánh → 1 branch `s6/db10-db15-ai2` → 1 PR. **PM independent verify** (build + full test + has-pending + pytest + **L3 Postgres thật**). ⚠ **2 worker (W1/W2) chạm session-limit giữa chừng → resume (SendMessage) sau reset 21:10, giữ nguyên work trong worktree.**

| Task | Service(s) | Nội dung | Trạng thái |
|---|---|---|---|
| **DB10** 🟠 | Auth·Payment·Interview·Campaign | Optimistic concurrency: **shadow property `xmin`** (uint→cột hệ thống `xid`, `ValueGeneratedOnAddOrUpdate`+`IsConcurrencyToken`), gate `IsNpgsql()`; **no DDL** (system column) | ✅ **passing** · `ba7da0b`·`c53c8ea`·`2c1aaf1` · ⚠ **Npgsql efcore.pg 10 bỏ `UseXminAsConcurrencyToken()`** (task text sai) → map tay, **Context7-confirmed**; `credit_accounts` inert (ExecuteUpdate-only, defense-in-depth); org/campaigns/practice_sessions tracked-RMW → hiệu lực |
| **DB15** 🟡 | Payment·Interview·Campaign | drop `subscriptions` · merge `rubric_anchors`→`rubric_levels.example_answers` jsonb · thay index trùng `practice_answers` bằng non-unique `session_id` · CHECK weight **(0,1]** + pass_score_pct [0,100] | ✅ **passing** · `521043e`·`a6c9d83`·`c7511da`·`2c1aaf1` · **ScoringJob wire contract UNTOUCHED** (`ScoringCriteriaBuilder` byte-identical) · ✅ **L3 Postgres PASSED** (backfill Up ordered + `[]`-default, Down 0-loss reversible) · weight = **(0,1]** khớp code (user chốt, không [0,1]) |
| **AI2** 🔴 | AIService (+1 file Interview) | DLX `scoring_pipeline_dlx` + DLQ `scoring_pipeline_dead_queue` (`declare_topology`); main queue thêm `x-dead-letter-exchange` → cả 2 `nack(requeue=False)` auto-route DLQ; `.NET ScoringJobPublisher` arg KHỚP | ✅ **passing** · `94afa90` · pytest 62→66 · pin `aio-pika==10.0.1` · ⚠ **deploy:** queue LIVE `arguments=None` → recreate (drain→delete→redeclare) HOẶC RabbitMQ **policy** |

> Base test sau vòng S6 đợt 9: **Auth 68 · Payment 143 · Campaign 202 · Interview 251 = 664 .NET** (+ AIService pytest **66**), `dotnet build` solution 0 error · `has-pending-model-changes`=No changes cả 4. **PM verify độc lập PASSED:** octopus-merge 0 conflict (4 nhánh disjoint file) · full-solution build 0 err · 664 test (1 flake Interview KHÔNG tái hiện qua 3 run — timing dưới tải song song, không phải regression) · **L3 Postgres tay** (local PG socket `/tmp:5432`): 4 idempotent script apply clean fresh DB + **rubric_anchors backfill seed-Up-Down** (level 2 anchor→`["ANS_A","ANS_B"]` ordered · level 0 anchor→`[]` · Down tái tạo 2 row 0-loss). **Bài học vòng này:** (1) **verify API thư viện qua Context7 TRƯỚC khi đưa vào brief** — brief giả định `UseXminAsConcurrencyToken()` (không có ở Npgsql 10) → 3 worker phải tự sửa; (2) **PM review đọc CODE thật, không keyword-grep** — grep hẹp giấu dòng `{`/`.ValueGeneratedOnAddOrUpdate()` → PM báo nhầm 2 "bug" (worker phản biện đúng bằng test pass); (3) **backfill data-migration BẮT BUỘC L3 Postgres tay** (SQLite EnsureCreated bỏ qua migration). ⚠ apply Neon: dedup/dọn row weight ngoài (0,1] & pass_score ngoài [0,100] & `subscriptions` rỗng trước; AI2 recreate queue/policy. Follow-up: DB15 weight-normalize round có thể ra `0.0000`→vi phạm CHECK (W3 flag, pathological, out-scope). **Kế S6:** DB6/7/8 · DB17. **Kế S7:** AI1/AI3/AI5.

## Vấn đề đã biết / cần xác minh
- ~~**E1 → e2e thật bị chặn bởi D2**~~ ✅ **GỠ:** D2 membership + BK14 reserve xong → luồng B2B end-to-end (campaign → join → start → làm bài → chấm → ranking) **chạy trọn thật live 2026-07-15/16** (§Đợt 1–3b).
- ~~**Campaign chưa wire `org_id`**~~ ✅ **GỠ (BK4, commit ea480e7):** `campaigns.employer_id`→`org_id` + ownership/credit theo org; owner từ JWT claim `org_id`. *(Còn ops **BK4b:** backfill rows cũ user→org qua `org_members` TRƯỚC apply Neon.)*
- **PaymentService** ✅ đã refactor sang **org-credit + reserve/consume + postpaid** (P1–P8). **✅ Đã wire deploy (2026-07-13):** `Dockerfile` (trước thiếu) + CI build+push image `paymentservice` (ci.yml, nay build **5** image) + gateway route `/api/v1/payment/{**}` (payment-cluster) + service trong compose server (DB `isas_payment`). AIService vẫn deploy tay trên Mac.
- **✅ Deploy-prep migration (2026-07-13):** đã **squash 33 migration → 1 `InitialCreate`/service** (Auth/Payment/Campaign/Interview) — apply **tay** lên postgres **local** (drop & tạo lại 4 DB rỗng: `isas`/`isas_interview`/`isas_campaign`/`isas_payment` → `dotnet ef database update` hoặc SQL script; seed rubric B2C bake sẵn). Giữ rule no-auto-migrate. Test giữ 451 (EnsureCreated không đụng migration).
- **AIService** `/suggest-criteria` đã **live HTTP** nhưng deploy qua `docker cp`+restart → **ephemeral**; muốn permanent phải **rebuild image** từ Dockerfile. Các vấn đề nền còn nguyên: 🔴 `/ai/**` public không auth · 🔴 Whisper `large-v3` CPU chậm · 🔴 prompt injection bộ chấm · 🔴 `nack` không DLQ ([services/ai.md](services/ai.md) §Vấn đề).
- ✅ **Trần `orderCode` PayOS đã verify** (D12, 2026-06-28): ≤ **9.007.199.254.740.991** (2^53−1) → snowflake 64-bit vượt trần (loại). Thêm ràng buộc PayOS: `description` ≤25 ký tự, webhook **HMAC-SHA256** — bảng đầy đủ [services/payment.md](services/payment.md) §PayOS.
- **CI/CD chung Neon (DB server):** không tự apply migration lên DB chung — schema apply qua pipeline/tay trước deploy.

## Bước tiếp theo (thứ tự đề xuất)
> ✅ V1: **E2,D1,P1,BC6**. ✅ V2: **P7,E3,E4,BC13**. ✅ V3: **P4,C11,E8**. ✅ V4: **P5,C12,BC11**. ✅ V5: **P6,E5,BC9**. ✅ V6: **P8a,E6,BC7**. ✅ V7: **E7,C13,BC2**. ✅ V8: **C14,BC8,BK7**. ✅ V9: **BK11,BK1,BK12**. ✅ V10: **P2,C15,BC12**. ✅ V11: **BC14,BK4,BC10-AI**. ✅ V12: **D2**. ✅ V13: **BC10(full),P3,D3**. ✅ V14: **BC15,D4,P8b**. ✅ V15: **I2,A4**. ✅ V16: **BK17,BK18,BK16** (BK-cleanup). ✅ V17: **E9**. ✅ V18: **E10**. ✅ V19: **E11,A6** (E9→E10→E11 xong + org members). ✅ V20: **BC7b** (wire billing CV, chốt BK5). ✅ V21: **BK15,BK6,A6b** (backlog cleanup — non-owner→404 · cv-analysis jobCategory→400 · org member joined_at/đổi-role/xoá). ✅ V22: **A5** (SOLO cross-cutting — auth gate `[Authorize(Roles)]` mọi service, 0 endpoint trần). ✅ V23: **BC16** (rubric cá nhân B2C — candidate tự CRUD theo JobCategory, không admin; đảo hướng BK3). Frontier mới bên dưới.
> **✅ P2 verify tay XONG (2026-07-13/15/16):** luồng tiền thật PayOS sandbox (payment-link + webhook HMAC → credit) chạy trên server deploy cho **cả B2C và B2B** (§Đợt 1–3b). **BK14 done** (commit 3399765) → B2B reserve org credit tại Start.
> **✅ Đã chốt (2026-07-12):** **BK5**→CV analysis **tính phí** (D22; còn wire code BC7b) · **BK9**→event-bus convention vào architecture.md §6.1 (doc-only, xong) · **BK10**→**bỏ** (BC3/BC4 = covered-by-E7). *(Không còn DECISION treo trong backlog.)*
1. **D2 ✅ · D3 ✅ · D4 ✅ · BK16 ✅ (eedfeef) · BK14 ✅ (3399765)** — membership model + resume regression + reissue invitation + resume Interview-side khoá hợp đồng + **reserve org credit tại Start** (BK14). **Gỡ chặn HTTP entry B2B.** **✅ E2E B2B thật done live 2026-07-15/16** (join→start→làm bài→chấm→ranking Pass). → **còn (first-class gap B2B):** **D5** invitation-email consumer (**✅ code merged main — branch `feat/b2b-email-anticheat`: `InvitationEmailConsumer`+`CampaignEmailSender` SMTP; còn cấp SMTP creds thật + verify live**) · **SEC1** anti-cheat (**🟡 backend scaffold + AIService face-detect (InsightFace) merged main; còn FE emit tín hiệu webcam/tab-switch/paste ngoài repo + verify live**) · BK14 tail (retake sau Completed · JWT-refresh · orgName resolve).
2. **P2 ✅ · P3 ✅ · P8b ✅ · A4 ✅ · BK17 ✅ (ee72a0d)** — engine tiền đủ: mua pack (P2) · active-polling (P3) · invoice postpaid (P8b) · HrMember→403 billing (A4) · postpaid Overdue-block + close race fix (BK17). **✅ PayOS sandbox verify tay XONG** (payment-link + webhook HMAC → credit, live 2026-07-13/15) → **luồng tiền E2E-Xong** cả B2C+B2B; **BC1** (mua pack ví User) verified live. BK17 còn (3) ~~amount int overflow~~ ✅ fixed (`AmountVndToBigint`, commit 58e6919) + (4) due_at/paid_at — để riêng khi cần.
3. **S5 B2C roadmap HOÀN TẤT** (BC11 ✅ · BC9 ✅ · BC7 ✅ · BC2 ✅ · BC8 ✅ · BC12 ✅ · BC14 ✅ · BC10 ✅ · **BC15 ✅ b975f8c**): pipeline luyện cá nhân hoá (roadmap → milestone → lesson → improvement → report) đủ engine. → **BC3/BC4** = **covered-by-E7** (chốt BK10 — E7 consume/release generic mọi session; còn test e2e broker) · **BC7b ✅ (87f3367)** wire cv-analysis billing (reserve→consume ví User; hết→402) · BC1 (chờ P2 e2e) · **e2e B2C thật** (compose + Gemini) verify tay.
4. **Lọc CV B2B** (C13 ✅ · C14 ✅ · **C15 ✅**): pipeline sàng CV → shortlist → bulk invite đã đủ; còn e2e broker thật (compose) + chốt D2 để ứng viên vào bài.
5. **I2 ✅ (443e831) · BK18 ✅ (00fb678)** — per-question time-limit + `session.deadline` + sweeper auto-submit/abandon; Campaign đã gửi `expires_at` → B2B Deadline chạy thật. → còn lesson B2C release-credit-treo (Deadline=null, thuộc BC14/E7).
6. **Roadmap ôn tập B2C (D20) HOÀN TẤT:** `BC12` ✅ · `BC13` ✅ · `BC14` ✅ · `BC10` ✅ · **`BC15` ✅ (b975f8c)** — engine đủ; còn e2e thật (compose + Gemini) verify tay.
7. **Auth: A4 ✅ · A5 ✅ (v22·e6fb2c1) · A6 ✅ (ac40761) · A6b partial ✅ (v21·0a56902)** — **toàn bộ S1 Auth xong**: HrMember→403 billing (A4); **A5 auth gate `[Authorize(Roles)]` mọi service** (per-endpoint, bịt lỗ Package/admin-close, siết Employer/Candidate/Admin, guard reflection "0 endpoint trần"); OrgAdmin thêm HrMember (A6); A6b joined_at + đổi role + xoá. Còn A6b: invite-email · attach-account (phase-2); **A5 layer-3 live 401/403 = verify tay** khi có stack.
8. **Phase 0 còn lại**: `P0.1` (compose máy sạch), `P0.2` (`make setup/test/check`), `P0.5` (readiness + checkpoint). *(P0.3/P0.4 ✅.)*
9. **Backlog** (đã gom nhóm — xem §Backlog [tasks.md](tasks.md)): 📋 **ops** (trước apply Neon) BK2/BK4b · 🛠️ **cleanup** BK6/BK8/BK13/BK15/BK17(3)(4) · 🔵 **feature follow-up** BC7b/BK3/BK14/E9b/E10b/E11b/A6b. *(✅ done 9 + 🔒 chốt BK5/BK10; 🔴 DECISION group đã trống.)*
11. **FRONTIER SAU S8:** S8 (P0 `DB20`-`DB25` + P1 `DB26`-`DB31`) **đã xong** — còn `DB25b` (bọc 6 site transaction bằng `CreateExecutionStrategy` rồi mới bật retry cho Interview+Payment) và **8 finding P2 redesign** chưa vào bảng (liệt kê ở header §S8 tasks.md): tách `ranking_overrides` · `answer_scores.reasoning` ra bảng phụ · CHECK constraint enum toàn hệ · rename `candidate_criterion_scores.candidate_id` · soft-delete+audit Interview · version outbox event type · bảng refunds/billing_periods/money-audit · partition + currency/tax. Thêm khi chạm feature liên quan.

10. **S8 money-loss & security** (từ DB architecture review 2026-07-19, xem §Cập nhật 2026-07-19 ở đầu file): **`DB20`-`DB25` (P0) làm TRƯỚC mọi task khác** — đây là defect **đang chạy live** làm mất tiền thật, không phải hardening dự phòng. Thứ tự đề xuất: `DB20` (khách trả tiền không nhận credit — sửa trước lần deploy kế) → `DB21` (1 dòng WHERE) → `DB22` → `DB25` → `DB24` → `DB23`. Sau đó **P1 `DB26`-`DB31`**, trong đó `DB28` (retention `refresh_tokens`/outbox) là **value-per-effort cao nhất cả nhóm**. `DB20`/`DB21`/`DB22`/`DB25` đều **chạm Payment** → KHÔNG disjoint, chạy tuần tự 1 PR/task; `DB23`(Campaign) ‖ `DB24`(Auth) ‖ `DB27`/`DB29`/`DB30`(Interview) mới song song được.

> Quy trình **vào ca / tan ca**: xem [../AGENTS.md](../AGENTS.md).
